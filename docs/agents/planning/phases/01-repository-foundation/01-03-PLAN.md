---
phase: 01-repository-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - docs/MODULE_CONTRACTS.md
  - docs/RISK_REGISTER.md
  - docs/ADR/ADR-006_module-contracts-layout-and-citation-policy.md
  - .github/pull_request_template.md
  - docs/EXECUTION_BLUEPRINT/03_MODULES.md
  - docs/EXECUTION_BLUEPRINT/12_RISK_REGISTER.md
autonomous: true
requirements: [REQ-scope-in, REQ-risk-register, REQ-milestones]
security_asvs_level: 1
security_block_on: high

must_haves:
  truths:
    - "docs/MODULE_CONTRACTS.md is tracked, states the contract-first rule and the entry format in its header, and carries one entry each for src/ambo/common/config.py, src/ambo/common/errors.py and src/ambo/common/logging.py"
    - "MODULE_CONTRACTS.md declares that it is the contract of record and that docs/EXECUTION_BLUEPRINT/03_MODULES.md is superseded and frozen as of Phase 1, so no published document depends on reading a gitignored file"
    - "docs/RISK_REGISTER.md is tracked and carries all nine Charter risks R-1 through R-9 verbatim in ID order, each with a mitigation, fallback and detection cell, plus the phase-1 findings recorded under IDs outside the Charter range"
    - "ADR-006 exists, follows the TEMPLATE.md four-section shape, and covers all four of its D-05 topics: the SPEC-08 section 2 repoint at docs/MODULE_CONTRACTS.md, the D-14 layout additions, the D-04 standing rule with its two named deferrals, and the D-18 commit-history note"
    - ".github/pull_request_template.md carries only the judgment items the repo-layout test cannot check, with no item duplicated from tests/unit/test_repo_layout.py"
    - "MODULE_CONTRACTS entries are ordered by module import path and the layout guard compares sets, so entry order never changes the guard's verdict and two orderings of the same entries are equally valid"
    - "Risk IDs never collide: R-1 through R-9 are reserved for the Charter's own risks and every finding added during the build takes an ID at R-10 or above, so an appended finding can never shadow a Charter risk"
    - "No risk-register row may carry an empty mitigation, fallback or detection cell; a risk with an unknown fallback records the words none identified plus an owning phase rather than a blank"
    - "Risk entries are ordered by ID with Charter risks first; IDs are unique so no ordering tie is possible and the sort is total"
    - "Reviewing the register at a second phase entry is idempotent: the review appends one dated review line naming the phase and the reviewer, and never duplicates, restates or renumbers an existing risk row"
    - statement: "docs/RISK_REGISTER.md has no concurrent-writer contract and needs none: it is a single-author markdown file in a single-developer offline repository. The only interruption case is a half-written edit, and git's working-tree versus commit boundary already makes the published state atomic."
      verification: backstop
  artifacts:
    - path: "docs/MODULE_CONTRACTS.md"
      provides: "Tracked contract of record for every src/ambo module (D-02, D-03)"
      contains: "src/ambo/common/config.py"
    - path: "docs/RISK_REGISTER.md"
      provides: "Tracked risk register seeded with R-1..R-9 plus build findings (D-08, REQ-risk-register)"
      contains: "R-9"
    - path: "docs/ADR/ADR-006_module-contracts-layout-and-citation-policy.md"
      provides: "The four D-05 decisions in one ratified ADR"
      contains: "## Spec deviations"
    - path: ".github/pull_request_template.md"
      provides: "The 06_CHECKLISTS [STD] judgment items (D-24)"
      contains: "BUILD_LOG"
  key_links:
    - from: "docs/MODULE_CONTRACTS.md"
      to: "src/ambo/common/config.py"
      via: "one entry per module keyed by import path; tests/unit/test_repo_layout.py fails when a module has no entry"
      pattern: "src/ambo/common/"
    - from: "docs/ADR/ADR-006_module-contracts-layout-and-citation-policy.md"
      to: "docs/SPEC-08_engineering.md section 2"
      via: "the Spec deviations table records the repoint and the four layout additions with their REQ IDs"
      pattern: "SPEC-08"
    - from: "docs/RISK_REGISTER.md"
      to: "PROJECT_CHARTER.md section 6"
      via: "R-1..R-9 copied verbatim with their Charter mitigations, then expanded with fallback and detection"
      pattern: "R-[1-9]"
  prohibitions:
    - requirement_id: REQ-risk-register
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT remove, downgrade, soften or reword a risk in order to make a gate, a review or a report read better. A risk closes on evidence — a dated BUILD_LOG reference or an ADR — and its row is struck through with the closing reference, never deleted."
---

<objective>
Write the four tracked governance documents this phase adds beyond the WBS: the module-contract
document of record, the risk register, ADR-006, and the pull-request template.

Purpose: three of these exist to close a standing rule. D-04 forbids a published document citing an
unpublished one, and SPEC-08 section 2 currently points at `03_MODULES.md`, which D-01 keeps
gitignored — `MODULE_CONTRACTS.md` is what makes that citation resolve. REQ-risk-register wants a
reviewable artifact, and `12_RISK_REGISTER.md` is likewise gitignored. D-24 gives the
`06_CHECKLISTS [STD]` judgment items a literal home so the machine-checkable half can move into a
test without losing the rest.

Output: `docs/MODULE_CONTRACTS.md`, `docs/RISK_REGISTER.md`, `ADR-006`,
`.github/pull_request_template.md`, plus superseded headers on the two frozen blueprint files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-repository-foundation/01-CONTEXT.md
@PROJECT_CHARTER.md
@docs/SPEC-08_engineering.md
@docs/SPEC-09_governance_quality.md
@docs/ADR/ADR-000_document-precedence-and-blueprint-defaults.md
</context>

<assumption_delta_decision>
**no-change.** The assumption-delta detector fired on stale ROADMAP text claiming the repository is
on `master` with zero commits; CONTEXT.md correction #3 records that the repository is on `main`
with baseline commit `1851f39`. False positive on dead text.
</assumption_delta_decision>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/MODULE_CONTRACTS.md and freeze 03_MODULES.md</name>
  <files>docs/MODULE_CONTRACTS.md, docs/EXECUTION_BLUEPRINT/03_MODULES.md</files>
  <read_first>
    - `docs/EXECUTION_BLUEPRINT/03_MODULES.md` — the global contract preamble (config, logging, errors, determinism, thread safety, testing, reproducibility), section 1.1 `config.py` and section 1.2 `logging.py`, and the dependency-direction block near the end listing the FORBIDDEN edges
    - `docs/SPEC-08_engineering.md` section 2 — the contract-first rule this document is the target of
    - `.planning/phases/01-repository-foundation/01-CONTEXT.md` sections D-01, D-02, D-03 — what is promoted, what is frozen, and the grow-as-you-go scope
    - `docs/SPEC-09_governance_quality.md` section on GB section 8 — governance showpieces are scripts, which is why this document is paired with a test rather than a checklist
  </read_first>
  <action>
Create `docs/MODULE_CONTRACTS.md`, tracked. Its header states four things: that it is the contract
of record for every module that exists in `src/ambo/`; that `docs/EXECUTION_BLUEPRINT/03_MODULES.md`
is superseded as of Phase 1 and remains the design input for contracts not yet written; that the
contract-first rule from SPEC-08 section 2 means a new module inside a listed package requires its
entry here in the same pull request; and that `tests/unit/test_repo_layout.py` enforces the
one-entry-per-module invariant mechanically, so this document cannot silently fall behind the tree.

Define the entry format explicitly and use it consistently, because the layout guard parses it:
a level-3 heading that is exactly the module's repository-relative path, followed by
**Purpose**, **Public API** (one bullet per exported symbol with its signature),
**Invariants**, **Failure modes**, **Testing**. State in the header that entries are ordered by
import path and that the guard compares sets, so ordering is a readability convention rather than
a contract.

Write exactly three entries, for what Phase 1 ships:

- `src/ambo/common/errors.py` — Purpose: the typed exception root and its config subclass. Public
  API: `AmboError(Exception)` as the project root exception, and `ConfigError(AmboError)`.
  Invariants: exception messages never contain private-drop content or paths; subclasses are added
  per module as those modules land. Failure modes: none, this module raises nothing itself.
  Testing: raised and asserted from `tests/unit/test_config.py`.
- `src/ambo/common/config.py` — copy the Public API, Invariants, Failure modes, Extension points
  and Testing content from `03_MODULES.md` section 1.1, adapted to the symbols this phase actually
  ships: `PathsConfig`, `SamplerConfig`, `ScenarioConfig`, `Settings`, `load_settings()`,
  `repo_root()`.
- `src/ambo/common/logging.py` — from `03_MODULES.md` section 1.2: `get_logger(name)`,
  `PrivatePathFilter`, the fixed `LOG_FORMAT`, and the `AMBO_LOG_LEVEL` default.

Add a short **Dependency directions** section reproducing the allowed-import table and the four
forbidden edges from `03_MODULES.md`, since two of those edges become guard tests in plan 01-07 and
this is the tracked place a reviewer can read them.

Then handle the freeze. D-01 keeps `docs/EXECUTION_BLUEPRINT/` gitignored and treats commit
`1851f39` as canonical, so the authoritative record of the supersession is this tracked document
plus ADR-006 — not an edit to the blueprint. Add the superseded banner to the local working copy of
`docs/EXECUTION_BLUEPRINT/03_MODULES.md` anyway, as a read-surface signpost: a blockquote at the
very top stating that the file is superseded as of Phase 1 by `docs/MODULE_CONTRACTS.md`, is never
edited again, and remains the design input for unwritten contracts. Confirm with
`git status --porcelain docs/EXECUTION_BLUEPRINT/` that this edit stays untracked and unstaged.
  </action>
  <acceptance_criteria>
    - `git ls-files docs/MODULE_CONTRACTS.md` returns the path
    - The file contains exactly three level-3 headings matching `src/ambo/`, one per module shipped this phase
    - Each of the three entries contains all five section labels: Purpose, Public API, Invariants, Failure modes, Testing
    - The header names `03_MODULES.md` as superseded and states the contract-first same-pull-request rule
    - `git status --porcelain docs/EXECUTION_BLUEPRINT/` returns nothing, proving the blueprint edit stayed untracked
  </acceptance_criteria>
  <verify>
    <automated>[ -n "$(git ls-files docs/MODULE_CONTRACTS.md)" ] && [ "$(grep -cE '^### src/ambo/' docs/MODULE_CONTRACTS.md)" = "3" ] && [ "$(grep -c '\*\*Public API\*\*' docs/MODULE_CONTRACTS.md)" = "3" ] && [ "$(grep -c '\*\*Invariants\*\*' docs/MODULE_CONTRACTS.md)" = "3" ] && [ "$(grep -c '\*\*Testing\*\*' docs/MODULE_CONTRACTS.md)" = "3" ] && grep -q 'superseded' docs/MODULE_CONTRACTS.md && [ -z "$(git status --porcelain docs/EXECUTION_BLUEPRINT/)" ]</automated>
  </verify>
  <done>The tracked contract of record exists with three parseable entries and the dependency-direction table, and the blueprint freeze is signposted locally without breaking D-01.</done>
</task>

<task type="auto">
  <name>Task 2: Seed docs/RISK_REGISTER.md with the Charter risks and the build findings</name>
  <files>docs/RISK_REGISTER.md, docs/EXECUTION_BLUEPRINT/12_RISK_REGISTER.md</files>
  <read_first>
    - `PROJECT_CHARTER.md` section 6 — the risk table R-1..R-9 with likelihood, impact and mitigation, copied verbatim
    - `docs/EXECUTION_BLUEPRINT/12_RISK_REGISTER.md` — the mitigation/fallback/detection triad expansion this document promotes, and the per-milestone framing
    - `.planning/phases/01-repository-foundation/01-CONTEXT.md` section D-08 — the seeding instruction and the `g++` finding
    - `.planning/phases/01-repository-foundation/01-RESEARCH.md` Pattern 2 residual risk and the Note for RISK_REGISTER — the two additional findings with evidence
    - `.planning/REQUIREMENTS.md` REQ-risk-register — the acceptance shape
  </read_first>
  <action>
Create `docs/RISK_REGISTER.md`, tracked. Header states: that the Charter section 6 risks R-1..R-9
remain authoritative and are copied here verbatim rather than restated; that IDs R-1..R-9 are
reserved for the Charter so every finding discovered during the build takes an ID at R-10 or above;
that the register is reviewed at every phase entry and that a review appends exactly one dated line
to the Review log section, never a copy of the register; that a risk closes on evidence with a
BUILD_LOG or ADR reference and its row is struck through with that reference rather than deleted;
and that no cell in the Mitigation, Fallback or Detection columns may be blank — an unknown
fallback is written as `none identified` plus an owning phase.

The main table has columns `ID | Risk | Likelihood | Impact | Mitigation | Fallback | Detection |
Owner phase | Status`. Rows R-1 through R-9 in numeric order carry the Charter's own Risk,
Likelihood, Impact and Mitigation text verbatim; the Fallback and Detection cells are the
promotion from `12_RISK_REGISTER.md`'s triad expansion, and the Owner phase is drawn from the
roadmap phase that owns the mitigating mechanism.

Then append the build findings, each with a full triad:

- **R-10 — no C compiler on the development machine.** `g++` is absent, so PyTensor falls back to
  its NumPy backend. Impact: threatens the normative per-fit ceiling in `07_QUALITY_STANDARDS`
  Part A and the roughly-14-fit compute ledger. Mitigation: install MSVC Build Tools or an m2w64
  toolchain, decided in Phase 4. Fallback: accept the NumPy backend and re-budget the compute
  ledger. Detection: a compiler check in `make setup`. Owner phase: 4.
- **R-11 — leak-scan pattern set is deliberately narrow at M0.** D-26 scopes the M0 subset to three
  high-precision, path-scoped checks so the gate is still trusted in Phase 6. Impact: a leak class
  outside the subset is not caught until the set widens. Mitigation: widen the set when real data
  lands. Fallback: full mode with the private-drop blocklist covers the specific values.
  Detection: the Phase 6 intake tasks T-501..T-506 revisit the pattern set. Owner phase: 6.
- **R-12 — the `pymc<6` upper bound is load-bearing.** PyMC 6.0, PyTensor 3.0 and ArviZ 1.0 are
  discussed upstream as breaking releases; the SPEC-08 section 3 bound already guards against them.
  Impact: widening the bound without an ADR would silently admit a breaking major. Mitigation:
  EB-030 requires an ADR for any bound change. Fallback: pin to the last known-good resolved
  version. Detection: `uv lock` diff review in any pull request touching `pyproject.toml`.
  Owner phase: standing.
- **R-13 — the Chocolatey `make` package provenance is unconfirmed.** RESEARCH.md could not fetch
  the package page. Impact: a different GNU Make lineage on the Windows CI leg than on the
  development machine could produce a CI-only Makefile behavior difference. Mitigation: read the
  installed version string from the first real CI run and compare against the development
  machine's GNU Make 4.4.1. Fallback: pin the Chocolatey package version, or install the
  ezwinports build directly. Detection: the `test` job's Windows leg prints `make --version`.
  Owner phase: 1, closing evidence recorded by plan 01-09.

Close with a `## Review log` section holding one dated line: the Phase 1 entry review, naming the
phase, the date, and the count of open risks.

Add the same superseded banner to the local working copy of
`docs/EXECUTION_BLUEPRINT/12_RISK_REGISTER.md` that Task 1 added to `03_MODULES.md`, pointing at
`docs/RISK_REGISTER.md`, and confirm it stays untracked.
  </action>
  <acceptance_criteria>
    - `git ls-files docs/RISK_REGISTER.md` returns the path
    - `grep -cE '^\| *R-[0-9]+ ' docs/RISK_REGISTER.md` returns at least 13
    - Every Charter ID R-1 through R-9 appears exactly once as a row ID
    - No table row contains an empty Mitigation, Fallback or Detection cell — no row matches an empty-cell pattern such as `| |`
    - A `## Review log` section exists with at least one dated Phase 1 line
    - `git status --porcelain docs/EXECUTION_BLUEPRINT/` returns nothing
  </acceptance_criteria>
  <verify>
    <automated>[ -n "$(git ls-files docs/RISK_REGISTER.md)" ] && [ "$(grep -cE '^\| *R-[0-9]+ ' docs/RISK_REGISTER.md)" -ge 13 ] && [ "$(grep -oE '^\| *R-[1-9] ' docs/RISK_REGISTER.md | sort -u | wc -l)" = "9" ] && [ "$(grep -cE '^\| *R-[0-9]+ .*\|[[:space:]]*\|' docs/RISK_REGISTER.md)" = "0" ] && grep -q '^## Review log' docs/RISK_REGISTER.md && [ -z "$(git status --porcelain docs/EXECUTION_BLUEPRINT/)" ]</automated>
  </verify>
  <done>The tracked register carries all nine Charter risks verbatim plus four build findings at R-10 and above, every row has a complete triad, and the review log has its Phase 1 entry.</done>
</task>

<task type="auto">
  <name>Task 3: Write ADR-006 and the pull-request template</name>
  <files>docs/ADR/ADR-006_module-contracts-layout-and-citation-policy.md, .github/pull_request_template.md</files>
  <read_first>
    - `docs/ADR/TEMPLATE.md` — the four-section shape ADR-006 must follow (created by plan 01-01)
    - `docs/ADR/ADR-000_document-precedence-and-blueprint-defaults.md` — ADR-006 must not contradict D-1 (scoped precedence) or D-2 (BP-D-01..20 accepted wholesale)
    - `docs/SPEC-08_engineering.md` section 2 and section 8 — the layout text ADR-006 repoints and amends, and the EB-082 rule ADR-006 cites
    - `docs/SPEC-09_governance_quality.md` GB-201 — the required ADR sections
    - `docs/EXECUTION_BLUEPRINT/06_CHECKLISTS.md` — the `[STD]` merge-readiness list, split by D-23 and D-24 into machine-checked and judgment halves
    - `.planning/phases/01-repository-foundation/01-CONTEXT.md` sections D-04, D-05, D-14, D-18, D-24 — the four ADR topics and the template scope
  </read_first>
  <action>
Create `docs/ADR/ADR-006_module-contracts-layout-and-citation-policy.md` following `TEMPLATE.md`
exactly: status Ratified, dated today, with `## Context`, `## Decision`, `## Consequences` and
`## Spec deviations`.

The Decision section carries four numbered decisions, one per D-05 topic:

1. **SPEC-08 section 2's contract-first citation repoints at `docs/MODULE_CONTRACTS.md`.** The
   section currently names `03_MODULES`, which is gitignored per D-01. A published specification
   citing a file no reader can open violates the D-04 standing rule. `03_MODULES.md` is frozen and
   remains the design input for unwritten contracts.
2. **SPEC-08 section 2's canonical layout gains four entries: `.github/`, `.planning/`, `uv.lock`
   and `.gitattributes`.** All four are permanent and load-bearing, so their absence from the
   listing was an authoring gap rather than a rule. This is what makes the no-allowlist repository
   layout test in plan 01-07 possible. Per D-14 this is recorded here rather than in a separate ADR.
3. **Standing rule: no published document may cite an unpublished one.** Record the two known
   violations with named owners: the normative per-fit ceiling in `07_QUALITY_STANDARDS` Part A,
   cited by SPEC-08 section 5, moves into `config/settings.yaml` in Phase 4; the DL-1 release probe
   in `11_ACCEPTANCE_CRITERIA` section 4, cited by SPEC-08 section 5, Charter DL-1 and ADR-000,
   gets a tracked home in Phase 9. Both are deferrals with an owning phase, not open questions.
4. **Commit-history conformance begins with the first `m0-bootstrap` commit.** Commits `1851f39`
   through `151d32b` predate the EB-080 convention; the baseline commit's message differs from the
   text T-001 specifies. EB-082 forbids amending them and this ADR is the record instead. The
   format is already fixed by `07_QUALITY_STANDARDS` line 113 and is not a decision made here.

The Spec deviations table lists, per row, the document, the section, the change and the REQ IDs:
SPEC-08 section 2 repoint (REQ-scope-in), SPEC-08 section 2 layout additions (REQ-scope-in,
REQ-dl8-quality), the D-04 standing rule (REQ-scope-in), and the commit-history note
(REQ-milestones). Also record, per D-06, that from this ADR onward every specification edit gets
its own ADR.

Then create `.github/pull_request_template.md`. It carries **only** the `06_CHECKLISTS [STD]`
items that a test cannot decide, because D-24 forbids duplicating anything
`tests/unit/test_repo_layout.py` already checks. Include exactly these judgment checkboxes:
a `docs/BUILD_LOG.md` entry was appended for this milestone with scope, decisions and wall times;
evidence is attached — `make lint && make test` output pasted and the CI run linked; the effort
budget was respected, with actual elapsed effort stated and the strictly-greater-than 2x tripwire
confirmed not tripped, or an ADR filed; every specification edit in this pull request has its own
ADR; the numeric SSOT was regenerated in this pull request if any reported number changed; and the
review was performed against `07_QUALITY_STANDARDS.md` and `09_ANTI_PATTERNS.md` with the reviewer
stating checked explicitly. Add a short header explaining that the layout, ignore-rule,
forbidden-dependency, import-independence and line-ending items are deliberately absent because
they are enforced by tests rather than by ticking a box.
  </action>
  <acceptance_criteria>
    - `docs/ADR/ADR-006_module-contracts-layout-and-citation-policy.md` exists and contains the four TEMPLATE.md section headings
    - The Decision section contains four numbered decisions, and the file names `docs/MODULE_CONTRACTS.md`, `.gitattributes`, `uv.lock`, `.planning/`, `.github/`, Phase 4 and Phase 9
    - The Spec deviations table has at least four rows and every row carries at least one REQ ID
    - `.github/pull_request_template.md` exists with exactly six checkbox items and contains none of the words layout, forbidden-deps or line-ending as a checkbox item
    - `git ls-files .github/pull_request_template.md docs/ADR/ADR-006_module-contracts-layout-and-citation-policy.md` returns both paths
  </acceptance_criteria>
  <verify>
    <automated>ADR=docs/ADR/ADR-006_module-contracts-layout-and-citation-policy.md; [ -f "$ADR" ] && [ "$(grep -cE '^## (Context|Decision|Consequences|Spec deviations)' "$ADR")" = "4" ] && grep -q 'MODULE_CONTRACTS.md' "$ADR" && grep -q '.gitattributes' "$ADR" && grep -q 'uv.lock' "$ADR" && [ "$(grep -c 'REQ-' "$ADR")" -ge 4 ] && [ "$(grep -c '^- \[ \]' .github/pull_request_template.md)" = "6" ] && [ -n "$(git ls-files .github/pull_request_template.md)" ]</automated>
  </verify>
  <done>ADR-006 records all four D-05 decisions in the GB-201 shape with a populated spec-deviations table, and the pull-request template carries the six judgment items with no overlap against the layout test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan produces prose only — no executable code, no input parsing, no network. The one boundary
that matters is what these documents are permitted to disclose.

| Boundary | Description |
|----------|-------------|
| private drop / client identity → published documents | The register, the contracts and the ADR are published artifacts; a real client name, a real drop path or an absolute real euro figure would leak by publication |
| gitignored blueprint → tracked documents | Content promoted from `03_MODULES.md` and `12_RISK_REGISTER.md` becomes public; only content already safe to publish may be promoted |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-12 | Information Disclosure | `docs/RISK_REGISTER.md` R-1 and R-6 rows | medium | mitigate | The Charter's own risk text is already public-safe and is copied verbatim rather than elaborated. No client name, no agency name, no drop path, and no absolute real euro figure enters any cell; R-6's anonymization risk is described by mechanism only |
| T-01-13 | Information Disclosure | promotion from gitignored blueprint files | medium | mitigate | Only the module-contract API text and the risk triads are promoted. The promotion is explicit and enumerated in Tasks 1 and 2 rather than a bulk copy, so nothing incidental crosses the boundary |
| T-01-14 | Repudiation | ADR-006 as the record for un-amendable commits | low | accept | The record substitutes for a fix that EB-082 forbids; this is the intended governance outcome, not a gap |
| T-01-15 | Tampering | `.github/pull_request_template.md` | low | accept | The template is advisory; the enforcing half moved into `tests/unit/test_repo_layout.py` under D-23 precisely because a checkbox is not a control |
</threat_model>

<verification>
- All four documents are tracked (`git ls-files`) and none of the two blueprint edits is
- ADR-006 does not contradict ADR-000: precedence stays scoped and BP-D-01..20 stay accepted
- Every Charter risk ID appears exactly once, and every finding ID is at R-10 or above
- The pull-request template and `tests/unit/test_repo_layout.py` (plan 01-07) have zero overlapping items
</verification>

<success_criteria>
1. SPEC-08 section 2's contract-first rule cites a document a reader can open, closing the D-04 violation this phase created awareness of.
2. REQ-risk-register has a tracked, reviewable artifact with all nine Charter risks and complete mitigation/fallback/detection triads.
3. The four D-05 decisions are ratified in one ADR with a populated spec-deviations table.
4. The judgment half of the standing checklist has a literal home, and the machine-checkable half is deliberately absent from it.
</success_criteria>

## Artifacts this phase produces

Every symbol below is **created by Phase 1** — none exists in the repository today. The
source-grounding pass must treat all of them as new, not as drift.

**Repository root:** `.gitattributes`, `.env.example`, `LICENSE`, `README.md`, `Makefile`,
`pyproject.toml`, `uv.lock`, `.pre-commit-config.yaml`

**`.github/`:** `.github/workflows/ci.yml`, `.github/pull_request_template.md`

**`docs/`:** `docs/BUILD_LOG.md`, `docs/MODULE_CONTRACTS.md`, `docs/RISK_REGISTER.md`,
`docs/ADR/TEMPLATE.md`, `docs/ADR/README.md`,
`docs/ADR/ADR-006_module-contracts-layout-and-citation-policy.md`

**`config/`, `dbt/`:** `config/settings.yaml`, `dbt/seeds/season_windows.csv`

**Python modules:** `src/ambo/__init__.py`, `src/ambo/common/__init__.py`,
`src/ambo/common/config.py`, `src/ambo/common/errors.py`, `src/ambo/common/logging.py`

**Scripts:** `scripts/leak_scan.py`, `scripts/generate_season_windows.py`,
`scripts/check_layer_order.py`, `scripts/check_ssot_consistency.py`

**Tests:** `tests/conftest.py`, `tests/unit/test_config.py`, `tests/unit/test_logging.py`,
`tests/unit/test_leak_scan.py`, `tests/unit/test_season_windows.py`,
`tests/unit/test_forbidden_deps.py`, `tests/unit/test_import_independence.py`,
`tests/unit/test_no_requests.py`, `tests/unit/test_repo_layout.py`,
`tests/unit/test_line_endings.py`, `tests/unit/test_gitignore.py`,
`tests/unit/test_governance_checks.py`

**Python symbols**
- `src/ambo/__init__.py` — `__version__`
- `src/ambo/common/errors.py` — `AmboError`, `ConfigError`
- `src/ambo/common/config.py` — `PathsConfig`, `SamplerConfig`, `ScenarioConfig`, `Settings`, `load_settings()`, `repo_root()`
- `src/ambo/common/logging.py` — `LOG_FORMAT`, `PrivatePathFilter`, `get_logger()`
- `scripts/leak_scan.py` — `Finding`, `PRIVATE_DROP_PATTERNS`, `CONTACT_PATTERNS`, `CURRENCY_LITERAL_PATTERN`, `CONTACT_SCAN_ROOTS`, `scan_tree()`, `scan_staged()`, `main()`
- `scripts/generate_season_windows.py` — `YEAR_RANGE`, `SEED_PATH`, `JAN_DIP_WEEKS`, `SPRING_WEEKS`, `SUMMER_LULL_WEEKS`, `advent_weeks()`, `schulbeginn_weeks()`, `build_rows()`, `main()`
- `scripts/check_layer_order.py` — `LAYER_R_GLOBS`, `first_commit_adding()`, `main()`
- `scripts/check_ssot_consistency.py` — `SSOT_PATH`, `parse_ssot()`, `main()`
- `tests/conftest.py` — fixtures `seeded_rng`, `repo_root`, `tmp_repo`

**Makefile targets (18, SPEC-08 section 5):** `setup`, `simulate`, `validate-sim`, `intake`,
`anonymize`, `validate-intake`, `transform`, `fit-synthetic`, `fit-real`, `recover`,
`sensitivity`, `decide`, `ssot`, `export`, `report`, `test`, `lint`, `all`

**CI job names (6, EB-060 / Guide section 9):** `lint`, `test`, `dbt`, `ssot`, `layer-order`, `leak`

**pytest markers:** `smoke`, `fit`

<output>
Create `.planning/phases/01-repository-foundation/01-03-SUMMARY.md` when done.
</output>
