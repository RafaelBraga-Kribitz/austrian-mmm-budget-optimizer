---
phase: 01-repository-foundation
plan: 07
type: execute
wave: 4
depends_on: ["01-02", "01-03", "01-04", "01-05"]
files_modified:
  - tests/conftest.py
  - tests/unit/test_repo_layout.py
  - tests/unit/test_forbidden_deps.py
  - tests/unit/test_import_independence.py
  - tests/unit/test_no_requests.py
  - tests/unit/test_line_endings.py
  - tests/unit/test_gitignore.py
  - docs/BUILD_LOG.md
autonomous: true
requirements: [REQ-scope-out, REQ-scope-in, REQ-dl8-quality]
security_asvs_level: 1
security_block_on: high

must_haves:
  truths:
    - "tests/conftest.py provides a seeded RNG fixture, a repo_root fixture and a tmp_repo helper, and the marker policy in pyproject.toml runs under --strict-markers so an unregistered marker is an error"
    - "A pytest session that collects zero items fails rather than reporting success, so make test cannot go green on an empty or mis-globbed test tree"
    - "tests/unit/test_repo_layout.py asserts the real tree against the SPEC-08 section 2 canonical layout plus the four D-14 additions, with no allowlist, and permits not-yet-created source files"
    - "tests/unit/test_repo_layout.py fails when any module under src/ambo lacks an entry in docs/MODULE_CONTRACTS.md, and equally when MODULE_CONTRACTS.md carries an entry for a module that does not exist"
    - "Exactly one MODULE_CONTRACTS entry exists per src/ambo module: a duplicate heading for one module and an orphan entry for a deleted module are both failures"
    - "With zero modules present under a src/ambo subpackage the contract cross-check passes and reports the count it scanned, so a vacuous pass is visibly vacuous rather than silent"
    - "The forbidden-dependency guard matches on whole module and distribution names, never on substrings, so a legitimate package whose name contains a forbidden name as a fragment does not trip it and a forbidden package cannot hide inside a longer name"
    - "Each guard reports the number of files or lockfile entries it scanned, so a guard that scanned nothing announces that fact instead of passing silently"
    - "No tracked text file contains a carriage return byte"
    - "The gitignore guard proves the EB-081 rules: a file created under an ignored path is not offered for staging, while a committed-by-design path is"
  artifacts:
    - path: "tests/conftest.py"
      provides: "Seeded RNG fixture, repo_root, tmp_repo, and the empty-collection guard hook"
      exports: ["seeded_rng", "repo_root", "tmp_repo"]
    - path: "tests/unit/test_repo_layout.py"
      provides: "The fourth guard: SPEC-08 section 2 layout plus contract-first enforcement (D-23)"
    - path: "tests/unit/test_forbidden_deps.py"
      provides: "EB-030 / Charter O-3 dependency-level scope wall"
    - path: "tests/unit/test_import_independence.py"
      provides: "SIM-003 / W-2 simulate-versus-model firewall via AST walk"
    - path: "tests/unit/test_no_requests.py"
      provides: "EB-070 no-network guarantee at import level"
    - path: "tests/unit/test_line_endings.py"
      provides: "D-22 proof that the LF pin holds across tracked text files"
    - path: "tests/unit/test_gitignore.py"
      provides: "T-003 validation probe converted from a manual check into a test"
  key_links:
    - from: "tests/unit/test_repo_layout.py"
      to: "docs/MODULE_CONTRACTS.md"
      via: "parses the level-3 module-path headings and compares that set against the modules discovered under src/ambo"
      pattern: "MODULE_CONTRACTS"
    - from: "tests/unit/test_forbidden_deps.py"
      to: "uv.lock"
      via: "scans lockfile package names as well as source imports, so a transitive arrival is caught too"
      pattern: "uv\\.lock"
    - from: "tests/unit/test_import_independence.py"
      to: "src/ambo/simulate and src/ambo/model"
      via: "AST walk over files rather than runtime import, so a lazy import inside a function is still caught"
      pattern: "ast\\."
  prohibitions:
    - requirement_id: REQ-scope-out
      category: values
      status: resolved
      verification: judgment
      statement: "MUST NOT introduce a third marketing-mix implementation or a model-averaging layer under any flag, optional extra, notebook, or dependency group. Charter O-3 fixes exactly two implementations; a framework added for a one-off comparison is still a violation, and the guard is not to be narrowed to accommodate one."
    - requirement_id: REQ-scope-in
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT back-fill a module contract after the fact to turn a red layout guard green. The contract entry is written in the same pull request as the module it describes, because its purpose is to force the interface to be decided before the code, not to be a receipt written afterwards."
---

<objective>
Build the test scaffold and the four standing architectural guards, so the rules this project
argues for are scripts a reviewer runs rather than sentences in a checklist.

Purpose: GB section 8 states that the governance showpieces are runnable artifacts. D-23 extends
the WBS's three guards to four by turning the `06_CHECKLISTS [STD]` layout item into a test with no
allowlist — possible only because D-14 completed the SPEC-08 section 2 listing. Two further
manual probes, the line-ending pin and the ignore-rule check, become tests here for the same reason.

Output: `tests/conftest.py` and six test modules, plus the planted-violation evidence the WBS
requires.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-repository-foundation/01-CONTEXT.md
@docs/SPEC-08_engineering.md
@docs/MODULE_CONTRACTS.md
@pyproject.toml
@.gitignore
</context>

<assumption_delta_decision>
**no-change.** The assumption-delta detector fired on stale ROADMAP text claiming the repository is
on `master` with zero commits; CONTEXT.md correction #3 records that the repository is on `main`
with baseline commit `1851f39`. False positive on dead text.
</assumption_delta_decision>

<tasks>

<task type="auto">
  <name>Task 1: Build the test scaffold and the repository-layout guard</name>
  <files>tests/conftest.py, tests/unit/test_repo_layout.py</files>
  <read_first>
    - `docs/SPEC-08_engineering.md` section 2 — the canonical layout the guard asserts against, and section 7 (EB-070..073) for the testing policy
    - `docs/ADR/ADR-006_module-contracts-layout-and-citation-policy.md` — the four D-14 layout additions the guard must accept
    - `docs/MODULE_CONTRACTS.md` — the heading format the guard parses and the three entries it currently finds
    - `docs/EXECUTION_BLUEPRINT/02_WBS.md` lines 206-222 (T-010) — the scaffold contents, marker policy and acceptance criteria
    - `docs/EXECUTION_BLUEPRINT/06_CHECKLISTS.md` — the `[STD]` layout item this test replaces
    - `.planning/phases/01-repository-foundation/01-CONTEXT.md` sections D-14, D-23 — the no-allowlist instruction and what makes it possible
    - `pyproject.toml` — the registered markers and the pytest options
  </read_first>
  <action>
Create `tests/conftest.py` with three fixtures and one session hook.

`repo_root` is a session-scoped fixture returning the repository root discovered the same way
`ambo.common.config.repo_root()` discovers it, so tests and runtime agree on one definition.
`seeded_rng` returns a NumPy `Generator` seeded from a module-level constant, never from the global
RNG state — determinism per the module-contract preamble. `tmp_repo` is a function-scoped fixture
that initializes a throwaway git repository inside `tmp_path`, configures a local user name and
email so commits succeed in CI, and yields the path; the layer-order tests in plan 01-09 reuse it.

Add a `pytest_sessionfinish` hook that converts pytest's no-tests-collected exit status into a
failure. A suite that collects zero items must not report success — a mis-globbed `testpaths`, a
renamed directory or an empty tree would otherwise turn `make test` green on nothing, which is the
same silent-pass failure mode D-16 forbids in CI. Emit an explicit message naming the collected
count when the hook fires.

Create `tests/unit/test_repo_layout.py` — the fourth guard.

Build the canonical set from SPEC-08 section 2 plus the four D-14 additions `.github/`,
`.planning/`, `uv.lock` and `.gitattributes`. Express it as two literal frozen sets in the test
module: permitted top-level entries, and permitted `src/ambo/` subpackages. Add a comment stating
that this is deliberately not an allowlist of exceptions — it is the canonical layout itself, and a
new entry belongs in SPEC-08 section 2 via an ADR before it belongs here.

Assert three things. First, every tracked top-level entry is in the permitted set; enumerate the
tree with `git ls-files` so untracked scratch files and gitignored artifacts are out of scope by
construction. Second, every tracked directory under `src/ambo/` is in the permitted subpackage set.
Third, the contract-first cross-check: parse the level-3 headings out of `docs/MODULE_CONTRACTS.md`
into a set of module paths, discover every non-`__init__` Python module under `src/ambo/`, and
assert the two sets are equal. Equality catches both directions — a module with no entry, and an
entry for a module that does not exist — and it also catches a duplicate heading, because a
duplicate would make the parsed list longer than its set; assert that length equality separately so
the failure message says duplicate rather than mismatch.

Handle the empty case explicitly rather than by accident: when a subpackage contains no modules,
the cross-check is vacuously satisfied, and the test must report the number of modules it
discovered so a reader can see the scan was real. Do the same for the top-level enumeration.

Permit not-yet-created source files, per T-003 AC-1: the guard asserts that nothing exists outside
the canonical layout, never that everything in the layout exists. `LIMITATIONS.md` is a Phase 9
deliverable and `.pre-commit-config.yaml` arrives in plan 01-08; neither absence may fail this test.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_repo_layout.py -x` passes
    - Adding a scratch top-level directory, tracking it, and re-running makes the test fail; removing it makes it pass again
    - Adding a module under `src/ambo/common/` without a `docs/MODULE_CONTRACTS.md` entry makes the test fail
    - Duplicating a MODULE_CONTRACTS heading makes the test fail with a message naming duplication
    - The absence of `LIMITATIONS.md` and `.pre-commit-config.yaml` does not fail the test
    - `uv run pytest tests --collect-only -q` reports a nonzero item count, and a run against an empty directory exits non-zero
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit/test_repo_layout.py -x -q && uv run pytest tests -q --collect-only >/dev/null && [ ! -f LIMITATIONS.md ] && uv run pytest tests/unit/test_repo_layout.py -q</automated>
  </verify>
  <done>The scaffold exists with deterministic fixtures and an empty-collection guard, and the layout test enforces the canonical tree and the contract-first rule in both directions with no allowlist.</done>
</task>

<task type="auto">
  <name>Task 2: Write the three original architectural guards</name>
  <files>tests/unit/test_forbidden_deps.py, tests/unit/test_import_independence.py, tests/unit/test_no_requests.py</files>
  <read_first>
    - `docs/SPEC-08_engineering.md` section 3 (EB-030) and section 7 (EB-070) — the forbidden-dependency and no-network rules
    - `docs/MODULE_CONTRACTS.md` Dependency directions section — the allowed-import table and the four forbidden edges
    - `PROJECT_CHARTER.md` section 2.2 O-3 — the scope wall the dependency guard enforces
    - `docs/EXECUTION_BLUEPRINT/02_WBS.md` lines 206-222 (T-010) — the AST-walk instruction and the marker policy
    - `uv.lock` — the lockfile the dependency guard scans
    - `tests/conftest.py` — the fixtures available (created in Task 1)
  </read_first>
  <action>
Create `tests/unit/test_forbidden_deps.py`. Hold the forbidden names as a module-level frozen set
covering the Charter O-3 frameworks and their distribution spellings, including the case where the
import name and the distribution name differ. Assert twice over: an AST walk of every `.py` file
under `src/ambo/`, `scripts/` and `tests/` finds no import of a forbidden module, and a parse of
`uv.lock` finds no package whose name is a forbidden distribution.

Match on whole names, never on substrings. For imports, compare the first dotted segment of the
module path for equality against the forbidden set rather than testing whether the source text
contains the name — otherwise a comment, a docstring, or a legitimate package whose name embeds a
forbidden one as a fragment would trip the guard, and equally a `import x as sklearn` alias would
slip past a naive text search. For the lockfile, parse the package name field and compare it for
equality after normalizing case and separators the way PyPI does.

Report the count of files walked and the count of lockfile entries parsed via an assertion that
both counts are greater than zero, so a guard that silently scanned nothing fails instead of
passing.

Create `tests/unit/test_import_independence.py` implementing the W-2 firewall. AST-walk every `.py`
file under `src/ambo/simulate/` and `src/ambo/model/` and assert that no file in either package
imports from the other, in either direction, including relative imports and imports written inside
a function body. Use the AST rather than a runtime import so a lazy import is caught. Both packages
are empty this phase, so the walk finds zero files; assert the discovered file count explicitly and
state in the test's message that a zero count means the firewall is vacuously intact, not that the
check was skipped. This is the D-17 shape applied to a test.

Also encode the two remaining forbidden edges from the module-contract dependency table as
assertions in this module, since they are the same kind of fact: `pymc_marketing` is imported
nowhere outside `validate/crosscheck.py`, and the PyMC sampling entry point is called nowhere
outside `model/fit.py`. Both are vacuously true today and become load-bearing in Phase 4.

Create `tests/unit/test_no_requests.py` for EB-070. AST-walk `src/ambo/`, `scripts/` and `tests/`
and assert no module imports the HTTP client library named in EB-070, nor any of the other common
network clients a future contributor might reach for instead. Note in a comment that intake reads
local files only, which is why the rule has no exemption. Assert the scanned file count is greater
than zero.

Register nothing new in `pyproject.toml`: the `smoke` and `fit` markers are already declared there
and `--strict-markers` is already active, so an unregistered marker is an error rather than a
warning. Confirm that by asserting a deliberately unregistered marker is rejected, in a test that
uses `pytest.main` against a temporary file rather than by adding one to the suite.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_forbidden_deps.py tests/unit/test_import_independence.py tests/unit/test_no_requests.py -x` passes
    - Each of the three modules asserts a scanned-count greater than zero, or, where the count is legitimately zero, asserts the count explicitly and says so in the failure message
    - A planted forbidden import in a scratch file under `src/ambo/` turns `test_forbidden_deps.py` red
    - A planted cross-import between `simulate` and `model` turns `test_import_independence.py` red
    - Name matching is whole-name: a file importing a package whose name merely contains a forbidden name as a fragment does not trip the guard
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit/test_forbidden_deps.py tests/unit/test_import_independence.py tests/unit/test_no_requests.py -x -q && uv run python -c "import ast,pathlib,sys; mods=[p for p in pathlib.Path('tests/unit').glob('test_*.py')]; assert any('ast' in p.read_text() for p in mods), 'guards must use AST, not text search'; print('ok')"</automated>
  </verify>
  <done>All three original guards pass, each reports what it scanned, matching is whole-name rather than substring, and planted violations turn the relevant guard red.</done>
</task>

<task type="auto">
  <name>Task 3: Convert the line-ending and ignore-rule probes into tests and record the planted-violation proof</name>
  <files>tests/unit/test_line_endings.py, tests/unit/test_gitignore.py, docs/BUILD_LOG.md</files>
  <read_first>
    - `.gitattributes` — the pin the line-ending test verifies (created in plan 01-01)
    - `.gitignore` — the EB-081 rules and the committed-by-design list the ignore test verifies
    - `docs/SPEC-08_engineering.md` section 8 (EB-081) — the ignore list and the committed-by-design exceptions
    - `docs/EXECUTION_BLUEPRINT/02_WBS.md` lines 80-85 (T-003 validation and acceptance criteria) and lines 218-221 (T-010 AC-1, the planted-violation proof)
    - `docs/BUILD_LOG.md` — the append-only file the evidence entry goes into
    - `.planning/phases/01-repository-foundation/01-CONTEXT.md` section D-22 — the CRLF assertion this test satisfies
  </read_first>
  <action>
Create `tests/unit/test_line_endings.py`. Enumerate tracked files with `git ls-files`, filter to
those git treats as text by consulting `git check-attr text`, read each as bytes, and assert none
contains a carriage return byte. Skip files git treats as binary rather than hardcoding an
extension list, so the test tracks `.gitattributes` rather than duplicating it. Assert the number
of files checked is greater than zero.

Create `tests/unit/test_gitignore.py`, converting the WBS's manual probe. Using the `tmp_repo`
fixture is not right here — the rules under test are this repository's, so operate on the real
working tree but only through non-destructive means. Create throwaway files at
`data/warehouse/probe.duckdb`, `data/cache/probe.bin` and `exports/probe.csv` inside a
try-and-clean-up block, then assert via `git check-ignore` that the first two are ignored and the
third is not, since `exports/*.csv` is committed by design per EB-081. Assert the same for the
other EB-081 entries by path rather than by creating a file for each: a netCDF path, a
`__pycache__` path, a `dbt/target/` path and `.env`. Assert that the three committed-by-design data
directories are not ignored. Clean up every created file in a finally block and assert
`git status --porcelain` is unchanged afterwards.

Then produce the planted-violation evidence T-010 AC-1 requires, on a scratch branch that is
created, used and deleted — never merged, so the append-only history on `m0-bootstrap` is
untouched. On that branch: add a file under `src/ambo/simulate/` importing from `src/ambo/model/`
and confirm `test_import_independence.py` goes red; add a file importing a Charter O-3 framework
and confirm `test_forbidden_deps.py` goes red; add a top-level directory and confirm
`test_repo_layout.py` goes red; add a file with carriage-return line endings, staged with the
attribute overridden, and confirm `test_line_endings.py` goes red. Record each red run's test node
id and message. Delete the scratch branch when done and confirm the current branch is
`m0-bootstrap` with a clean tree.

Append one entry to `docs/BUILD_LOG.md` under the M0 section recording the four planted-violation
proofs, each naming the guard, what was planted, the failing test node id, and the fact that the
scratch branch was deleted rather than merged. Do not edit any existing entry.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_line_endings.py tests/unit/test_gitignore.py -x` passes
    - The line-ending test consults `git check-attr` rather than a hardcoded extension list and asserts a nonzero checked count
    - The gitignore test leaves `git status --porcelain` byte-identical to its pre-run state
    - `docs/BUILD_LOG.md` gains one entry naming all four guards, each with a planted violation and a failing node id, and no existing entry is modified
    - `git branch --list` shows no scratch branch, and `git rev-parse --abbrev-ref HEAD` returns `m0-bootstrap`
    - `uv run pytest tests -q` is green with the full unit suite
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit/test_line_endings.py tests/unit/test_gitignore.py -x -q && [ -z "$(git status --porcelain)" ] && [ "$(git rev-parse --abbrev-ref HEAD)" = "m0-bootstrap" ] && [ "$(git branch --list | wc -l)" -le 2 ] && grep -qi 'planted' docs/BUILD_LOG.md && [ "$(git diff HEAD~1 --numstat -- docs/BUILD_LOG.md | cut -f2)" = "0" ] && uv run pytest tests -q</automated>
  </verify>
  <done>Line endings and ignore rules are tests rather than manual probes, all four guards are proven to fail on a planted violation, and the evidence is in the append-only build log with the scratch branch deleted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository tree → guard assertions | The guards read the tree, the lockfile and the contract document; a guard that silently scans nothing is the failure mode, not injection |
| scratch branch → milestone branch | The planted-violation proof deliberately creates violating content; none of it may reach `m0-bootstrap` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-03 | Tampering | Charter O-3 scope wall | high | mitigate | `tests/unit/test_forbidden_deps.py` asserts against both source imports and `uv.lock` package names with whole-name matching, so a forbidden framework cannot arrive as a direct import, an aliased import, or a transitive dependency |
| T-01-27 | Tampering | a guard that scans zero files and passes | high | mitigate | Every guard asserts its scanned count. Where the count is legitimately zero — the empty `simulate` and `model` packages — the test asserts the zero explicitly and states in its message that the pass is vacuous. This is D-17's shape applied to tests |
| T-01-28 | Tampering | planted-violation proof leaking onto the milestone branch | high | mitigate | The proof runs on a scratch branch that is deleted, never merged; the acceptance criteria assert the branch list is clean and `HEAD` is back on `m0-bootstrap` with a clean tree, preserving EB-082 |
| T-01-29 | Tampering | contract-first rule bypassed by a back-filled entry | medium | mitigate | The layout guard makes a missing entry a red test at the moment the module lands; the prohibition above records that back-filling to turn it green defeats the rule's purpose |
| T-01-30 | Information Disclosure | test fixtures | medium | mitigate | Every fixture in this plan is synthetic — throwaway probe filenames and a temporary git repository. EB-071 and AG-032 forbid real excerpts, and plan 01-08 carries the standing prohibition for leak-scan fixtures |
</threat_model>

<verification>
- `uv run pytest tests -q` green across the whole unit suite
- Each of the four guards demonstrably red against a planted violation, evidenced in the build log
- `git status --porcelain` clean and `HEAD` on `m0-bootstrap` with no scratch branch remaining
- A zero-item pytest session exits non-zero
</verification>

<success_criteria>
1. The `06_CHECKLISTS [STD]` layout item is machine-enforced with no allowlist, and the contract-first rule is enforced in both directions.
2. Charter O-3 is a dependency-level wall that catches direct, aliased and transitive arrivals.
3. No guard can pass by scanning nothing, and no test session can pass by collecting nothing.
4. The line-ending pin and the ignore rules are tests, and every guard is proven to fail on a planted violation without that violation touching the milestone branch.
</success_criteria>

<!-- planner-discipline-allow: robyn -->
<!-- planner-discipline-allow: lightweight_mmm -->
<!-- planner-discipline-allow: prophet -->
<!-- planner-discipline-allow: sklearn -->
<!-- planner-discipline-allow: requests -->
<!-- planner-discipline-allow: pymc_marketing -->

## Artifacts this phase produces

Every symbol below is **created by Phase 1** — none exists in the repository today. The
source-grounding pass must treat all of them as new, not as drift.

**Repository root:** `.gitattributes`, `.env.example`, `LICENSE`, `README.md`, `Makefile`,
`pyproject.toml`, `uv.lock`, `.pre-commit-config.yaml`

**`.github/`:** `.github/workflows/ci.yml`, `.github/pull_request_template.md`

**`docs/`:** `docs/BUILD_LOG.md`, `docs/MODULE_CONTRACTS.md`, `docs/RISK_REGISTER.md`,
`docs/ADR/TEMPLATE.md`, `docs/ADR/README.md`,
`docs/ADR/ADR-006_module-contracts-layout-and-citation-policy.md`

**`config/`, `dbt/`:** `config/settings.yaml`, `dbt/seeds/season_windows.csv`

**Python modules:** `src/ambo/__init__.py`, `src/ambo/common/__init__.py`,
`src/ambo/common/config.py`, `src/ambo/common/errors.py`, `src/ambo/common/logging.py`

**Scripts:** `scripts/leak_scan.py`, `scripts/generate_season_windows.py`,
`scripts/check_layer_order.py`, `scripts/check_ssot_consistency.py`

**Tests:** `tests/conftest.py`, `tests/unit/test_config.py`, `tests/unit/test_logging.py`,
`tests/unit/test_leak_scan.py`, `tests/unit/test_season_windows.py`,
`tests/unit/test_forbidden_deps.py`, `tests/unit/test_import_independence.py`,
`tests/unit/test_no_requests.py`, `tests/unit/test_repo_layout.py`,
`tests/unit/test_line_endings.py`, `tests/unit/test_gitignore.py`,
`tests/unit/test_governance_checks.py`

**Python symbols**
- `src/ambo/__init__.py` — `__version__`
- `src/ambo/common/errors.py` — `AmboError`, `ConfigError`
- `src/ambo/common/config.py` — `PathsConfig`, `SamplerConfig`, `ScenarioConfig`, `Settings`, `load_settings()`, `repo_root()`
- `src/ambo/common/logging.py` — `LOG_FORMAT`, `PrivatePathFilter`, `get_logger()`
- `scripts/leak_scan.py` — `Finding`, `PRIVATE_DROP_PATTERNS`, `CONTACT_PATTERNS`, `CURRENCY_LITERAL_PATTERN`, `CONTACT_SCAN_ROOTS`, `scan_tree()`, `scan_staged()`, `main()`
- `scripts/generate_season_windows.py` — `YEAR_RANGE`, `SEED_PATH`, `JAN_DIP_WEEKS`, `SPRING_WEEKS`, `SUMMER_LULL_WEEKS`, `advent_weeks()`, `schulbeginn_weeks()`, `build_rows()`, `main()`
- `scripts/check_layer_order.py` — `LAYER_R_GLOBS`, `first_commit_adding()`, `main()`
- `scripts/check_ssot_consistency.py` — `SSOT_PATH`, `parse_ssot()`, `main()`
- `tests/conftest.py` — fixtures `seeded_rng`, `repo_root`, `tmp_repo`

**Makefile targets (18, SPEC-08 section 5):** `setup`, `simulate`, `validate-sim`, `intake`,
`anonymize`, `validate-intake`, `transform`, `fit-synthetic`, `fit-real`, `recover`,
`sensitivity`, `decide`, `ssot`, `export`, `report`, `test`, `lint`, `all`

**CI job names (6, EB-060 / Guide section 9):** `lint`, `test`, `dbt`, `ssot`, `layer-order`, `leak`

**pytest markers:** `smoke`, `fit`

<output>
Create `.planning/phases/01-repository-foundation/01-07-SUMMARY.md` when done.
</output>
