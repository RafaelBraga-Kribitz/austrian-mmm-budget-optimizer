---
phase: 01-repository-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - pyproject.toml
  - uv.lock
  - src/ambo/__init__.py
  - .env.example
  - LICENSE
  - .gitignore
  - docs/BUILD_LOG.md
autonomous: false
requirements: [REQ-dl8-quality, REQ-scope-out]
security_asvs_level: 1
security_block_on: high

must_haves:
  truths:
    - "uv sync from a clean checkout succeeds and uv run python -c \"import ambo\" prints nothing and exits 0"
    - "uv.lock is committed and was produced by uv lock inside this repository, never hand-authored or version-transcribed"
    - "No forbidden modeling framework named by Charter O-3 appears anywhere in uv.lock, including as a transitive dependency"
    - "mypy runs clean on the empty ambo package under the strict scope configured in pyproject.toml"
    - "pyproject.toml is the only tool-configuration home: ruff, mypy and pytest are configured there and no ruff.toml, mypy.ini, setup.cfg or pytest.ini exists"
    - "The SPEC-08 section 2 directory skeleton exists with .gitkeep in every otherwise-empty directory, and the gitignored-by-design directories carry a .gitkeep while their contents stay ignored"
    - ".env.example documents exactly one variable, AMBO_PRIVATE_DROP, and contains no real path from the developer machine"
    - "LICENSE is MIT and its copyright line names Rafael Braga-Kribitz, matching the PROJECT_CHARTER.md author line"
    - "The coverage gate has an exact boundary: --cov-fail-under=80 fails a run reporting below 80 and passes a run reporting exactly 80; in Phase 1 the flag is present but configured non-blocking per D-15, and it flips to blocking at M0 exit"
    - statement: "Coverage percentage rounding is pytest-cov's own contract: the gate compares the reported total, rounded to the configured precision, against the 80 threshold — a run reporting exactly 80 passes and 79 fails. This is inert while D-15 keeps the gate non-blocking and becomes observable only at M0 exit."
      verification: backstop
  artifacts:
    - path: "pyproject.toml"
      provides: "Package metadata, SPEC-08 section 3 pinned bounds, ruff/mypy/pytest configuration"
      contains: "[tool.pytest.ini_options]"
    - path: "uv.lock"
      provides: "Cross-platform universal lockfile (T-002 AC-1/AC-2)"
    - path: "src/ambo/__init__.py"
      provides: "Importable ambo package with version string"
      contains: "__version__"
    - path: ".env.example"
      provides: "Documents AMBO_PRIVATE_DROP only (EB-041)"
      contains: "AMBO_PRIVATE_DROP"
    - path: "LICENSE"
      provides: "MIT license with the Charter author line (T-003 AC-3)"
      contains: "MIT"
  key_links:
    - from: "pyproject.toml"
      to: "docs/SPEC-08_engineering.md section 3"
      via: "every runtime and dev dependency bound is copied from the spec table, not chosen"
      pattern: "pymc>=5\\.15,<6"
    - from: "uv.lock"
      to: "pyproject.toml"
      via: "generated by uv lock against the declared bounds; regenerating must not change the bounds"
      pattern: "requires-dist"
  prohibitions:
    - requirement_id: REQ-dl8-quality
      category: values
      status: resolved
      verification: judgment
      statement: "MUST NOT reach a green gate by weakening the gate. The coverage threshold, the ruff rule set and the mypy strict scope move only by ADR — never by lowering a number, adding a blanket suppression, deleting a failing assertion, or marking a guard test expected-to-fail so a run turns green."
---

<objective>
Stand up the Python project: `pyproject.toml` with the SPEC-08 section 3 bounds verbatim, a
committed cross-platform `uv.lock`, the importable `ambo` package, and the rest of the SPEC-08
section 2 directory skeleton that T-003 left unbuilt.

Purpose: every later task in this phase imports this environment. EB-030 makes the pinned bounds
an ADR-level contract from the moment they land, so they are copied from the spec rather than
chosen. Research already resolved T-002 AC-1 empirically — 112 packages resolve into one universal
lockfile with zero fork markers at exactly these bounds — so this plan pins, it does not
investigate.

Output: `pyproject.toml`, `uv.lock`, `src/ambo/__init__.py`, the `.gitkeep` skeleton,
`.env.example`, `LICENSE`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-repository-foundation/01-CONTEXT.md
@.planning/phases/01-repository-foundation/01-RESEARCH.md
@docs/SPEC-08_engineering.md
@docs/EXECUTION_BLUEPRINT/02_WBS.md
</context>

<assumption_delta_decision>
**no-change.** The assumption-delta detector fired on stale ROADMAP text claiming the repository is
on `master` with zero commits; CONTEXT.md correction #3 records that the repository is on `main`
with baseline commit `1851f39`. False positive on dead text.
</assumption_delta_decision>

<tasks>

<task type="auto">
  <name>Task 1: Author pyproject.toml with SPEC-08 bounds and generate the lockfile</name>
  <files>pyproject.toml, src/ambo/__init__.py, uv.lock</files>
  <read_first>
    - `docs/SPEC-08_engineering.md` section 1 (EB-001), section 3 (the dependency table with exact bounds), section 7 (EB-070..073 testing policy) — the values copied verbatim
    - `docs/EXECUTION_BLUEPRINT/02_WBS.md` lines 43-63 (T-002) — objective, implementation notes and the four acceptance criteria
    - `.planning/phases/01-repository-foundation/01-RESEARCH.md` Pattern 3 and Pitfall 3 — why the lock is generated, never transcribed
    - `.planning/phases/01-repository-foundation/01-CONTEXT.md` section D-15 — the coverage-gate sequencing
    - `.gitignore` — confirm `.venv/` is already ignored before creating one
  </read_first>
  <action>
Create `pyproject.toml`. Declare `[project]` with `name = "ambo"`, `version = "0.1.0"`,
`requires-python = ">=3.12,<3.13"`, and `dependencies` listing every runtime package from the
SPEC-08 section 3 table with its stated bound and nothing else: pandas, numpy, pymc, arviz,
pymc-marketing, scipy, duckdb, dbt-core, dbt-duckdb, holidays, matplotlib, pydantic, PyYAML,
python-dotenv. Do not pin pytensor — SPEC-08 says pymc controls it. Declare the six dev packages
(pytest, pytest-cov, ruff, mypy, pre-commit, nbstripout) under
`[dependency-groups] dev`. Use the src layout via `[tool.hatchling]` or the uv default build
backend with `packages = ["src/ambo"]`.

Configure all three tools inside this one file — EB-040's one-config-home rule applies to tooling
too, and the WBS says no scattered ini files. `[tool.ruff]` sets `line-length = 100` and enables
lint plus format. `[tool.mypy]` sets `strict = true` with `files = ["src/ambo"]` and
`ignore_missing_imports = true` scoped by `[[tool.mypy.overrides]]` to the modules named in the
WBS: pymc, arviz, pytensor, pymc_marketing, duckdb, holidays. `[tool.pytest.ini_options]` sets
`addopts = "-q --strict-markers --cov=src/ambo --cov-report=term-missing"`, `testpaths = ["tests"]`,
and registers the two markers `smoke` and `fit` with descriptions.

Per D-15 the coverage threshold lands **present but non-blocking** in this phase. Express that as
`[tool.coverage.report] fail_under = 80` together with `[tool.coverage.report] exclude_also` left
default, and add the `--cov-fail-under=80` flag **commented out** in `addopts` with an adjacent
comment naming D-15 and stating the exact commit-time condition under which it is uncommented: M0
exit, after `common/config.py` and `common/logging.py` have their test suites. Do not invent a
different mechanism — the flag is either in `addopts` or commented there, so flipping it is a
one-line diff a reviewer can see.

Create `src/ambo/__init__.py` containing only `__version__ = "0.1.0"` and a module docstring
carrying the `Implements: EB-001` convention from `07_QUALITY_STANDARDS` line 103.

Then run `uv venv` and `uv lock` in the repository. Do not create the lockfile any other way and do
not copy any version number out of RESEARCH.md — those are a dated snapshot. Leave `uv.lock`
uncommitted at the end of this task; Task 2 gates its first commit.
  </action>
  <acceptance_criteria>
    - `pyproject.toml` exists and `grep -v '^#' pyproject.toml | grep -c 'pymc>=5.15,<6'` returns 1
    - `uv.lock` exists on disk and was produced by `uv lock` in this repository (its header names the uv version)
    - No `ruff.toml`, `mypy.ini`, `setup.cfg`, `pytest.ini` or `tox.ini` exists at the repository root
    - `uv run python -c "import ambo; print(ambo.__version__)"` prints `0.1.0`
    - `grep -v '^#' pyproject.toml | grep -c 'cov-fail-under'` returns 0, and the commented form plus its D-15 comment are present
  </acceptance_criteria>
  <verify>
    <automated>[ -f pyproject.toml ] && [ -f uv.lock ] && [ "$(grep -v '^#' pyproject.toml | grep -c 'pymc>=5.15,<6')" = "1" ] && [ ! -f ruff.toml ] && [ ! -f mypy.ini ] && [ ! -f setup.cfg ] && [ ! -f pytest.ini ] && [ "$(uv run python -c 'import ambo; print(ambo.__version__)')" = "0.1.0" ] && [ "$(grep -v '^#' pyproject.toml | grep -c 'cov-fail-under')" = "0" ] && grep -q 'D-15' pyproject.toml</automated>
  </verify>
  <done>`pyproject.toml` carries the SPEC-08 section 3 bounds and all three tool configurations, `uv.lock` was generated in-repo, the `ambo` package imports, and the coverage gate is present but non-blocking with its flip condition documented.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Package legitimacy review before the lockfile is committed</name>
  <files>uv.lock</files>
  <read_first>
    - `.planning/phases/01-repository-foundation/01-RESEARCH.md` section `## Package Legitimacy Audit` — the 20-package table, the heuristic-artifact analysis and the one-combined-checkpoint instruction
    - `docs/SPEC-08_engineering.md` section 3 — the dependency table the resolved set is compared against
    - `uv.lock` — the artifact under review (generated in Task 1, not yet committed)
  </read_first>
  <action>
Present the resolved dependency set for human review **before** `uv.lock` is staged or committed,
because that commit is the EB-030 point of no return after which the pins move only by ADR. Run the
five checks listed in the verification steps below and show their output alongside the SPEC-08
section 3 table, then stop and wait. Do not stage `uv.lock`, do not proceed to Task 3, and do not
treat `workflow.auto_advance` as approval — a legitimacy checkpoint is never auto-approvable.
  </action>
  <what-built>
`uv lock` has resolved the SPEC-08 section 3 dependency set into `uv.lock`. The lockfile is on disk
but **not yet committed** — committing it is the point of no return for EB-030, after which the
pins move only by ADR.

RESEARCH.md's Package Legitimacy Audit returned `SUS` for all 20 SPEC-08 section 3 packages and
identified that verdict as a heuristic artifact of this environment: the two triggering reasons
were `unknown-downloads` (no download-stats source is configured, not that downloads are low) and
`too-new` (the checker keys off the latest release date, which is recent for every actively
maintained package in the list). The researcher corroborated legitimacy via canonical repository
URLs — `pymc-devs/pymc`, `dbt-labs/dbt-core`, `pydantic/pydantic`, `pytest-dev/pytest`,
`pre-commit/pre-commit`, `duckdb/duckdb-python`, `jwills/dbt-duckdb`, `theskumar/python-dotenv` —
and via the successful resolution against `pypi.org/simple` with real `files.pythonhosted.org`
artifact URLs. No package was verdicted `SLOP`; none was removed.

This is one combined check rather than twenty, because the dependency table is a single locked
authoritative spec artifact and twenty separate gates would be disproportionate to a 0.5 d budget.
  </what-built>
  <how-to-verify>
1. Run `uv tree --depth 1` and compare the direct dependencies against the SPEC-08 section 3 table
   in `docs/SPEC-08_engineering.md`. The direct list should be exactly the 14 runtime packages plus
   the 6 dev packages — nothing extra at depth 1.
2. Run `grep -c 'name = ' uv.lock` and note the total package count. Research measured 112 at these
   bounds; a total far outside roughly 100-130 is worth a second look before committing.
3. Spot-check any package name you do not recognize at `https://pypi.org/project/<name>` and
   confirm it is a transitive dependency of something you do recognize with `uv tree --invert`.
4. Confirm no additional MMM framework was pulled in: run
   `uv tree | grep -Ei 'robyn|lightweight|meridian|prophet|scikit'` and confirm it returns nothing.
5. Confirm the resolution is universal rather than platform-forked: `grep -c 'resolution-markers' uv.lock`
   should return 0.
  </how-to-verify>
  <resume-signal>Type "approved" to commit `uv.lock`, or describe what looked wrong.</resume-signal>
  <verify>
    <automated>[ -f uv.lock ] && [ -z "$(git ls-files uv.lock)" ] && [ "$(grep -c 'resolution-markers' uv.lock)" = "0" ]</automated>
  </verify>
  <done>The resolved dependency set has been shown to a human against SPEC-08 section 3 and approved, with `uv.lock` still uncommitted at the moment of review.</done>
</task>

<task type="auto">
  <name>Task 3: Commit the lockfile and build the SPEC-08 directory skeleton</name>
  <files>uv.lock, .env.example, LICENSE, .gitignore, docs/BUILD_LOG.md, src/ambo/*/__init__.py, .gitkeep across the SPEC-08 section 2 tree</files>
  <read_first>
    - `docs/SPEC-08_engineering.md` section 2 — the canonical tree that the skeleton must match, and section 4 (EB-040/EB-041) for the `.env.example` contract
    - `docs/EXECUTION_BLUEPRINT/02_WBS.md` lines 65-85 (T-003) — outputs, implementation notes and the three acceptance criteria
    - `PROJECT_CHARTER.md` lines 1-12 — the author line the LICENSE must match
    - `.gitignore` — which of these directories are ignored, so the `.gitkeep` placement is correct
    - `docs/BUILD_LOG.md` — the audit rows this task closes
  </read_first>
  <action>
Commit `uv.lock` first, on its own, with a commit message naming EB-030 so the pin event is
findable in history.

Then create the SPEC-08 section 2 directory skeleton, adding an empty `.gitkeep` to every directory
that would otherwise be empty. Create: `config/`, `config/scenarios/`, `src/ambo/simulate/`,
`src/ambo/intake/`, `src/ambo/model/`, `src/ambo/validate/`, `src/ambo/decide/`,
`src/ambo/report/`, `src/ambo/common/`, `dbt/`, `dbt/models/`, `dbt/seeds/`, `scripts/`,
`data/synthetic/`, `data/real_anon/`, `data/posteriors/`, `data/warehouse/`, `data/cache/`,
`exports/`, `reports/`, `reports/ingestion/`, `dashboards/`, `docs/assets/`, `tests/`,
`tests/unit/`, `tests/fixtures/`, `tests/golden/`.

Three directories are ignored by `.gitignore` but still need to exist for path-globbing scripts:
`data/warehouse/`, `data/cache/` and `dbt/target/`. For those, add the `.gitkeep` and a matching
negation so the placeholder is tracked while the contents stay ignored — append
`!data/warehouse/.gitkeep`, `!data/cache/.gitkeep` and `!dbt/target/.gitkeep` lines to `.gitignore`
under a comment explaining that only the placeholder is exempt. Do not un-ignore the directories
themselves and do not remove any existing `.gitignore` line, including `docs/EXECUTION_BLUEPRINT/`
which is D-01 and is correct.

Add `__init__.py` (empty, with a one-line docstring) to each `src/ambo/` subpackage in place of a
`.gitkeep`, since they are Python packages rather than plain directories.

Create `.env.example` documenting exactly one variable, `AMBO_PRIVATE_DROP`. It carries the
variable name, a clearly-fictional example Windows path that is not the developer's real drop
location, and a comment stating that the path itself is not the secret but its contents are
(EB-041), that nothing under it is ever copied into the repository or written to a log, and that
`intake/standardize.py` is the only module permitted to read from it. No other variable appears in
this file.

Create `LICENSE` as the MIT license with the copyright line
`Copyright (c) 2026 Rafael Braga-Kribitz` — the author name is copied from the `**Author:**` line
in `PROJECT_CHARTER.md`, without the city, which is not part of a copyright line.

Finally, re-run the T-003 AC-2 ignore probe as proof:
`touch data/warehouse/x.duckdb exports/foo.csv && git status --porcelain` must list `exports/foo.csv`
as untracked (exports are committed by design per EB-081) and must **not** list the warehouse file.
Delete both probe files afterwards. Append the outcome to the corresponding `docs/BUILD_LOG.md`
audit rows by flipping their Verdict cells — this is the one sanctioned in-place edit, because the
rows were authored with those cells routed to this task.
  </action>
  <acceptance_criteria>
    - `git ls-files uv.lock` returns `uv.lock`
    - Every directory listed in SPEC-08 section 2 exists; `find config src dbt scripts data exports reports dashboards tests -type d -empty` returns nothing
    - `git ls-files 'data/warehouse/.gitkeep' 'data/cache/.gitkeep'` returns both paths, while `git check-ignore -q data/warehouse/x.duckdb` succeeds
    - `.env.example` contains exactly one assignment line and it is `AMBO_PRIVATE_DROP`
    - `grep -c 'Rafael Braga-Kribitz' LICENSE` returns 1 and the file contains the MIT permission text
    - `git status --porcelain` is clean after the ignore probe
  </acceptance_criteria>
  <verify>
    <automated>[ -n "$(git ls-files uv.lock)" ] && [ -z "$(find config src dbt scripts data exports reports dashboards tests -type d -empty)" ] && [ -n "$(git ls-files data/warehouse/.gitkeep)" ] && git check-ignore -q data/warehouse/probe.duckdb && [ "$(grep -c '^[A-Z_]*=' .env.example)" = "1" ] && grep -q '^AMBO_PRIVATE_DROP=' .env.example && [ "$(grep -c 'Rafael Braga-Kribitz' LICENSE)" = "1" ] && grep -q 'MIT' LICENSE && [ -z "$(git status --porcelain)" ]</automated>
  </verify>
  <done>`uv.lock` is committed, the SPEC-08 section 2 tree exists with tracked placeholders in the ignored directories, `.env.example` documents only `AMBO_PRIVATE_DROP`, LICENSE is MIT with the Charter author, and the ignore probe is proven and recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI → local environment | `uv sync` executes package build backends and installs third-party code; this is the phase's only inbound untrusted input |
| private drop → repository | `.env.example` describes the drop path; the real value must never be written into a tracked file |
| working tree → git history | `uv.lock` is permanent once committed (EB-082); EB-030 makes a pin change an ADR event |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | `uv lock` / `uv sync` package installs | high | mitigate | Task 2 is a blocking human checkpoint over the resolved dependency set before `uv.lock` is committed: direct-dependency comparison against SPEC-08 section 3, package-count sanity check, unknown-name spot check on pypi.org, forbidden-framework sweep, and a fork-marker check. The lockfile is committed (EB-030) so every later `uv sync` installs exactly the reviewed set |
| T-01-03 | Tampering | Charter O-3 scope wall at dependency level | high | mitigate | Task 2 step 4 sweeps `uv tree` for any additional MMM framework; plan 01-07 lands the standing `tests/unit/test_forbidden_deps.py` guard that also scans `uv.lock`, so the sweep becomes permanent rather than a one-time check |
| T-01-06 | Information Disclosure | `.env.example` | medium | mitigate | The example path is explicitly fictional and the file carries exactly one variable; `.env` itself is already ignored by `.gitignore`. Plan 01-07's leak scan adds the standing check |
| T-01-10 | Tampering | tool configuration in `pyproject.toml` | medium | mitigate | Single config home means a weakened threshold is a visible one-line diff in a reviewed file rather than a hidden `setup.cfg`; the acceptance criteria assert no competing config file exists |
| T-01-11 | Denial of Service | build-backend compilation during `uv sync` | low | accept | Offline single-user machine; the known `g++`/pytensor NumPy-backend fallback is a performance issue owned by Phase 4 and is on the risk register, not a Phase 1 blocker |

**Package legitimacy gate.** `01-RESEARCH.md` carries a complete `## Package Legitimacy Audit`
table for all 20 SPEC-08 section 3 packages. Zero `SLOP` verdicts; all 20 `SUS` with disposition
Approved on documented heuristic-artifact grounds. Per protocol a blocking human checkpoint
precedes the first commit of the lockfile — Task 2 above. That checkpoint is not
auto-approvable regardless of `workflow.auto_advance`.
</threat_model>

<verification>
- `uv sync` from a clean checkout succeeds on this machine
- `uv run mypy` reports success on the empty package
- `uv tree --depth 1` matches SPEC-08 section 3 exactly
- The T-003 AC-2 ignore probe behaves as specified and leaves the tree clean
</verification>

<success_criteria>
1. The pinned dependency set is committed as a single universal `uv.lock`, reviewed by a human before that commit, with no forbidden modeling framework present at any depth.
2. `pyproject.toml` is the sole configuration home for ruff, mypy, pytest and coverage.
3. The coverage gate exists at 80 and is demonstrably non-blocking this phase, with its flip condition written next to it.
4. The SPEC-08 section 2 tree exists so every later path-globbing script and the layout guard have a real tree to assert against.
</success_criteria>

<!-- planner-discipline-allow: robyn -->
<!-- planner-discipline-allow: lightweight_mmm -->
<!-- planner-discipline-allow: prophet -->
<!-- planner-discipline-allow: sklearn -->

## Artifacts this phase produces

Every symbol below is **created by Phase 1** — none exists in the repository today. The
source-grounding pass must treat all of them as new, not as drift.

**Repository root:** `.gitattributes`, `.env.example`, `LICENSE`, `README.md`, `Makefile`,
`pyproject.toml`, `uv.lock`, `.pre-commit-config.yaml`

**`.github/`:** `.github/workflows/ci.yml`, `.github/pull_request_template.md`

**`docs/`:** `docs/BUILD_LOG.md`, `docs/MODULE_CONTRACTS.md`, `docs/RISK_REGISTER.md`,
`docs/ADR/TEMPLATE.md`, `docs/ADR/README.md`,
`docs/ADR/ADR-006_module-contracts-layout-and-citation-policy.md`

**`config/`, `dbt/`:** `config/settings.yaml`, `dbt/seeds/season_windows.csv`

**Python modules:** `src/ambo/__init__.py`, `src/ambo/common/__init__.py`,
`src/ambo/common/config.py`, `src/ambo/common/errors.py`, `src/ambo/common/logging.py`

**Scripts:** `scripts/leak_scan.py`, `scripts/generate_season_windows.py`,
`scripts/check_layer_order.py`, `scripts/check_ssot_consistency.py`

**Tests:** `tests/conftest.py`, `tests/unit/test_config.py`, `tests/unit/test_logging.py`,
`tests/unit/test_leak_scan.py`, `tests/unit/test_season_windows.py`,
`tests/unit/test_forbidden_deps.py`, `tests/unit/test_import_independence.py`,
`tests/unit/test_no_requests.py`, `tests/unit/test_repo_layout.py`,
`tests/unit/test_line_endings.py`, `tests/unit/test_gitignore.py`,
`tests/unit/test_governance_checks.py`

**Python symbols**
- `src/ambo/__init__.py` — `__version__`
- `src/ambo/common/errors.py` — `AmboError`, `ConfigError`
- `src/ambo/common/config.py` — `PathsConfig`, `SamplerConfig`, `ScenarioConfig`, `Settings`, `load_settings()`, `repo_root()`
- `src/ambo/common/logging.py` — `LOG_FORMAT`, `PrivatePathFilter`, `get_logger()`
- `scripts/leak_scan.py` — `Finding`, `PRIVATE_DROP_PATTERNS`, `CONTACT_PATTERNS`, `CURRENCY_LITERAL_PATTERN`, `CONTACT_SCAN_ROOTS`, `scan_tree()`, `scan_staged()`, `main()`
- `scripts/generate_season_windows.py` — `YEAR_RANGE`, `SEED_PATH`, `JAN_DIP_WEEKS`, `SPRING_WEEKS`, `SUMMER_LULL_WEEKS`, `advent_weeks()`, `schulbeginn_weeks()`, `build_rows()`, `main()`
- `scripts/check_layer_order.py` — `LAYER_R_GLOBS`, `first_commit_adding()`, `main()`
- `scripts/check_ssot_consistency.py` — `SSOT_PATH`, `parse_ssot()`, `main()`
- `tests/conftest.py` — fixtures `seeded_rng`, `repo_root`, `tmp_repo`

**Makefile targets (18, SPEC-08 section 5):** `setup`, `simulate`, `validate-sim`, `intake`,
`anonymize`, `validate-intake`, `transform`, `fit-synthetic`, `fit-real`, `recover`,
`sensitivity`, `decide`, `ssot`, `export`, `report`, `test`, `lint`, `all`

**CI job names (6, EB-060 / Guide section 9):** `lint`, `test`, `dbt`, `ssot`, `layer-order`, `leak`

**pytest markers:** `smoke`, `fit`

<output>
Create `.planning/phases/01-repository-foundation/01-02-SUMMARY.md` when done.
</output>
