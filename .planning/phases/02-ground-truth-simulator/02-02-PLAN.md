---
phase: 02-ground-truth-simulator
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ambo/common/errors.py
  - src/ambo/simulate/config.py
  - docs/MODULE_CONTRACTS.md
  - tests/unit/test_scenario_config.py
autonomous: true
requirements: [REQ-q1-truth-recovery, REQ-grain-and-windows]

must_haves:
  truths:
    - "`ScenarioConfig` rejects any key not declared on the model (`extra='forbid'`), so a typo in a scenario YAML is a load-time failure and never a silently ignored parameter."
    - "A scenario whose `weeks` is not exactly 156, 104 or 78 is rejected at load time — the Layer P grain of SPEC-01 §5 cannot be widened by editing a YAML (REQ-grain-and-windows)."
    - "The `(id, weeks, seed)` triple must be one of ('s_a',156,101), ('s_b',104,202), ('s_c',78,303); any other combination raises `SimulationError`."
    - "`ScenarioConfig.channels` must be exactly the six SPEC-01 §4 channels in taxonomy order; the seventh taxonomy slot `other` is a Layer R channel and is rejected in a Layer P scenario."
    - "A scenario declaring `id: s_c` must have `display_video` β exactly 0.0, and `s_a`/`s_b` must have β strictly greater than 0 — the zero-effect channel cannot be introduced or removed by accident."
    - "Every promo week and every burst span must fall inside the declared window `[start_iso_year/start_iso_week .. end_iso_year/end_iso_week]`; a schedule referencing a week outside the window raises `SimulationError` at config validation, never at runtime."
    - "Two bursts in the same channel whose spans overlap are rejected."
    - statement: "Two bursts in the same channel whose spans are exactly adjacent (previous start + burst_length == next start) are accepted as two distinct bursts and are never merged into one longer burst; only true overlap is rejected."
      verification: backstop
    - "A channel's platform-bias block is either fully populated (`phi`, `theta`, `cpm` all present) or fully absent (all three `None`) — a half-populated offline channel is rejected."
    - "`load_scenario()` raises `SimulationError` naming the expected path when the scenario YAML is missing, and naming the failing key path when validation fails — never a bare `KeyError`, `FileNotFoundError` or pydantic `ValidationError` escaping the module boundary."
  artifacts:
    - path: "src/ambo/simulate/config.py"
      provides: "ScenarioConfig model tree and load_scenario()"
      exports: ["TrueParams", "SpendPattern", "PlatformBiasParams", "ChannelConfig", "SeasonWeights", "ScenarioConfig", "SPEC_CHANNEL_ORDER", "load_scenario"]
    - path: "src/ambo/common/errors.py"
      provides: "SimulationError typed exception"
      contains: "class SimulationError"
    - path: "docs/MODULE_CONTRACTS.md"
      provides: "contract-first entry for simulate/config.py (D-23)"
      contains: "### src/ambo/simulate/config.py"
    - path: "tests/unit/test_scenario_config.py"
      provides: "structural validation tests for ScenarioConfig"
      min_lines: 120
  key_links:
    - from: "src/ambo/simulate/config.py"
      to: "src/ambo/common/config.py"
      via: "imports repo_root() and load_settings() — simulate may import ambo.common only (SIM-003)"
      pattern: "from ambo\\.common\\.config import"
    - from: "src/ambo/simulate/config.py"
      to: "src/ambo/common/errors.py"
      via: "raises SimulationError instead of a bare built-in (A-7)"
      pattern: "SimulationError"
    - from: "docs/MODULE_CONTRACTS.md"
      to: "src/ambo/simulate/config.py"
      via: "test_repo_layout.py set-equality between module files and contract headings"
      pattern: "### src/ambo/simulate/config\\.py"
---

<objective>
Build T-101's first half: the pydantic `ScenarioConfig` model tree that makes SPEC-01
§3–§6 mechanically checkable, plus the typed error every `src/ambo/simulate/` module will
raise, plus the contract-first `docs/MODULE_CONTRACTS.md` entry that D-23's layout guard
requires in the same PR as the module.

Purpose: SIM-002 makes the scenario YAML the authoritative parameter source. A schema with
`extra='forbid'` and domain validators is what turns "the YAML is authoritative" from a
convention into a load-time failure. Plan 02-03 authors the three YAMLs against this schema;
every later plan reads parameters only through `load_scenario()`.

Output: `src/ambo/simulate/config.py`, `SimulationError` in `src/ambo/common/errors.py`,
two `docs/MODULE_CONTRACTS.md` edits, and `tests/unit/test_scenario_config.py`'s structural
half (the SPEC-table-equality half lands in 02-03, once the YAMLs exist).

This plan authors no parameter *values* — it authors the shape those values must satisfy.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ground-truth-simulator/02-CONTEXT.md
@.planning/phases/02-ground-truth-simulator/02-RESEARCH.md
@.planning/phases/02-ground-truth-simulator/02-PATTERNS.md
@docs/SPEC-01_ground_truth_simulator.md
@docs/MODULE_CONTRACTS.md
@src/ambo/common/config.py
@src/ambo/common/errors.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SimulationError, the ScenarioConfig model tree, and their module contracts</name>
  <files>src/ambo/common/errors.py, src/ambo/simulate/config.py, docs/MODULE_CONTRACTS.md</files>
  <read_first>
    - `src/ambo/common/config.py` (all 172 lines) — the exact patterns to replicate: the module docstring shape naming the SPEC/EB IDs plus an `Implements:` line and a single-home statement; `model_config = ConfigDict(extra="forbid", frozen=True)` on every model; `@field_validator` with `@classmethod`; `repo_root()`; `@functools.lru_cache` on the loader; the `try/except ValidationError` block that re-raises with the failing key path in the message.
    - `src/ambo/common/errors.py` (all 27 lines) — the `AmboError` root, the `ConfigError` subclass, and the redaction invariant stated in the docstring.
    - `docs/MODULE_CONTRACTS.md` — the preamble's "Entry format" (five labelled sections in this order: `Purpose`, `Public API`, `Invariants`, `Failure modes`, `Testing`), the level-3-heading-is-the-path rule, and the existing `src/ambo/common/config.py` entry as the shape to copy.
    - `tests/unit/test_repo_layout.py` lines 112–160 — the set-equality guard: a module with no entry fails, and an entry naming a module that does not exist fails equally. The scan uses `rglob("*.py")` on the filesystem, so an untracked stray `.py` under `src/ambo/` also trips it.
    - `docs/SPEC-01_ground_truth_simulator.md` §3, §4, §5, §6 — the parameter tables the schema must be able to hold.
    - `docs/EXECUTION_BLUEPRINT/03_MODULES.md` §2.1 (lines 94–100) — the declared `ScenarioConfig` field list and its two stated invariants.
    - `docs/EXECUTION_BLUEPRINT/02_WBS.md` T-101 (lines 268–292) — objective, validation and acceptance criteria.
    - `docs/EXECUTION_BLUEPRINT/05_IMPLEMENTATION_GUIDES.md` §1.1 and §1.3 — the promo/burst authoring rules and the collinearity-switch-is-data rule.
    - `config/settings.yaml` — the seven-entry `channels` taxonomy; the six Layer P channels are its first six entries, and `other` is Layer R only.
  </read_first>
  <behavior>
    - `ScenarioConfig(**valid_s_a_dict)` constructs and is frozen (assignment raises).
    - `ScenarioConfig(**{**valid, "unexpected_key": 1})` raises `ValidationError` naming `unexpected_key`.
    - `weeks=157` raises; `weeks` in {156, 104, 78} with the matching id/seed constructs.
    - `("s_a", 156, 999)` raises — the seed is pinned by SPEC-01 §5.
    - `channels` missing `radio`, or containing `other`, or ordered differently from `SPEC_CHANNEL_ORDER`, raises.
    - `id="s_c"` with `display_video.true_params.beta=6000.0` raises; with `0.0` constructs.
    - `id="s_a"` with `display_video.true_params.beta=0.0` raises.
    - A `print_regional` burst schedule with starts `(10, 11)` and `burst_length=2` raises (overlap); with `(10, 12)` constructs (exactly adjacent, accepted as two bursts).
    - A promo week or burst start outside `[start_iso_year/start_iso_week .. end_iso_year/end_iso_week]` raises.
    - A `platform` block with `phi` set but `cpm` `None` raises; all-three-set and all-three-`None` both construct.
    - `load_scenario("s_z")` raises `SimulationError`; `load_scenario` against a missing file raises `SimulationError` whose message contains the expected path.
  </behavior>
  <action>
**Step 1 — `src/ambo/common/errors.py`.** Add `class SimulationError(AmboError)` with a
docstring stating it is raised by every module in `src/ambo/simulate/` for: a missing or
invalid scenario YAML, an out-of-domain math input, a missing season-window row, and a
failed decomposition audit. Keep the existing redaction invariant wording. Do not touch
`AmboError` or `ConfigError`.

This is a deliberate promotion over `docs/EXECUTION_BLUEPRINT/03_MODULES.md` §2.2/§2.3,
which say `ValueError` for those two failure modes. `docs/MODULE_CONTRACTS.md` is the
contract of record and supersedes `03_MODULES.md` for contracts written here, and
09_ANTI_PATTERNS A-7 plus the existing `errors.py` contract forbid bare built-ins for
expected failure conditions. Plain `ValueError` remains correct *inside* a pydantic
`field_validator`/`model_validator` (that is the pydantic idiom — pydantic collects it into
a `ValidationError`), and `load_scenario()` converts that `ValidationError` into
`SimulationError` at the module boundary. Record this promotion as an interpretation in the
plan SUMMARY so plan 02-10 can carry it into `docs/BUILD_LOG.md` (ADR README D-06/D-07:
filling a gap without changing spec text is a BUILD_LOG interpretation, not an ADR).

**Step 2 — `src/ambo/simulate/config.py`.** Module docstring naming SIM-002, SIM-030, EB-040
and SPEC-01 §3–§6, an `Implements: REQ-q1-truth-recovery, REQ-grain-and-windows` line, and
an explicit single-home statement: this module is the only one that reads
`config/scenarios/*.yaml`, and it never imports anything from `ambo.model` (SIM-003).

Declare, in this order, all with `model_config = ConfigDict(extra="forbid", frozen=True)`:

- `SPEC_CHANNEL_ORDER: tuple[str, ...] = ("search_brand", "search_generic", "meta", "display_video", "print_regional", "radio")` — the six SPEC-01 §4 channels. Add a module-level comment stating these are `Settings.channels[:6]` and that `other` is a Layer R taxonomy slot with no SPEC-01 §4 row.
- `class TrueParams` — `lam: float`, `K: float`, `s: float`, `beta: float`. Field names match `03_MODULES.md` §2.1 verbatim (`K` uppercase is intentional; ruff's selected rule set is `E, F, I, UP, B` — no pep8-naming). Domains: `0.0 <= lam < 1.0` (the closed form `x/(1-lam)` diverges at 1), `K > 0`, `s > 0`, `beta >= 0`.
- `class SpendPattern` — `mean: float` (> 0), `sd: float` (>= 0), `floor_eur: int | None`, `advent_factor: float` (>= 0), `spring_factor: float` (>= 0), `pulse_every: int | None` (> 0 when set), `pulse_multiplier: float | None` (> 0 when set), `burst_length: int | None` (> 0 when set), `burst_starts: dict[int, tuple[int, ...]] | None` keyed by ISO year. A `model_validator` enforces: `burst_length` and `burst_starts` are both set or both `None`; `pulse_every` and `pulse_multiplier` are both set or both `None`.
- `class PlatformBiasParams` — `phi: float | None`, `theta: float | None`, `cpm: float | None`. A `model_validator` enforces all-three-set or all-three-`None` (offline channels are all-`None` per SPEC-01 §6).
- `class ChannelConfig` — `true_params: TrueParams`, `spend: SpendPattern`, `platform: PlatformBiasParams`.
- `class SeasonWeights` — `advent: float`, `schulbeginn: float`, `spring: float`, `jan_dip: float`, `summer_lull: float`. No defaults: the YAML must state all five, and 02-03's spec-equality test pins them to `+0.55, +0.25, +0.15, -0.20, -0.10`.
- `class ScenarioConfig` — `id: str`, `weeks: int`, `seed: int`, `start_iso_year: int`, `start_iso_week: int`, `end_iso_year: int`, `end_iso_week: int`, `collinearity: bool`, `b0: float`, `growth: float`, `noise_share: float`, `aov_base: float`, `aov_advent_bonus: float`, `promo_multiplier: float`, `season_weights: SeasonWeights`, `promo_weeks: dict[int, tuple[int, ...]]`, `channels: dict[str, ChannelConfig]`.

`ScenarioConfig` validators:

1. `field_validator("channels")` — `tuple(value) == SPEC_CHANNEL_ORDER`; the error message names the offending key set and the expected order. This rejects `other` and any reordering.
2. `field_validator("weeks")` — must be in `{156, 104, 78}`.
3. `model_validator(mode="after")` `_validate_scenario_identity` — the `(id, weeks, seed)` triple must be one of `("s_a", 156, 101)`, `("s_b", 104, 202)`, `("s_c", 78, 303)` (SPEC-01 §5).
4. `model_validator(mode="after")` `_validate_zero_effect_channel` — `channels["display_video"].true_params.beta == 0.0` if and only if `id == "s_c"` (SPEC-01 §4 footnote).
5. `model_validator(mode="after")` `_validate_schedules_inside_window` — every `promo_weeks` key and every `burst_starts` key must be an ISO year in `range(start_iso_year, end_iso_year + 1)`; every promo week `(year, week)` and every burst span `(year, start) .. (year, start + burst_length - 1)` must satisfy `(start_iso_year, start_iso_week) <= (year, w) <= (end_iso_year, end_iso_week)` under plain tuple comparison. Window membership is checked on `(iso_year, iso_week)` tuples only — this module performs no calendar arithmetic and never reads `dbt/seeds/season_windows.csv`; that read belongs to `dgp.py` (AD-020, single home).
6. `model_validator(mode="after")` `_validate_bursts_do_not_overlap` — per channel, per year, sort the starts and require `next_start >= prev_start + burst_length`. Equality is **accepted**: two exactly-adjacent bursts remain two distinct bursts and are never merged. Only `next_start < prev_start + burst_length` raises. State this reading in the validator docstring — SPEC-01 §3 and Guide §1.1 forbid overlap and are silent on touching, so the accept-on-adjacency reading is an interpretation for `docs/BUILD_LOG.md`.

Add two read-only helpers used by later plans and by the 02-03 tests:
- `covered_iso_years(self) -> tuple[int, ...]` — `tuple(range(start_iso_year, end_iso_year + 1))`.
- `covered_week_count(self, iso_year: int) -> int` — for a fully covered year the caller supplies the year's ISO week count; for the first/last year return the covered span. Implement it purely from the declared endpoints plus a `weeks_in_iso_year` argument so the function stays calendar-free: signature `covered_week_count(self, iso_year: int, weeks_in_iso_year: int) -> int`.

`load_scenario(name: str) -> ScenarioConfig`, decorated `@functools.lru_cache(maxsize=None)`:
resolve `load_settings().scenarios[name].config` (raise `SimulationError` naming the known
scenario ids if `name` is not a key), anchor it through `repo_root()`, raise `SimulationError`
naming the expected absolute path if the file does not exist, `yaml.safe_load` it, and wrap
`ValidationError` in `SimulationError` whose message lists the failing key paths — copy the
`failing_keys` join pattern from `ambo/common/config.py`'s `load_settings()` verbatim.

**Step 3 — `docs/MODULE_CONTRACTS.md`.** Two edits, both in the same commit as the code:
(a) extend the existing `### src/ambo/common/errors.py` entry's `Public API` list with a
`SimulationError(AmboError)` bullet and note in `Invariants` that it is the simulate-package
error root; (b) append a new level-3 entry `### src/ambo/simulate/config.py` after the
`src/ambo/common/logging.py` entry and before the `---` that precedes `## Dependency
directions`, with all five labelled sections in the prescribed order. `Public API` lists
every symbol above with its signature. `Invariants` states: `extra='forbid'` and `frozen=True`
on every model; `channels` exactly `SPEC_CHANNEL_ORDER`; the `(id, weeks, seed)` triple pinned
to SPEC-01 §5; `s_c` ⇔ zero β on `display_video`; schedules inside the declared window;
bursts non-overlapping with adjacency permitted; no calendar arithmetic and no season-window
read in this module. `Failure modes` states every raise site as `SimulationError`.
`Testing` points at `tests/unit/test_scenario_config.py`.

Do not create any other file under `src/ambo/simulate/` in this task — the layout guard's
set-equality would fail on an entry-less module.
  </action>
  <verify>
    <automated>uv run mypy && uv run pytest tests/unit/test_repo_layout.py tests/unit/test_import_independence.py tests/unit/test_forbidden_deps.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run mypy` exits 0 under `strict = true` (no `Any` leaks, every signature annotated).
    - `uv run pytest tests/unit/test_repo_layout.py -x` exits 0 — proves the `### src/ambo/simulate/config.py` heading exists and the module/entry sets are equal.
    - `uv run pytest tests/unit/test_import_independence.py -x` exits 0 — proves no `ambo.model` import edge was introduced.
    - `uv run python -c "from ambo.simulate.config import SPEC_CHANNEL_ORDER; from ambo.common.config import load_settings; assert SPEC_CHANNEL_ORDER == tuple(load_settings().channels[:6]); assert 'other' not in SPEC_CHANNEL_ORDER"` exits 0.
    - `uv run python -c "from ambo.common.errors import AmboError, SimulationError; assert issubclass(SimulationError, AmboError)"` exits 0.
    - `make lint` exits 0.
    - `grep -c '^### src/ambo/simulate/config.py$' docs/MODULE_CONTRACTS.md` returns exactly 1.
  </acceptance_criteria>
  <done>`SimulationError` exists, `ScenarioConfig` and its six validators exist and type-check under mypy strict, both `docs/MODULE_CONTRACTS.md` edits landed in the same commit, and the four Phase 1 architectural guards are still green.</done>
</task>

<task type="auto">
  <name>Task 2: Structural validation tests for ScenarioConfig</name>
  <files>tests/unit/test_scenario_config.py</files>
  <read_first>
    - `tests/unit/test_config.py` (all 162 lines) — the exact test conventions to replicate: the `@pytest.fixture(autouse=True)` cache-clearing fixture (`load_settings.cache_clear()` before and after each test), the `with pytest.raises(ConfigError, match="totally_unexpected_key"):` typed-error assertion style, the "re-derive independently in the test rather than trusting the loader" round-trip style, and the single-config-home grep guard at lines 142–162.
    - `tests/conftest.py` — the `repo_root` and `seeded_rng` fixtures already available; do not construct a new `default_rng()` inline.
    - `src/ambo/simulate/config.py` — the module written in Task 1; the exact field names and validator error messages the tests assert on.
    - `.planning/phases/02-ground-truth-simulator/02-PATTERNS.md` section "`tests/unit/test_scenario_config.py` … (test, assertion-heavy)".
  </read_first>
  <action>
Create `tests/unit/test_scenario_config.py`. Module docstring naming SIM-002, BP-G-02 and
`REQ-grain-and-windows`.

Provide one module-level helper `_valid_scenario_dict(**overrides) -> dict[str, object]`
that returns a minimal but fully valid `s_a`-shaped payload (id `s_a`, weeks 156, seed 101,
window 2021/1 to 2023/52, `collinearity=False`, the six channels in `SPEC_CHANNEL_ORDER`
with plausible in-domain values, `display_video` β strictly positive, offline channels'
`platform` block all-`None`), with shallow-merge overrides. Every negative test builds from
this helper so a failure isolates exactly one deviation. This helper hard-codes no SPEC-01
§4 value that plan 02-03's spec-equality test also asserts — it is a shape fixture, not a
second home for the parameter table.

Add an `@pytest.fixture(autouse=True)` that calls `load_scenario.cache_clear()` before and
after each test, mirroring `test_config.py`'s `_clear_settings_cache`.

Write one test per behaviour listed in Task 1's `<behavior>` block, plus:
- `test_spec_channel_order_matches_settings_taxonomy` — `SPEC_CHANNEL_ORDER == tuple(load_settings().channels[:6])` and `"other" not in SPEC_CHANNEL_ORDER`. This is the A-8 single-home guard for the taxonomy.
- `test_models_are_frozen` — assigning to a field on a constructed `ScenarioConfig` raises.
- `test_adjacent_bursts_are_two_distinct_bursts` — with `burst_length=2` and starts `(10, 12)` the model constructs AND `len(cfg.channels["print_regional"].spend.burst_starts[2021]) == 2`; the two bursts are not collapsed. Name the test so `pytest -k adjacency` selects it.
- `test_load_scenario_missing_file_names_the_path` — monkeypatch the settings registry (or point at a `tmp_path` that has no YAML) and assert `pytest.raises(SimulationError, match=...)` where the match is the expected filename.
- `test_load_scenario_unknown_name` — `load_scenario("s_z")` raises `SimulationError` whose message lists the known ids.
- `test_covered_iso_years` — `s_a`-shaped config returns `(2021, 2022, 2023)`; `s_c`-shaped returns `(2022, 2023)`.

Use `pytest.raises(ValidationError)` for direct-construction negatives and
`pytest.raises(SimulationError)` for `load_scenario()` negatives — the boundary conversion
is itself the behaviour under test. Every `pytest.raises` carries a `match=` argument
naming the offending field or value, so a test cannot pass on an unrelated validation error.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_scenario_config.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_scenario_config.py -x` exits 0 and reports at least 14 collected tests.
    - `uv run pytest tests/unit/test_scenario_config.py -k adjacency -x` exits 0 and collects at least 1 test.
    - Every `pytest.raises(...)` call in the file passes a `match=` keyword (`grep -c 'pytest.raises(' tests/unit/test_scenario_config.py` equals `grep -c 'match=' tests/unit/test_scenario_config.py`).
    - `make test` exits 0 (whole suite, Phase 1 guards included).
    - `make lint` exits 0.
  </acceptance_criteria>
  <done>Every validator in `ScenarioConfig` has a green positive test and a red-on-violation negative test with a message-matched assertion, and the full suite is green.</done>
</task>

</tasks>

## Artifacts this phase produces

Full phase-wide symbol list (all ten plans). This plan creates the first two rows.

| Owner | Symbols / files |
|-------|-----------------|
| **`src/ambo/common/errors.py` (this plan, extended)** | `class SimulationError(AmboError)` |
| **`src/ambo/simulate/config.py` (this plan)** | `SPEC_CHANNEL_ORDER`, `TrueParams`, `SpendPattern`, `PlatformBiasParams`, `ChannelConfig`, `SeasonWeights`, `ScenarioConfig` (+ `covered_iso_years`, `covered_week_count`), `load_scenario(name)` |
| `config/scenarios/{s_a,s_b,s_c}.yaml` (02-03) | authored scenario parameter files (SIM-002 authoritative) |
| `scripts/author_scenario_schedules.py` (02-03) | `main(argv=None) -> int` — dev-only authoring aid (D-02) |
| `src/ambo/simulate/spend_patterns.py` (02-04) | `generate_spend(cfg, rng)` |
| `src/ambo/simulate/dgp.py` (02-05, 02-06) | `week_index`, `season_index`, `baseline_demand`, `adstock_recursive`, `hill`, `assemble_scenario`, `SimulationResult`, `decomposition_audit`, `plausibility_audit`, `peak_week_audit` |
| `src/ambo/simulate/platform_bias.py` (02-07) | `platform_report(result, cfg)` |
| `src/ambo/simulate/truth.py` (02-08) | `ChannelTruth`, `TruthFile`, `response_curve_at`, `marginal_roas_at`, `compute_truth`, `write_truth` |
| `src/ambo/simulate/__main__.py` (02-09) | `main(argv=None)`, `validate_sim(outdir)`; CLI positionals `all\|s_a\|s_b\|s_c\|validate`, flag `--outdir PATH` |
| `docs/MODULE_CONTRACTS.md` | one new level-3 entry per module above (contract-first, D-23) |
| `docs/ADR/ADR-007_hypothesis-dev-dependency.md` (02-01) | EB-030 ADR for the new dev dependency |
| `Makefile` (02-09) | `simulate` and `validate-sim` targets replacing the Phase 1 stubs |
| `data/synthetic/{s_a,s_b,s_c}/` (02-10) | `media_weekly.csv`, `outcome_weekly.csv`, `truth.json` |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `config/scenarios/*.yaml` → `ScenarioConfig` | The only untrusted-input boundary in this plan. A hand-authored YAML file is parsed and becomes the parameter set of the entire ground-truth argument. |

Explicitly **not applicable**, and why: **V2 Authentication**, **V3 Session Management**,
**V4 Access Control** — no user-facing surface, no server, no principal exists in this
phase. **V6 Cryptography** — no secrets, no crypto operations; all values are disclosed by
design (SPEC-01's opening line). **Network** — EB-070 forbids `requests` anywhere in
`src/ambo/`, guarded by `tests/unit/test_no_requests.py`; this module performs one local
file read and nothing else.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04 | Tampering | `config/scenarios/*.yaml` → `ScenarioConfig` (V5 Input Validation) | medium | mitigate | `model_config = ConfigDict(extra="forbid", frozen=True)` on every model, so an unknown or misspelled key is a load-time failure rather than a silently ignored parameter; `yaml.safe_load` only (never `yaml.load`), so a YAML file cannot construct arbitrary Python objects; every numeric field carries an explicit domain constraint. |
| T-02-05 | Tampering | scenario identity (`id`, `weeks`, `seed`, zero-β) | high | mitigate | `_validate_scenario_identity` and `_validate_zero_effect_channel` pin the SPEC-01 §5 triple and the S-C zero-effect channel, so the ground truth the recovery argument depends on cannot be moved by a one-character YAML edit; violation raises before any data is generated. |
| T-02-06 | Information Disclosure | `SimulationError` message content | low | mitigate | Error messages carry key paths and the expected repository-relative file path only, never file contents; the `errors.py` redaction invariant (no `AMBO_PRIVATE_DROP` path in any message) is restated in the new subclass's docstring. No private data exists in this phase regardless. |
| T-02-07 | Repudiation | schedule authoring provenance | low | accept | Promo/burst week lists are authored constants whose provenance is the 02-03 authoring aid plus `docs/BUILD_LOG.md`; git history is the audit trail. No further mechanism is warranted for disclosed synthetic parameters. |
</threat_model>

<verification>
- `uv run mypy` exits 0 (strict).
- `make lint` exits 0.
- `make test` exits 0, including `tests/unit/test_repo_layout.py` (contract-first set equality) and `tests/unit/test_import_independence.py` (SIM-003 firewall, now non-vacuous for `simulate/config.py`).
- `uv run pytest tests/unit/test_scenario_config.py -x` exits 0 with at least 14 tests.
</verification>

<success_criteria>
- `ScenarioConfig` can hold every SPEC-01 §3–§6 parameter and rejects every violation listed in `must_haves.truths`.
- `SimulationError` is the single typed error the `simulate` package raises; no bare built-in escapes `load_scenario()`.
- `docs/MODULE_CONTRACTS.md` gained the `src/ambo/simulate/config.py` entry in the same commit as the module (D-23).
- Plan 02-03 can author the three YAMLs against a schema that will reject their mistakes.
</success_criteria>

<output>
Create `.planning/phases/02-ground-truth-simulator/02-02-SUMMARY.md` when done. Record the
two interpretations this plan makes for `docs/BUILD_LOG.md` at M1 close: (1) `SimulationError`
supersedes `03_MODULES.md` §2.2/§2.3's `ValueError`; (2) exactly-adjacent bursts are accepted
as two distinct bursts.
</output>
