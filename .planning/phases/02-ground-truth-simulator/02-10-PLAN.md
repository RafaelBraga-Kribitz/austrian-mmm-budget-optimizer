---
phase: 02-ground-truth-simulator
plan: 10
type: execute
wave: 9
depends_on: ["02-09"]
files_modified:
  - data/synthetic/s_a/media_weekly.csv
  - data/synthetic/s_a/outcome_weekly.csv
  - data/synthetic/s_a/truth.json
  - data/synthetic/s_b/media_weekly.csv
  - data/synthetic/s_b/outcome_weekly.csv
  - data/synthetic/s_b/truth.json
  - data/synthetic/s_c/media_weekly.csv
  - data/synthetic/s_c/outcome_weekly.csv
  - data/synthetic/s_c/truth.json
  - docs/BUILD_LOG.md
autonomous: false
requirements: [REQ-q1-truth-recovery, REQ-grain-and-windows]

must_haves:
  truths:
    - "All nine Layer P artifacts are committed under `data/synthetic/{s_a,s_b,s_c}/` — they are on EB-081's committed-by-design list and are Phase 3's dbt input contract."
    - "The committed bytes survive a git round-trip: after `git checkout -- data/synthetic/`, `make validate-sim` is still green, so no checkout filter reintroduces CRLF into a file whose byte-identity is gated (the failure mode plan 01-07 hit with `core.autocrlf=true`)."
    - "`make simulate && make validate-sim` is green on the committed tree with the full seven-row gate table captured as PR evidence."
    - "`docs/BUILD_LOG.md` carries an M1 entry recording every interpretation this phase made, the promo/burst authoring rationale, and the measured effort total against the 720-minute Charter §5 budget and its 1440-minute stop-and-ADR tripwire."
    - "The M1 checklist from `06_CHECKLISTS.md` is ticked with evidence for every row, including the `[STD]` merge-readiness rows: lint and test green locally, coverage of `src/ambo/` at or above 80%, no TODO/FIXME/XXX in touched files, no file added outside the SPEC-08 §2 layout, and conventional commits carrying REQ IDs."
  prohibitions:
    - "MUST NOT tune a SPEC-01 §4 parameter, widen a SIM-07x bound, or hand-edit a generated artifact to make the milestone close. Every committed file is regenerated output; if a gate is red, the phase reopens rather than the artifact being adjusted (anti-patterns A-9/A-18, ROADMAP Phase 2 rollback rule)."
    - "MUST NOT present any committed artifact as real or client-derived. All nine files are disclosed synthetic GROUND-TRUTH; no brand-voice or narrative framing of the fictional advertiser is added here — that is Phase 9's job (CONTEXT.md D-04)."
  artifacts:
    - path: "data/synthetic/s_a/truth.json"
      provides: "S-A disclosed ground truth, 156 weeks"
      contains: "schema_version"
    - path: "data/synthetic/s_c/truth.json"
      provides: "S-C disclosed ground truth including the zero-effect channel"
      contains: "display_video"
    - path: "docs/BUILD_LOG.md"
      provides: "M1 entry with interpretations, authoring rationale and the effort tally"
      contains: "M1"
  key_links:
    - from: "data/synthetic/s_a/media_weekly.csv"
      to: "src/ambo/simulate/__main__.py"
      via: "the committed artifacts are the output of make simulate and are re-derivable from it"
      pattern: "media_weekly"
    - from: "docs/BUILD_LOG.md"
      to: ".planning/STATE.md"
      via: "the effort tally sums the per-plan durations recorded in the Performance Metrics table"
      pattern: "effort"
---

<objective>
Build T-109: close M1. Commit the nine Layer P artifacts, prove they survive a git round-trip
with the determinism gate still green, write the `docs/BUILD_LOG.md` entry recording every
interpretation this phase made and the measured effort against the Charter §5 budget, and
present the M1 checklist evidence for human sign-off.

Purpose: the committed `data/synthetic/` tree is Phase 3's only input and Phase 5's only
grading key. Everything else in this phase exists to make these nine files trustworthy; this
plan is where they become a fact in git history rather than a local artifact.

Output: nine committed data files, an appended `docs/BUILD_LOG.md` M1 entry, and a
human-approved M1 checklist.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ground-truth-simulator/02-CONTEXT.md
@.planning/phases/02-ground-truth-simulator/02-RESEARCH.md
@.planning/phases/02-ground-truth-simulator/02-09-SUMMARY.md
@docs/BUILD_LOG.md
@docs/SPEC-01_ground_truth_simulator.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate, commit and round-trip-verify the nine Layer P artifacts</name>
  <files>data/synthetic/s_a/media_weekly.csv, data/synthetic/s_a/outcome_weekly.csv, data/synthetic/s_a/truth.json, data/synthetic/s_b/media_weekly.csv, data/synthetic/s_b/outcome_weekly.csv, data/synthetic/s_b/truth.json, data/synthetic/s_c/media_weekly.csv, data/synthetic/s_c/outcome_weekly.csv, data/synthetic/s_c/truth.json</files>
  <read_first>
    - `.gitignore` — the `COMMITTED BY DESIGN` block naming `data/synthetic/` (EB-081); confirm no pattern excludes the new files and that `data/synthetic/.gitkeep` stays tracked.
    - `.gitattributes` — how `*.csv` and `*.json` are handled; plan 01-07's finding that `core.autocrlf=true` reintroduced CRLF into already-tracked files on a plain `git checkout --` is the precedent this task must guard against.
    - `tests/unit/test_line_endings.py` and `tests/unit/test_gitignore.py` — the Phase 1 guards these files must satisfy.
    - `tests/unit/test_repo_layout.py` lines 20–50 — `data` is a permitted top-level entry, so nine new files under it introduce no layout violation.
    - `src/ambo/simulate/__main__.py` — the CLI and gate runner that produce and verify these files.
  </read_first>
  <action>
Run `make simulate` to regenerate all nine artifacts into `data/synthetic/`, then
`make validate-sim` and capture the full seven-row gate table.

Stage and commit the nine files with a conventional commit carrying the REQ IDs, for example
`feat(02): commit Layer P synthetic datasets and truth files (REQ-q1-truth-recovery, REQ-grain-and-windows)`.
Confirm `git status --short` shows no other unexpected addition and no `.tmp-` debris.

Then run the git round-trip check, which is the point of this task and not a formality:
delete the working-tree copies, `git checkout -- data/synthetic/`, and re-run
`make validate-sim`. SIM-070 must still be green. If it is not, the cause is a checkout
filter rewriting line endings — fix it in `.gitattributes` (not by regenerating the files or
by relaxing the gate) and re-verify. Record the outcome either way; plan 01-07 hit exactly
this failure mode with `core.autocrlf=true`, so a green result here is evidence worth
capturing rather than an assumption.

Finally confirm `git diff --check` is clean and `uv run pytest tests/unit/test_line_endings.py tests/unit/test_gitignore.py tests/unit/test_repo_layout.py -x` exits 0.
  </action>
  <verify>
    <automated>make validate-sim && uv run pytest tests/unit/test_line_endings.py tests/unit/test_gitignore.py tests/unit/test_repo_layout.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-files data/synthetic | wc -l` returns 10 (nine artifacts plus the pre-existing `.gitkeep`).
    - `make validate-sim` exits 0 and its seven-row gate table is captured verbatim.
    - After `rm -rf data/synthetic/s_a data/synthetic/s_b data/synthetic/s_c && git checkout -- data/synthetic/`, `make validate-sim` still exits 0 — the committed bytes survive a checkout round-trip.
    - `git diff --check` produces no output.
    - No file matching `data/synthetic/**/*.tmp-*` exists.
    - `uv run pytest tests/unit/test_line_endings.py tests/unit/test_gitignore.py tests/unit/test_repo_layout.py -x` exits 0.
    - `git log -1 --format=%s` shows a conventional-commit subject containing both phase-requirement IDs.
  </acceptance_criteria>
  <done>The nine Layer P artifacts are committed, regenerate byte-identically, and survive a git checkout round-trip with the determinism gate still green.</done>
</task>

<task type="auto">
  <name>Task 2: The M1 BUILD_LOG entry — interpretations, rationale and the effort tally</name>
  <files>docs/BUILD_LOG.md</files>
  <read_first>
    - `docs/BUILD_LOG.md` — the append-only rule stated at the top and the Phase 1 / M0 entry's shape, especially its "Elapsed effort against the … Charter section 5 budget" paragraph, which established the 8-hour-working-day convention for the `d` unit and the practice of summing each plan's measured duration from `.planning/STATE.md`'s Performance Metrics table.
    - `.planning/STATE.md` — the Performance Metrics table holding each plan's measured duration.
    - All nine `.planning/phases/02-ground-truth-simulator/02-0*-SUMMARY.md` files — each records the interpretations and evidence this entry consolidates.
    - `docs/ADR/README.md` standing rule D-06/D-07 — an interpretation that fills a gap without changing spec text belongs in `docs/BUILD_LOG.md` plus an in-source comment, not in an ADR; a spec *edit* or a moved gate needs an ADR.
    - `docs/EXECUTION_BLUEPRINT/06_CHECKLISTS.md` M1 "Documentation" row — "Docstrings carry `Implements: SIM-xxx`; promo/burst authoring rationale in BUILD_LOG".
  </read_first>
  <action>
Append an M1 entry to `docs/BUILD_LOG.md` (append only — never edit an existing entry).
It must contain, each as its own short subsection:

**Scope.** T-101…T-109 plus the ADR-007 governance step; the ten plans and what each delivered.

**Interpretations recorded (not ADRs — D-07).** One line each, with the plan that made it:
1. `SimulationError(AmboError)` supersedes `03_MODULES.md` §2.2/§2.3's `ValueError` for expected failure conditions, because `docs/MODULE_CONTRACTS.md` is the contract of record and A-7 forbids bare built-ins; plain `ValueError` remains correct inside pydantic validators (02-02).
2. Exactly-adjacent burst spans in the same channel are accepted as two distinct bursts and never merged; only true overlap is rejected — SPEC-01 §3 and Guide §1.1 forbid overlap and are silent on touching (02-02).
3. Scenario windows: S-A 2021-W01…2023-W52 (156), S-B 2022-W01…2023-W52 (104), S-C 2022-W01…2023-W26 (78), each cross-checked against the row count in `dbt/seeds/season_windows.csv`; SPEC-01 §5 fixes the week counts but not the calendar endpoints (02-03).
4. The four authored per-channel CPM constants and their descriptive-only status — BP-D-02 mandates CPM constants in the scenario YAML but names no values (02-03).
5. `meta` carries an `advent_factor` of 0.5 in S-A and 0.9 in S-B/S-C: SPEC-01 §3's `meta` row states no seasonal multiplier, but SIM-030's "0.5→0.9 for search_generic/meta" presupposes the 0.5 baseline, and Guide §1.3 requires the switch to live in data (02-03).
6. Radio's two Advent-anchored bursts are authored as a lead-in burst plus an in-Advent burst, because two 3-week bursts cannot both fit inside a 4-week Advent window without overlapping (02-03).
7. `season_index` takes the five signed weights as an argument rather than hard-coding them, so SIM-002's YAML stays the only home (02-04).
8. `generate_spend` takes the week-index frame as a third argument rather than reading the calendar seed itself, preserving the Functional-Core/Imperative-Shell split and AD-020's single home (02-05).
9. SIM-073 is evaluated for ISO year 2022 only in S-C, because its 2023 half-year does not cover an Advent window; the skipped year is reported by the gate runner, never silently omitted (02-06).
10. The `truth.json` float-format mechanism actually used, closing 02-RESEARCH.md Assumption A3 (02-08).
11. SPEC-01 §8's grid note already resolves INGEST-CONFLICTS WARNING 4; the ROADMAP's Phase 2 "must resolve before execution" text was stale, and `truth.py` exposes `response_curve_at` as a function of a caller-supplied grid so Phase 3/8 reuses the closed form (02-08).

**Promo/burst authoring rationale** (the M1 Documentation checklist row). How
`scripts/author_scenario_schedules.py` places the anchored and unanchored weeks, why the
output is frozen into YAML rather than computed at runtime (Guide §1.1, D-02), and why the
aid lives outside `src/` (A-13).

**Governance.** ADR-007 for the `hypothesis` dev dependency (EB-030), and the blocking human
checkpoint on its automated `SUS` legitimacy verdict.

**Gate evidence.** The seven-row `make validate-sim` table, the SIM-070 hash pair, the
SIM-071 maximum deviation, the SIM-072 statistics per scenario, and the SIM-073 peak-week
lists — the numbers `06_CHECKLISTS.md` M1 requires "pasted", not merely asserted green.

**Elapsed effort against the 1.5 d Charter section 5 budget.** Sum each plan's measured
duration from `.planning/STATE.md`'s Performance Metrics table, using the same 8-hour-day
convention the M0 entry established: 1.5 d is 720 minutes and the stop-and-ADR tripwire is
2× that, 1440 minutes. State the total, the verdict, and — if the total exceeds 1440 minutes
— stop and write the ADR before closing the milestone rather than absorbing the overage
silently (Charter §5 standing rule 5; CONTEXT.md D-03 flagged this risk explicitly when it
accepted the property-based tests as added scope).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_line_endings.py -x && grep -c "M1" docs/BUILD_LOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/BUILD_LOG.md` contains a new M1 section with all six subsections named above.
    - All eleven interpretations are present, each naming the plan that made it.
    - The effort paragraph states the numeric total in minutes, the 720-minute budget, the 1440-minute tripwire, and an explicit verdict sentence.
    - The gate-evidence subsection contains the SIM-070 hash pair and the SIM-072 numbers for all three scenarios.
    - No pre-existing BUILD_LOG line was modified (`git diff docs/BUILD_LOG.md` shows additions only).
    - `uv run pytest tests/unit/test_line_endings.py -x` exits 0.
  </acceptance_criteria>
  <done>`docs/BUILD_LOG.md` carries a complete, append-only M1 entry with every interpretation, the authoring rationale, the pasted gate evidence and an explicit effort verdict.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: M1 milestone sign-off</name>
  <files>(none — verification gate)</files>
  <read_first>
    - `docs/EXECUTION_BLUEPRINT/06_CHECKLISTS.md` — the `[STD]` merge-readiness block (lines 11–21) and the M1 block (lines 59–90), which together are the checklist to paste into the PR and tick with evidence.
    - `docs/BUILD_LOG.md` — the M1 entry written in Task 2.
    - `.planning/phases/02-ground-truth-simulator/02-09-SUMMARY.md` — the gate table, hashes and coverage figure.
  </read_first>
  <what-built>
    The full Layer P simulator: `src/ambo/simulate/` with `config.py`, `spend_patterns.py`,
    `dgp.py`, `platform_bias.py`, `truth.py` and `__main__.py`; three authored scenario YAMLs;
    six new test modules; two real `Makefile` targets; nine committed data artifacts; ADR-007;
    and an M1 `docs/BUILD_LOG.md` entry. `make simulate && make validate-sim` is green with a
    seven-row gate table, and the committed artifacts survive a git checkout round-trip.
  </what-built>
  <how-to-verify>
    1. `make lint && make test` — both green; paste the output.
    2. `make simulate && make validate-sim` — green; paste the seven-row gate table.
    3. Confirm SIM-070's two hashes in the table are equal.
    4. Open `data/synthetic/s_c/truth.json` and confirm `display_video`'s `true_avg_roas`,
       `total_contribution_eur` and `contribution_share` are all `0.0` — the zero-effect
       channel's truth is an exact zero, which is what Phase 5's VR-304 gate will grade against.
    5. Open `data/synthetic/s_a/media_weekly.csv` and confirm the header is exactly
       `week_start,channel,spend_eur,impressions,platform_conversions,platform_revenue_eur`
       and that `print_regional` rows end in three empty fields.
    6. Read the `docs/BUILD_LOG.md` M1 entry's effort paragraph and confirm the total is under
       1440 minutes. If it is not, the milestone does not close here — an ADR analysing the
       overage comes first (Charter §5 standing rule 5).
    7. Paste the `[STD]` and M1 checklist blocks from `06_CHECKLISTS.md` into the milestone PR
       and tick each row with an evidence link. Unticked means not done.
    8. Confirm coverage of `src/ambo/` is at or above 80% and that all six CI jobs are green on
       the PR head commit.
    Approving means M1 is closed and Phase 3 may begin. Rejecting means naming the failing
    rows so a gap-closure plan can be written.
  </how-to-verify>
  <action>
Assemble and present the evidence bundle: the `make lint`, `make test` and `make validate-sim`
outputs, the SIM-070 hash pair, the coverage figure, the CI run link, the effort total against
720/1440 minutes, and the filled-in `[STD]` plus M1 checklist blocks. Do not tick a checklist
row without a concrete evidence link. Wait for the human's explicit response.
  </action>
  <verify>
    <automated>make lint && make test && make simulate && make validate-sim</automated>
  </verify>
  <acceptance_criteria>
    - `make lint`, `make test`, `make simulate` and `make validate-sim` all exit 0, with their outputs captured.
    - Every `[STD]` and M1 checklist row is ticked with a concrete evidence link, or explicitly named as failing.
    - The effort total is stated and compared against 720 and 1440 minutes.
    - `grep -rn "TODO\|FIXME\|XXX" src/ tests/ scripts/` returns no matches (the `[STD]` row).
    - An explicit human approval or rejection string is recorded in the plan SUMMARY.
  </acceptance_criteria>
  <resume-signal>Type "approved" to close M1, or name the failing checklist rows.</resume-signal>
  <done>A human has reviewed the gate table, the committed truth files, the effort tally and the full M1 checklist, and has explicitly approved or rejected the milestone close.</done>
</task>

</tasks>

## Artifacts this phase produces

Full phase-wide symbol list (all ten plans). This plan commits the data and closes M1.

| Owner | Symbols / files |
|-------|-----------------|
| `src/ambo/common/errors.py` (02-02, extended) | `class SimulationError(AmboError)` |
| `src/ambo/simulate/config.py` (02-02) | `SPEC_CHANNEL_ORDER`, `TrueParams`, `SpendPattern`, `PlatformBiasParams`, `ChannelConfig`, `SeasonWeights`, `ScenarioConfig`, `load_scenario(name)` |
| `config/scenarios/{s_a,s_b,s_c}.yaml` (02-03) | authoritative SPEC-01 parameter set per scenario |
| `scripts/author_scenario_schedules.py` (02-03) | dev-only placement aid, `main(argv=None) -> int`, flag `--scenario` |
| `src/ambo/simulate/dgp.py` (02-04, 02-06) | `SEED_PATH`, `round_half_up`, `week_index`, `season_index`, `baseline_demand`, `adstock_recursive`, `hill`, `SimulationResult`, `assemble_scenario`, `decomposition_audit`, `plausibility_audit`, `peak_week_audit` |
| `src/ambo/simulate/spend_patterns.py` (02-05) | `generate_spend(cfg, rng, weeks)` |
| `src/ambo/simulate/platform_bias.py` (02-07) | `platform_report(result)` |
| `src/ambo/simulate/truth.py` (02-08) | `ChannelTruth`, `TruthFile`, `response_curve_at`, `marginal_roas_at`, `compute_truth`, `write_truth` |
| `src/ambo/simulate/__main__.py` (02-09) | `main(argv=None)`, `validate_sim(outdir)`; CLI `python -m ambo.simulate {all\|s_a\|s_b\|s_c\|validate} [--outdir PATH]` |
| `docs/MODULE_CONTRACTS.md` | one level-3 entry per `src/ambo/` module above (contract-first, D-23) |
| `docs/ADR/ADR-007_hypothesis-dev-dependency.md` (02-01) | EB-030 ADR for the new dev dependency |
| `Makefile` (02-09) | real `simulate` and `validate-sim` targets |
| **`data/synthetic/{s_a,s_b,s_c}/` (this plan)** | `media_weekly.csv`, `outcome_weekly.csv`, `truth.json` — nine committed artifacts |
| **`docs/BUILD_LOG.md` (this plan)** | the M1 entry: scope, eleven interpretations, authoring rationale, governance, gate evidence, effort verdict |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working tree → git history | Nine generated artifacts become permanent, reviewable facts; EB-082 forbids rewriting history, so what lands here is what a reviewer will see. |
| git object store → working tree (checkout filters) | The round-trip boundary where a line-ending filter could silently break a byte-identity gate. |
| phase work → milestone claim | The human checkpoint is where "the gates are green" becomes an asserted milestone rather than a local observation. |

Explicitly **not applicable**: **V2 Authentication**, **V3 Session Management**,
**V4 Access Control**, **V6 Cryptography** — no user-facing surface, no principal, no secret,
no crypto in this phase. **Network** — EB-070 forbids `requests` in `src/ambo/`; nothing in
this plan performs network I/O.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-43 | Tampering | checkout filters rewriting committed artifact bytes | high | mitigate | The round-trip check deletes the working copies, restores from git and re-runs `make validate-sim`; a red SIM-070 there is fixed in `.gitattributes`, never by regenerating the files or relaxing the gate. Plan 01-07 hit this exact failure mode with `core.autocrlf=true`. |
| T-02-44 | Repudiation | undocumented interpretations | high | mitigate | Eleven named interpretations are recorded in the append-only `docs/BUILD_LOG.md` M1 entry, each attributed to the plan that made it, per ADR README D-06/D-07's split between interpretations and spec edits. Without this, a reviewer cannot tell a considered reading from an accident. |
| T-02-45 | Tampering | a hand-edited artifact closing the milestone | high | mitigate | Prohibition plus the SIM-070 gate, which byte-compares a fresh regeneration against the committed tree — a hand edit fails the gate. The human checkpoint additionally inspects `truth.json` and the CSV header directly. |
| T-02-46 | Repudiation | effort-budget tripwire crossed silently | medium | mitigate | The BUILD_LOG entry states the numeric total against 720 and 1440 minutes with an explicit verdict, and the checkpoint's step 6 makes the human confirm it before approving; crossing 1440 minutes requires an ADR before the milestone closes (Charter §5 standing rule 5). |
| T-02-47 | Information Disclosure | committing generated data | low | accept | All nine artifacts are disclosed synthetic GROUND-TRUTH and are on EB-081's committed-by-design list; disclosure is the design intent. No real or private data exists in this phase. |
</threat_model>

<verification>
- `make lint`, `make test`, `make simulate` and `make validate-sim` all exit 0.
- `git ls-files data/synthetic` lists ten entries.
- After `git checkout -- data/synthetic/`, `make validate-sim` is still green.
- `docs/BUILD_LOG.md` has an append-only M1 entry with all six subsections.
- A human has explicitly approved or rejected the M1 close.
</verification>

<success_criteria>
- The nine Layer P artifacts are committed and provably regenerable byte-for-byte, including across a git checkout round-trip.
- Every interpretation this phase made is written down where a reviewer will find it.
- The effort total is measured, stated and compared against the Charter §5 budget and tripwire.
- The M1 checklist is ticked with evidence and signed off by a human, unblocking Phase 3.
</success_criteria>

<output>
Create `.planning/phases/02-ground-truth-simulator/02-10-SUMMARY.md` when done, including the
final gate table, the round-trip result, the effort total with its verdict, the human
approval string, and the list of any checklist rows that were named as failing.
</output>
