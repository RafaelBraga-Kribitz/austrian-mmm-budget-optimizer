---
phase: 02-ground-truth-simulator
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/ADR/ADR-007_hypothesis-dev-dependency.md
  - docs/ADR/README.md
  - pyproject.toml
  - uv.lock
  - .gitignore
autonomous: false
requirements: [REQ-q1-truth-recovery]

must_haves:
  truths:
    - "An ADR file exists at docs/ADR/ADR-007_hypothesis-dev-dependency.md with Status Ratified, and docs/ADR/README.md's Existing ADRs table lists it — EB-030 (any new dependency requires an ADR, including small ones) is satisfied before the dependency is installed."
    - "`uv run python -c \"import hypothesis\"` exits 0 — the dev dependency is installed and importable."
    - ".gitignore contains a `.hypothesis/` entry, so the Hypothesis example database is never committed (02-RESEARCH.md Pitfall 3)."
    - "The package-legitimacy SUS verdict for `hypothesis` was surfaced to a human and explicitly approved before `uv add` ran — the verdict was never waved off on the researcher's confidence alone."
  artifacts:
    - path: "docs/ADR/ADR-007_hypothesis-dev-dependency.md"
      provides: "EB-030 ADR for the new hypothesis dev dependency"
      contains: "## Spec deviations"
    - path: "pyproject.toml"
      provides: "hypothesis in [dependency-groups] dev"
      contains: "hypothesis"
    - path: ".gitignore"
      provides: "Hypothesis example-database exclusion"
      contains: ".hypothesis/"
  key_links:
    - from: "docs/ADR/ADR-007_hypothesis-dev-dependency.md"
      to: "pyproject.toml"
      via: "the ADR names the exact dependency-group entry it authorises"
      pattern: "dependency-groups|dev"
    - from: "docs/ADR/README.md"
      to: "docs/ADR/ADR-007_hypothesis-dev-dependency.md"
      via: "Existing ADRs table row linking the new ADR"
      pattern: "ADR-007"
---

<objective>
Execute CONTEXT.md's D-03 decision (add `hypothesis` for property-based tests of the
simulator's adstock/Hill implementations) through this project's own governance path
before any test imports it.

Purpose: EB-030 makes **any** new dependency an ADR event, "including small ones" —
Phase 1 already honoured this by choosing a mypy override over adding `types-PyYAML`
specifically to avoid triggering it (STATE.md, plan 01-04). Separately, this project's
own automated legitimacy checker returned **SUS** on `hypothesis` (reasons: too-new /
unknown-downloads / no-repository). 02-RESEARCH.md assesses those signals as a checker
data artifact — `pip index versions hypothesis` shows an unbroken release history back
to 2013 — but the protocol requires the human gate regardless of that assessment.

Output: a ratified ADR-007, a human-approved legitimacy verdict, `hypothesis` in the
dev dependency group with `uv.lock` updated, and `.hypothesis/` gitignored.

This plan is wave 1 and is a hard prerequisite of plan 02-05, which is the first plan
that imports `hypothesis`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ground-truth-simulator/02-CONTEXT.md
@.planning/phases/02-ground-truth-simulator/02-RESEARCH.md
@docs/ADR/TEMPLATE.md
@docs/ADR/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write ADR-007 for the hypothesis dev dependency and index it</name>
  <files>docs/ADR/ADR-007_hypothesis-dev-dependency.md, docs/ADR/README.md</files>
  <read_first>
    - `docs/ADR/TEMPLATE.md` — the exact five-heading shape to copy (`## Context`, `## Decision`, `## Consequences`, `## Spec deviations` + the front matter bullet list `Status/Date/Deciders/Supersedes/Related`).
    - `docs/ADR/README.md` — the naming convention, the standing rule (D-06), the `## Existing ADRs` table shape, the reserved-slot table (ADR-001…005 are reserved for named topics and must NOT be consumed by this decision), and the "Standard (non-slot) triggers" list which names "a new dependency" explicitly.
    - `docs/ADR/ADR-006_module-contracts-layout-and-citation-policy.md` — the in-repo example of a ratified, non-slot ADR; match its tone and level of detail.
    - `.planning/phases/02-ground-truth-simulator/02-RESEARCH.md` — sections "Package Legitimacy Audit", "Common Pitfalls" #2 and #3, "Open Questions" #2 (whose recommendation — treat this as a new ordinary ADR at the next available number — is the one to follow).
    - `.planning/phases/02-ground-truth-simulator/02-CONTEXT.md` — decision D-03 (the user's accepted scope and accepted effort-budget risk).
  </read_first>
  <action>
Create `docs/ADR/ADR-007_hypothesis-dev-dependency.md` by copying `docs/ADR/TEMPLATE.md`
and filling it. ADR-007 is the next available number: ADR-000 and ADR-006 are taken,
and ADR-001 through ADR-005 are GB-202 reserved slots for named topics (permission/sector,
channel mapping, anonymization recipe, Layer R window, reparameterization ladder) — do
not consume a reserved slot.

Front matter: `Status: Ratified`, `Date:` today, `Deciders: —`, `Supersedes: —`,
`Related: EB-030, GB-202, SPEC-08 §3, SIM-074, WBS T-104, CONTEXT.md D-03`.

`## Context`: EB-030 requires an ADR for any new dependency including small ones;
Phase 1 (plan 01-04) already applied that rule by preferring a mypy `ignore_missing_imports`
override for `yaml.*` over adding `types-PyYAML`. Phase 2's D-03 adds property-based tests
for `simulate/dgp.py`'s `adstock_recursive` and `hill`, which SIM-074 covers only at fixed
points. `hypothesis` is absent from both SPEC-08 §3's dependency table and `pyproject.toml`'s
`[dependency-groups] dev` list. State the question: may a dev-only test dependency be added,
and on what evidence.

`## Decision`: add `hypothesis` to `[dependency-groups] dev` in `pyproject.toml` (dev group
only — never `[project] dependencies`, so it never ships in the runtime dependency closure
and never affects the O-3 forbidden-deps surface). Record the resolved version from
`uv.lock` in the ADR text. State that the shrinking/example-minimisation behaviour is the
reason a hand-rolled randomized loop was rejected (02-RESEARCH.md "Don't Hand-Roll").

`## Consequences`: property-based invariants for adstock/Hill become expressible and their
counterexamples become minimal and actionable; the dev install surface grows by one package
and its transitive closure; `.hypothesis/` must be gitignored (Task 3); the phase's 1.5-day
(720-minute) Charter §5 budget absorbs this governance step plus the property tests, with
the 2× stop-and-ADR tripwire at 1440 minutes tracked in `docs/BUILD_LOG.md` at M1 close.
Record the automated legitimacy verdict verbatim — verdict `SUS`, reasons `too-new`,
`unknown-downloads`, `no-repository` — alongside the counter-evidence (`pip index versions
hypothesis` shows releases from `0.0.1` in 2013 through `6.165.1`; the real source repository
is `github.com/HypothesisWorks/hypothesis`) and state that the verdict was resolved by the
blocking human checkpoint in Task 2, not by the researcher's assessment.

`## Spec deviations`: one table row — Document `docs/SPEC-08_engineering.md`, Section `§3`,
Change "adds `hypothesis` to the dev dependency group; the SPEC-08 §3 table is extended by
this ADR rather than edited in place", REQ IDs `REQ-dl8-quality, REQ-q1-truth-recovery`.

Then edit `docs/ADR/README.md`: append one row to the `## Existing ADRs` table linking
`ADR-007` with title "Adding `hypothesis` as a dev dependency for property-based tests",
Status `**Ratified**`, and a one-line Decisions summary. Do not touch the reserved-slot
table. Do not renumber or edit any existing row (ADRs are append-only).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_repo_layout.py tests/unit/test_line_endings.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `docs/ADR/ADR-007_hypothesis-dev-dependency.md` exists and contains the four headings `## Context`, `## Decision`, `## Consequences`, `## Spec deviations`.
    - The file's first heading line is exactly `# ADR-007 — Adding \`hypothesis\` as a dev dependency for property-based tests` (em dash, matching ADR-000/ADR-006).
    - `grep -c 'ADR-007' docs/ADR/README.md` returns at least 1, and the new row sits in the `## Existing ADRs` table, not the reserved-slot table.
    - `grep -c 'ADR-00[1-5]' docs/ADR/README.md` is unchanged from before this task (no reserved slot consumed).
    - The `## Spec deviations` table has exactly one populated data row (not the `— | — | — | —` placeholder).
    - The ADR text contains the literal verdict word `SUS` and the three reason tokens `too-new`, `unknown-downloads`, `no-repository`.
    - `uv run pytest tests/unit/test_line_endings.py -x` exits 0 (LF endings on the new file).
  </acceptance_criteria>
  <done>ADR-007 is written, ratified, indexed in the README's Existing ADRs table, and no GB-202 reserved slot was consumed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Human approval of the `hypothesis` package-legitimacy SUS verdict</name>
  <files>(none — approval gate)</files>
  <read_first>
    - `.planning/phases/02-ground-truth-simulator/02-RESEARCH.md` — the `## Package Legitimacy Audit` table and Assumptions Log row A1.
    - `docs/ADR/ADR-007_hypothesis-dev-dependency.md` — the ADR written in Task 1, which records the verdict verbatim.
  </read_first>
  <what-built>
    ADR-007 is written and ratified (Task 1). Nothing has been installed yet. This gate
    exists because the project's own automated checker returned a **SUS** verdict for
    `hypothesis`, and the package-legitimacy protocol makes a SUS verdict non-auto-approvable
    regardless of how confident the research is that it is a false positive. `workflow.auto_advance`
    does not apply to this checkpoint.
  </what-built>
  <how-to-verify>
    1. Re-run the checker and read its output:
       `gsd-tools query package-legitimacy check --ecosystem pypi hypothesis`
       Expected: verdict `SUS`, reasons `too-new` / `unknown-downloads` / `no-repository`.
    2. Re-run the registry cross-check: `pip index versions hypothesis`
       Expected: a version list whose history contradicts "too-new" (releases from 2013).
    3. Open `https://pypi.org/project/hypothesis/` in a browser and confirm all four:
       - the project page exists and the maintainer is HypothesisWorks / David R. MacIver;
       - the "Homepage"/"Source" link points at `https://github.com/HypothesisWorks/hypothesis`;
       - the release history starts in 2013, not in the last 90 days;
       - the latest version matches what `pip index versions hypothesis` reported.
    4. Confirm the intended install is dev-group-only: `uv add --group dev hypothesis`
       (never `[project] dependencies`).
    Approving means: you accept `hypothesis` as a legitimate package and authorise Task 3
    to install it. Rejecting means: Task 3 does not run, plan 02-05's property-based tests
    are dropped, and SIM-074's fixed-point tests alone cover the adstock/Hill invariants
    (02-RESEARCH.md "Environment Availability" documents this as the fallback — the phase's
    own SIM-070…075 exit gates do not require `hypothesis`).
  </how-to-verify>
  <action>
Pause and present the checker output, the `pip index versions` output, and the four
`pypi.org/project/hypothesis` facts above to the human. Do not run `uv add` before an
explicit approval. If the human rejects, record the rejection in the plan SUMMARY, mark
`hypothesis` as not-installed, and note that plan 02-05 Task 3 must be dropped.
  </action>
  <verify>
    <automated>gsd-tools query package-legitimacy check --ecosystem pypi hypothesis</automated>
  </verify>
  <acceptance_criteria>
    - The checker output and the `pip index versions hypothesis` output are both pasted into the plan SUMMARY verbatim.
    - An explicit human approval or rejection string is recorded in the plan SUMMARY.
    - No `uv add` / `uv sync` command was executed before the approval string was recorded (verifiable from the commit order: no `uv.lock` change exists in any commit preceding the approval).
  </acceptance_criteria>
  <resume-signal>Type "approved" to authorise the install, or "rejected" plus a reason to drop the property-based tests.</resume-signal>
  <done>The SUS verdict has been surfaced with its counter-evidence and a human has explicitly approved or rejected the install.</done>
</task>

<task type="auto">
  <name>Task 3: Install hypothesis into the dev group and gitignore its example database</name>
  <files>pyproject.toml, uv.lock, .gitignore</files>
  <read_first>
    - `pyproject.toml` — the existing `[dependency-groups] dev` list (`pytest>=8`, `pytest-cov`, `ruff`, `mypy`, `pre-commit`, `nbstripout`) and the `[tool.mypy]` block (`files = ["src/ambo"]`, so a dev test dependency needs no override).
    - `.gitignore` — its section structure and the trailing `COMMITTED BY DESIGN` comment block; the new entry must go in the `# --- Environment / tooling ---` section next to `.pytest_cache/`, never below the committed-by-design block.
    - `tests/unit/test_gitignore.py` — the Phase 1 guard that asserts the EB-081 entries are present; confirm the new entry does not violate any assertion in it before committing.
    - `docs/ADR/ADR-007_hypothesis-dev-dependency.md` — the authorisation this task executes.
  </read_first>
  <action>
Run `uv add --group dev hypothesis`. This edits `[dependency-groups] dev` in `pyproject.toml`
and regenerates `uv.lock`. Do not hand-edit either file; do not add `hypothesis` to
`[project] dependencies`.

Add `.hypothesis/` to `.gitignore` in the `# --- Environment / tooling ---` section,
immediately after the `.pytest_cache/` line, with a one-line comment stating it is
Hypothesis's local example database (a non-deterministic per-machine cache) and citing
ADR-007. The entry is a directory pattern with a trailing slash.

Back-fill the resolved version into ADR-007's `## Decision` section: read the exact
`hypothesis` version `uv.lock` resolved and write it into the ADR sentence that names the
dependency. ADRs are append-only in the sense that a *ratified decision* is never reversed
in place; completing a placeholder inside the same plan that authored the file is not a
reversal — state the resolved version, do not add an amendment heading.

Then confirm the toolchain is still clean: `make lint` and `make test` must both be green,
and `uv run python -c "import hypothesis"` must exit 0.
  </action>
  <verify>
    <automated>uv run python -c "import hypothesis; print(hypothesis.__version__)" && uv run pytest tests/unit/test_gitignore.py tests/unit/test_forbidden_deps.py tests/unit/test_no_requests.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python -c "import hypothesis; print(hypothesis.__version__)"` exits 0 and prints a `6.x` version string.
    - `grep -c '^hypothesis' pyproject.toml` is 0 and `grep -c '"hypothesis' pyproject.toml` is at least 1 — i.e. the entry landed inside the `[dependency-groups] dev` array, not as a bare top-level key.
    - The `[project] dependencies` array in `pyproject.toml` is byte-unchanged from before this task (`git diff` shows no line inside it).
    - `grep -c '^\.hypothesis/$' .gitignore` returns exactly 1.
    - `uv run pytest tests/unit/test_gitignore.py -x` exits 0.
    - `make lint` exits 0 and `make test` exits 0.
    - `uv.lock` is modified in the same commit as `pyproject.toml`.
  </acceptance_criteria>
  <done>`hypothesis` is a resolvable dev dependency, `uv.lock` is updated, `.hypothesis/` is gitignored, and the full Phase 1 test suite is still green.</done>
</task>

</tasks>

## Artifacts this phase produces

Full phase-wide symbol list (all ten plans). This plan creates only the last group.

| Owner | Symbols / files |
|-------|-----------------|
| `src/ambo/common/errors.py` (02-02, extended) | `class SimulationError(AmboError)` |
| `src/ambo/simulate/config.py` (02-02) | `TrueParams`, `SpendPattern`, `PlatformBiasParams`, `ChannelConfig`, `ScenarioConfig`, `SPEC_CHANNEL_ORDER`, `load_scenario(name)` |
| `config/scenarios/{s_a,s_b,s_c}.yaml` (02-03) | authored scenario parameter files (SIM-002 authoritative) |
| `scripts/author_scenario_schedules.py` (02-03) | `main(argv=None) -> int` — dev-only authoring aid (D-02) |
| `src/ambo/simulate/spend_patterns.py` (02-04) | `generate_spend(cfg, rng)` |
| `src/ambo/simulate/dgp.py` (02-05, 02-06) | `week_index`, `season_index`, `baseline_demand`, `adstock_recursive`, `hill`, `assemble_scenario`, `SimulationResult`, `decomposition_audit`, `plausibility_audit`, `peak_week_audit` |
| `src/ambo/simulate/platform_bias.py` (02-07) | `platform_report(result, cfg)` |
| `src/ambo/simulate/truth.py` (02-08) | `ChannelTruth`, `TruthFile`, `response_curve_at`, `marginal_roas_at`, `compute_truth`, `write_truth` |
| `src/ambo/simulate/__main__.py` (02-09) | `main(argv=None)`, `validate_sim(outdir)`; CLI positionals `all\|s_a\|s_b\|s_c\|validate`, flag `--outdir PATH` |
| `docs/MODULE_CONTRACTS.md` | one new level-3 entry per module above (contract-first, D-23) |
| `Makefile` (02-09) | `simulate` and `validate-sim` targets replacing the Phase 1 stubs |
| `data/synthetic/{s_a,s_b,s_c}/` (02-10) | `media_weekly.csv`, `outcome_weekly.csv`, `truth.json` |
| **This plan (02-01)** | `docs/ADR/ADR-007_hypothesis-dev-dependency.md`; `docs/ADR/README.md` Existing-ADRs row; `hypothesis` in `[dependency-groups] dev`; `.hypothesis/` in `.gitignore` |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI → developer machine / CI | The only boundary this plan crosses. `uv add` resolves and installs third-party code that then runs with full local privileges during `pytest`. |

Explicitly **not applicable** in this phase, and why: **V2 Authentication**, **V3 Session
Management**, **V4 Access Control** — this phase ships no user-facing surface, no server,
no session, no principal. **V6 Cryptography** — no secrets and no crypto operations
(the anonymization crypto-adjacent concerns belong to Phase 6). **Network I/O** — EB-070
forbids `requests` anywhere in `src/ambo/`, enforced by `tests/unit/test_no_requests.py`;
`uv` is the only process that touches the network and it does so outside `src/`.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-SC | Tampering | `uv add --group dev hypothesis` (supply chain) | high | mitigate | The automated legitimacy verdict is **SUS**; Task 2 is a blocking, non-auto-approvable human checkpoint that must confirm the PyPI project identity, maintainer, source repository and release history before Task 3 installs anything. `uv.lock` pins the exact resolved version and hashes thereafter. |
| T-02-01 | Tampering | `pyproject.toml` `[project] dependencies` | medium | mitigate | The install is dev-group-only; an acceptance criterion asserts the runtime `[project] dependencies` array is byte-unchanged, so the O-3 forbidden-deps surface (`tests/unit/test_forbidden_deps.py`) cannot silently widen. |
| T-02-02 | Information Disclosure | `.hypothesis/` example database | low | mitigate | Hypothesis serialises failing examples to a local directory; the `.gitignore` entry keeps that machine-local state out of history. Contents are synthetic numeric inputs only — no real or private data exists anywhere in this phase. |
| T-02-03 | Repudiation | ADR trail for a dependency change | low | mitigate | EB-030 ADR-007 records the decision, the verdict, and the counter-evidence in git history, so the dependency addition is attributable rather than incidental. |
</threat_model>

<verification>
- `make lint` exits 0.
- `make test` exits 0 (full Phase 1 suite, unchanged behaviour).
- `uv run python -c "import hypothesis"` exits 0.
- `git log --oneline` shows the ADR commit strictly before the `uv.lock` commit.
</verification>

<success_criteria>
- ADR-007 exists, is Ratified, is indexed in `docs/ADR/README.md`, and consumed no GB-202 reserved slot.
- The SUS legitimacy verdict was surfaced to a human with counter-evidence and explicitly approved (or explicitly rejected, with the fallback recorded).
- `hypothesis` is installed into the dev group only, `uv.lock` is regenerated, and `.hypothesis/` is gitignored.
- Plan 02-05 is unblocked for its property-based tests.
</success_criteria>

<output>
Create `.planning/phases/02-ground-truth-simulator/02-01-SUMMARY.md` when done, including
the verbatim checker output, the human approval/rejection string, and the resolved
`hypothesis` version.
</output>
