---
phase: 02-ground-truth-simulator
plan: 07
type: execute
wave: 6
depends_on: ["02-06"]
files_modified:
  - src/ambo/simulate/platform_bias.py
  - docs/MODULE_CONTRACTS.md
  - tests/unit/test_platform_bias.py
autonomous: true
requirements: [REQ-q1-truth-recovery]

must_haves:
  truths:
    - "`platform_revenue_{c,t} = m_{c,t} * φ_c + θ_c * base_t * share_{c,t}` reproduces a hand-computed example to within 1e-9 (SIM-060)."
    - "`share_{c,t}` is the channel's share of total spend in that week, and a week whose total spend across all six channels is 0 yields `share = 0` for every channel — never a division-by-zero NaN or infinity."
    - "`print_regional` and `radio` carry NULL (empty) `impressions`, `platform_conversions` and `platform_revenue_eur` in every week: offline channels have no platform reporting at all, and NULL is distinguishable from 0 in the emitted CSV."
    - "`impressions = round_half_up(spend_eur / cpm * 1000)` and `platform_conversions = round_half_up(platform_revenue_eur / AOV_t)` for online channels only (BP-D-02), using the same single rounding convention as spend and orders."
    - "The φ and θ values used are read from `ScenarioConfig`, never restated in code, so the SPEC-01 §6 table has exactly one home (the scenario YAML) and the BP-G-02 test already guards it."
    - "The known over-credit ordering is present in the generated data: on S-A and S-B the ratio of platform ROAS to true ROAS orders `display_video > meta > search_generic > search_brand`, matching the φ ordering 2.0 > 1.5 > 1.3 > 1.1 (SIM-061; this is what Phase 8's DC-702 gate must later recover)."
    - "`platform_report` is pure: it derives every value from the `SimulationResult` it is handed and performs no I/O."
    - "The returned frame has exactly `6 * cfg.weeks` rows and exactly SIM-004's six columns in order, with row order unchanged from the input `media` frame."
  artifacts:
    - path: "src/ambo/simulate/platform_bias.py"
      provides: "SIM-060 platform-reporting bias and BP-D-02 impressions/conversions"
      exports: ["platform_report"]
    - path: "tests/unit/test_platform_bias.py"
      provides: "SIM-060/061 formula, NULL-handling and hand-example tests"
      min_lines: 140
    - path: "docs/MODULE_CONTRACTS.md"
      provides: "contract-first entry for simulate/platform_bias.py (D-23)"
      contains: "### src/ambo/simulate/platform_bias.py"
  key_links:
    - from: "src/ambo/simulate/platform_bias.py"
      to: "src/ambo/simulate/dgp.py"
      via: "consumes SimulationResult.components (m_c, base) and imports round_half_up"
      pattern: "round_half_up"
    - from: "src/ambo/simulate/platform_bias.py"
      to: "src/ambo/simulate/config.py"
      via: "reads PlatformBiasParams phi/theta/cpm per channel from the scenario config"
      pattern: "platform"
---

<objective>
Build T-106: simulated platform reporting with the known over-credit of SPEC-01 §6, plus
BP-D-02's impressions and conversions, completing the three currently-null columns of
`media_weekly.csv`.

Purpose: SIM-061 makes "platform ROAS versus true ROAS" a known quantity in Layer P. Phase 8's
attribution-gap module must recover the direction and rough magnitude of the φ distortion,
and it can only be graded against a gap that was deliberately and disclosedly injected here.

Output: `src/ambo/simulate/platform_bias.py`, its `docs/MODULE_CONTRACTS.md` entry, and
`tests/unit/test_platform_bias.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ground-truth-simulator/02-CONTEXT.md
@.planning/phases/02-ground-truth-simulator/02-RESEARCH.md
@.planning/phases/02-ground-truth-simulator/02-PATTERNS.md
@.planning/phases/02-ground-truth-simulator/02-06-SUMMARY.md
@docs/SPEC-01_ground_truth_simulator.md
@src/ambo/simulate/config.py
@src/ambo/simulate/dgp.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: platform_report and its module contract</name>
  <files>src/ambo/simulate/platform_bias.py, docs/MODULE_CONTRACTS.md</files>
  <read_first>
    - `docs/SPEC-01_ground_truth_simulator.md` §6 — SIM-060's formula and the φ/θ table (`search_brand` φ=1.1 θ=0.05; `search_generic` φ=1.3 θ=0.01; `meta` φ=1.5 θ=0.01; `display_video` φ=2.0 θ=0.005; print/radio no platform reporting) and SIM-061's purpose statement.
    - `docs/SPEC-01_ground_truth_simulator.md` §1 SIM-004 — the six `media_weekly.csv` columns and their order.
    - `docs/EXECUTION_BLUEPRINT/02_WBS.md` T-106 (lines 364–379) — objective, the `share_c` zero-total-week rule, the BP-D-02 impressions/conversions rules, validation and the three acceptance criteria.
    - `docs/EXECUTION_BLUEPRINT/00_MASTER_PLAN.md` line 106 (BP-D-02) — `impressions = round(spend/cpm × 1000)`, `platform_conversions = round(platform_revenue / AOV_t)`, offline NULL, parameters recorded in `truth.json`.
    - `docs/EXECUTION_BLUEPRINT/03_MODULES.md` §2.4 (lines 120–122) — the declared `platform_report(result) -> pd.DataFrame` contract.
    - `src/ambo/simulate/dgp.py` — `SimulationResult`'s field names and the `components` column names (`base`, `m_<channel>`), plus `round_half_up`.
    - `src/ambo/simulate/config.py` — `PlatformBiasParams` (`phi`, `theta`, `cpm`) and its all-set-or-all-None invariant.
    - `scripts/generate_season_windows.py` — the pure-function docstring and citation conventions to copy.
  </read_first>
  <behavior>
    - `platform_report(result)` returns a frame with exactly `6 * cfg.weeks` rows and columns `week_start, channel, spend_eur, impressions, platform_conversions, platform_revenue_eur`.
    - For an online channel and a chosen week, `platform_revenue_eur` equals `m_c * phi_c + theta_c * base_t * share_c_t` to within 1e-9 of a value computed by hand in the test.
    - For `print_regional` and `radio`, all three platform columns are NULL in every row.
    - `impressions` equals `round_half_up(spend_eur / cpm * 1000)` for online channels.
    - `platform_conversions` equals `round_half_up(platform_revenue_eur / (aov_base + aov_advent_bonus * advent_flag))` for online channels.
    - On a synthetic result whose every channel's spend is 0 in some week, every `share_c` for that week is 0.0 and no output value is NaN or infinite.
    - The row order of the returned frame is identical to the input `result.media` row order.
  </behavior>
  <action>
Create `src/ambo/simulate/platform_bias.py`. Module docstring naming SPEC-01 §6, SIM-060,
SIM-061 and BP-D-02; an `Implements: REQ-q1-truth-recovery` line; a statement that this module
is pure (derives everything from the handed `SimulationResult`, performs no I/O) and imports
nothing from `ambo.model`; and a sentence recording that the over-credit injected here is the
known quantity Phase 8's DC-702 gate must recover.

`platform_report(result: SimulationResult) -> pd.DataFrame` — signature exactly as
`03_MODULES.md` §2.4 declares it, since `result.cfg` already carries the φ/θ/CPM parameters.

Implementation:

1. Compute `total_spend_t` as the row-wise sum of `result.spend` across all six channels, and
   `share_{c,t} = spend_{c,t} / total_spend_t` where `total_spend_t > 0`, else exactly `0.0`.
   Use an explicit `np.where(total > 0, spend / np.where(total > 0, total, 1.0), 0.0)` shape or
   equivalent so no division by zero is ever evaluated — a `NaN` here would propagate silently
   into `platform_revenue_eur` and then into `truth.json`'s platform ROAS.
2. `aov_t = cfg.aov_base + cfg.aov_advent_bonus * weeks["advent_flag"]`.
3. Per channel in `SPEC_CHANNEL_ORDER`: if `cfg.channels[c].platform.phi is None`, the channel
   is offline — leave all three columns NULL for every week. Otherwise compute
   `platform_revenue = components["m_" + c] * phi + theta * components["base"] * share_c`,
   `impressions = round_half_up(spend_c / cpm * 1000.0)`, and
   `platform_conversions = round_half_up(platform_revenue / aov_t)`.
4. Return a copy of `result.media` with the three columns populated, preserving the existing
   row order (do not re-sort). Column dtypes: `impressions` and `platform_conversions` as
   pandas nullable `Int64`, `platform_revenue_eur` as nullable `Float64`, so an offline row
   carries a true missing value that writes as an empty CSV field rather than as `0`, `nan`
   or `None`.

Raise `SimulationError` if the handed result's `media` frame does not have the expected row
count or column set — this module is a completion step and must not silently reshape its input.

Append the `### src/ambo/simulate/platform_bias.py` entry to `docs/MODULE_CONTRACTS.md` in
the same commit, five labelled sections: `Purpose`, `Public API` with the signature,
`Invariants` (purity; offline channels fully NULL; zero-total-week share is exactly 0;
nullable dtypes so NULL survives to CSV; row order preserved; φ/θ/CPM read from config, never
restated), `Failure modes`, `Testing`.
  </action>
  <verify>
    <automated>uv run mypy && uv run pytest tests/unit/test_repo_layout.py tests/unit/test_import_independence.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run mypy` exits 0 under strict.
    - `uv run pytest tests/unit/test_repo_layout.py -x` exits 0 — `### src/ambo/simulate/platform_bias.py` exists and the module/entry sets are equal.
    - `uv run python -c "import numpy as np; from ambo.simulate.config import load_scenario; from ambo.simulate.dgp import assemble_scenario as A; from ambo.simulate.platform_bias import platform_report as P; r=A(load_scenario('s_a'), np.random.default_rng(101)); m=P(r); assert len(m)==936; assert list(m.columns)==['week_start','channel','spend_eur','impressions','platform_conversions','platform_revenue_eur']; off=m[m['channel'].isin(['print_regional','radio'])]; assert off[['impressions','platform_conversions','platform_revenue_eur']].isna().all().all()"` exits 0.
    - `uv run python -c "import numpy as np; from ambo.simulate.config import load_scenario; from ambo.simulate.dgp import assemble_scenario as A; from ambo.simulate.platform_bias import platform_report as P; r=A(load_scenario('s_b'), np.random.default_rng(202)); m=P(r); on=m[~m['channel'].isin(['print_regional','radio'])]; assert on['platform_revenue_eur'].notna().all(); assert np.isfinite(on['platform_revenue_eur'].astype(float)).all()"` exits 0.
    - The module contains no literal φ, θ or CPM number — every one is read from `cfg.channels[c].platform`.
    - `make lint` exits 0.
  </acceptance_criteria>
  <done>`platform_report` completes the three SIM-004 platform columns for online channels, leaves offline channels genuinely NULL, handles the zero-total-spend week without producing a NaN, and its contract entry landed in the same commit.</done>
</task>

<task type="auto">
  <name>Task 2: SIM-060/061 formula, NULL-handling and over-credit ordering tests</name>
  <files>tests/unit/test_platform_bias.py</files>
  <read_first>
    - `tests/unit/test_config.py` (all 162 lines) — the assertion and typed-error conventions to replicate.
    - `tests/conftest.py` — `repo_root` and `seeded_rng`.
    - `src/ambo/simulate/platform_bias.py` — the module under test.
    - `docs/SPEC-01_ground_truth_simulator.md` §6 — the φ/θ values the hand-computed example uses.
    - `docs/EXECUTION_BLUEPRINT/02_WBS.md` T-106 validation notes — "identity re-derivation on a small hand-computed example" and "platform ROAS ordering by construction (φ ordering) present in generated data".
    - `tests/unit/test_import_independence.py` lines 55–64 — the non-vacuous-scan assertion style.
  </read_first>
  <action>
Create `tests/unit/test_platform_bias.py`. Module docstring naming SIM-060, SIM-061, BP-D-02
and `REQ-q1-truth-recovery`.

Tests:

- `test_hand_computed_example_matches_to_1e_9` — pick one scenario, one online channel and one concrete week index. In the test, read `m_c`, `base_t` and the six spend values for that week directly off the assembled result, compute `share_c`, then compute `m_c * phi + theta * base_t * share_c` **in the test with literal arithmetic**, and assert the module's output matches within 1e-9. Re-deriving the formula in the test rather than calling the module twice is what makes this a real identity check (the `test_config.py` "trust nothing" pattern).
- `test_offline_channels_are_null_everywhere` — parameterized over all three scenarios: `print_regional` and `radio` rows have all three platform columns missing; assert a non-zero offline row count first so the test cannot pass vacuously.
- `test_online_channels_are_never_null_or_non_finite` — parameterized over all three scenarios.
- `test_impressions_follow_the_cpm_rule` — `impressions == round_half_up(spend_eur / cpm * 1000)` elementwise for each online channel.
- `test_platform_conversions_follow_the_aov_rule` — `platform_conversions == round_half_up(platform_revenue_eur / (aov_base + aov_advent_bonus * advent_flag))` elementwise for each online channel.
- `test_zero_total_spend_week_gives_zero_share_and_no_nan` — build a `SimulationResult` copy whose `spend` frame is zeroed for one week (and whose components are adjusted so the SIM-071 constructor invariant still holds), run `platform_report`, and assert every online `platform_revenue_eur` for that week equals `theta * base_t * 0.0` exactly — that is, the demand-claiming term vanishes rather than producing NaN or infinity.
- `test_phi_theta_come_from_config_not_code` — monkeypatch a loaded config's `display_video.platform.phi` to 3.0, re-run `platform_report`, and assert the resulting `platform_revenue_eur` for that channel scales accordingly. This proves the formula reads the config rather than a hard-coded literal.
- `test_over_credit_ordering_matches_phi_ordering` — on S-A and S-B only, compute per online channel `platform_roas = Σ platform_revenue / Σ spend` and `true_roas = Σ m_c / Σ spend`, then assert the ratio `platform_roas / true_roas` orders `display_video > meta > search_generic > search_brand`. Document in the test docstring why S-C is excluded: its `display_video` true ROAS is exactly 0, so the ratio is undefined there by construction, and SIM-061's ordering claim is about the injected over-credit, not about the zero-effect scenario.
- `test_row_order_is_preserved` — the returned frame's `(week_start, channel)` sequence is identical to the input `result.media` sequence.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_platform_bias.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_platform_bias.py -x` exits 0 and collects at least 9 tests.
    - `uv run pytest tests/unit/test_platform_bias.py -x` completes in under 30 seconds.
    - The hand-computed example's expected value is written as literal arithmetic in the test, not obtained by calling `platform_report` a second time.
    - Every scenario-iterating test asserts a non-zero row count before its main assertion.
    - `make test` exits 0 and `make lint` exits 0.
  </acceptance_criteria>
  <done>SIM-060's formula is verified against a hand-computed identity to 1e-9, offline NULL handling is proven for all three scenarios, the zero-total-spend week produces no NaN, and SIM-061's over-credit ordering is present in the generated data.</done>
</task>

</tasks>

## Artifacts this phase produces

Full phase-wide symbol list (all ten plans). This plan creates `platform_bias.py`.

| Owner | Symbols / files |
|-------|-----------------|
| `src/ambo/common/errors.py` (02-02, extended) | `class SimulationError(AmboError)` |
| `src/ambo/simulate/config.py` (02-02) | `SPEC_CHANNEL_ORDER`, `TrueParams`, `SpendPattern`, `PlatformBiasParams`, `ChannelConfig`, `SeasonWeights`, `ScenarioConfig`, `load_scenario(name)` |
| `config/scenarios/{s_a,s_b,s_c}.yaml` (02-03) | authoritative SPEC-01 parameter set per scenario |
| `scripts/author_scenario_schedules.py` (02-03) | dev-only placement aid, `main(argv=None) -> int`, flag `--scenario` |
| `src/ambo/simulate/dgp.py` (02-04, 02-06) | `SEED_PATH`, `round_half_up`, `week_index`, `season_index`, `baseline_demand`, `adstock_recursive`, `hill`, `SimulationResult`, `assemble_scenario`, `decomposition_audit`, `plausibility_audit`, `peak_week_audit` |
| `src/ambo/simulate/spend_patterns.py` (02-05) | `generate_spend(cfg, rng, weeks)` |
| **`src/ambo/simulate/platform_bias.py` (this plan)** | `platform_report(result)` |
| `src/ambo/simulate/truth.py` (02-08) | `ChannelTruth`, `TruthFile`, `response_curve_at`, `marginal_roas_at`, `compute_truth`, `write_truth` |
| `src/ambo/simulate/__main__.py` (02-09) | `main(argv=None)`, `validate_sim(outdir)`; CLI positionals `all\|s_a\|s_b\|s_c\|validate`, flag `--outdir PATH` |
| `docs/MODULE_CONTRACTS.md` | one level-3 entry per `src/ambo/` module above (contract-first, D-23) |
| `docs/ADR/ADR-007_hypothesis-dev-dependency.md` (02-01) | EB-030 ADR for the new dev dependency |
| `Makefile` (02-09) | `simulate` and `validate-sim` targets replacing the Phase 1 stubs |
| `data/synthetic/{s_a,s_b,s_c}/` (02-10) | `media_weekly.csv`, `outcome_weekly.csv`, `truth.json` |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `SimulationResult` → platform-reported columns | Derived values enter the committed `media_weekly.csv` contract that Phase 3's dbt staging layer and Phase 8's attribution-gap module both read. |

Explicitly **not applicable**: **V2 Authentication**, **V3 Session Management**,
**V4 Access Control**, **V6 Cryptography** — no user-facing surface, no principal, no
secret, no crypto in this phase. **Network** — EB-070 forbids `requests` in `src/ambo/`;
this module performs no I/O. Note that despite the name, no real advertising platform is
contacted: `platform_report` simulates a report, it does not fetch one.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-28 | Denial of Service | `share_c` division on a zero-total-spend week (V5 Input Validation) | high | mitigate | The share is computed with an explicit guarded expression that never evaluates a division by zero; a dedicated test builds a zeroed week and asserts no NaN or infinity reaches `platform_revenue_eur`. A NaN here would flow into `truth.json`'s platform ROAS and only surface as an unexplainable Phase 8 gate failure. |
| T-02-29 | Tampering | NULL versus 0 for offline channels | medium | mitigate | Nullable `Int64`/`Float64` dtypes carry a true missing value through to an empty CSV field; a test asserts all three columns are missing for `print_regional` and `radio` on every row. Writing 0 instead of NULL would silently claim that offline channels reported zero revenue rather than not reporting at all. |
| T-02-30 | Tampering | φ/θ restated in code rather than read from config | medium | mitigate | The module contains no φ/θ/CPM literal (acceptance criterion), and a monkeypatch test proves the formula reads the config — so the SPEC-01 §6 table keeps exactly one home, already guarded by BP-G-02 in plan 02-03. |
| T-02-31 | Repudiation | the injected over-credit as Phase 8's grading key | medium | mitigate | `test_over_credit_ordering_matches_phi_ordering` proves the intended ordering is actually present in the generated data, so DC-702's later pass or fail measures the model rather than an absent signal; the S-C exclusion is documented in the test rather than silently skipped. |
</threat_model>

<verification>
- `uv run pytest tests/unit/test_platform_bias.py -x` exits 0 in under 30 seconds.
- `uv run pytest tests/unit/test_repo_layout.py tests/unit/test_import_independence.py -x` exits 0.
- `uv run mypy` exits 0; `make lint` and `make test` both exit 0.
</verification>

<success_criteria>
- `media_weekly.csv`'s three platform columns are populated for the four online channels and genuinely NULL for the two offline ones.
- SIM-060's formula is verified against a hand-computed identity to 1e-9.
- SIM-061's known over-credit ordering is demonstrably present in S-A and S-B.
- No NaN or infinity can reach a committed artifact from a zero-total-spend week.
</success_criteria>

<output>
Create `.planning/phases/02-ground-truth-simulator/02-07-SUMMARY.md` when done. Record the
hand-computed example (scenario, channel, week, inputs, expected value), the realized
over-credit ratios per channel for S-A and S-B, and the elapsed plan duration for the
Charter §5 effort tally.
</output>
