---
phase: 04-mmm-on-s-a
plan: 07
type: execute
wave: 7
depends_on:
  - 04-03
  - 04-04
  - 04-05
  - 04-06
files_modified:
  - src/ambo/model/fit.py
  - Makefile
  - data/posteriors/P-SA.parquet
  - reports/model/diag_P-SA.md
  - reports/model/ppc_P-SA.png
  - docs/BUILD_LOG.md
autonomous: true
requirements:
  - REQ-q1-truth-recovery
  - REQ-dl8-quality
---

<objective>
T-307: `run_fit` CLI + `make fit-synthetic` (P-SA only, D-20). Run the real
MD-050 S-A fit. Commit thinned parquet + diag report. Log wall time. If MD-071
fails, MD-073 ladder in order — ADR-005 past rung 1; two failures ⇒ stop.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: run_fit + make fit-synthetic</name>
  <files>src/ambo/model/fit.py, Makefile, docs/MODULE_CONTRACTS.md</files>
  <action>
`python -m ambo.model.fit --layer P-SA [--variant …]`: print expected runtime
from `max_fit_minutes` first (EB-050); read mart; parse channels_present; scale;
build; sample_model from Settings.sampler; save_posterior; write_diag_report.
Variants parsed/validated now (`flat|nopromo|loco-&lt;ch&gt;|holdout`); default
path is the only one fully wired.

Replace the fit-synthetic stub with the runtime echo plus
`uv run python -m ambo.model.fit --layer P-SA`. Comment: S-B/S-C are T-402.
  </action>
  <acceptance_criteria>
    - `make -n fit-synthetic` does not contain NOT IMPLEMENTED
    - `pm.sample` still only in fit.py
  </acceptance_criteria>
  <verify>
    <automated>make -n fit-synthetic | grep -v "NOT IMPLEMENTED" | grep -q ambo.model.fit</automated>
  </verify>
  <done>The make target is a real sampler invocation for P-SA.</done>
</task>
<task type="auto">
  <name>Task 2: Full S-A NUTS fit and committed artifacts</name>
  <files>data/posteriors/P-SA.parquet, reports/model/diag_P-SA.md, docs/BUILD_LOG.md</files>
  <action>
Run `make fit-synthetic`. Confirm MD-071 all-green and MD-072 PPC ≥ 85%.
Commit `data/posteriors/P-SA.parquet` (with metadata) and the diag report + PPC
plot. BUILD_LOG wall-clock minutes. If red: follow MD-073; do not loosen gates.
  </action>
  <acceptance_criteria>
    - `data/posteriors/P-SA.parquet` tracked and loadable with scale factors
    - `reports/model/diag_P-SA.md` shows MD-071/072 green
    - BUILD_LOG records wall time and that it was ≤ max_fit_minutes or an ADR
      exists for the miss
  </acceptance_criteria>
  <verify>
    <automated>test -f data/posteriors/P-SA.parquet &amp;&amp; test -f reports/model/diag_P-SA.md</automated>
  </verify>
  <done>M2's S-A sampler-health gate has committed evidence.</done>
</task>
</tasks>
