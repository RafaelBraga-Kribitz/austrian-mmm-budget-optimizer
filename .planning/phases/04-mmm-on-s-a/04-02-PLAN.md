---
phase: 04-mmm-on-s-a
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/ambo/model/priors.py
  - config/priors_synthetic.yaml
  - config/settings.yaml
  - src/ambo/common/config.py
  - tests/unit/test_priors.py
  - tests/unit/test_config.py
  - docs/MODULE_CONTRACTS.md
  - docs/BUILD_LOG.md
autonomous: true
requirements:
  - REQ-q1-truth-recovery
---

<objective>
T-303: pydantic `PriorConfig` + MD-040 `config/priors_synthetic.yaml`, and D-01
`max_fit_minutes: 35` on `Settings` (closes ADR-006's Phase-4 citation deferral).
Do not create `priors_real.yaml`.
</objective>

<context>
@.planning/phases/04-mmm-on-s-a/04-CONTEXT.md
@docs/SPEC-04_mmm_model.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: max_fit_minutes on Settings (D-01)</name>
  <files>config/settings.yaml, src/ambo/common/config.py, tests/unit/test_config.py, docs/MODULE_CONTRACTS.md</files>
  <action>
Add top-level `max_fit_minutes: 35` next to `adstock_length`. Update the header
comment that currently says the ceiling "moves here in Phase 4". Add
`max_fit_minutes: int` on `Settings` with a validator `> 0`. Extend
`test_yaml_round_trip` and add `test_max_fit_minutes_is_thirty_five`. Update
MODULE_CONTRACTS `Settings` field list. Do not put this on `SamplerConfig`.
  </action>
  <acceptance_criteria>
    - `load_settings().max_fit_minutes == 35`
    - `SamplerConfig` field set unchanged
    - settings.yaml no longer says the ceiling is "deliberately absent"
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit/test_config.py -q</automated>
  </verify>
  <done>The 35 min ceiling has a published home in settings.yaml.</done>
</task>
<task type="auto">
  <name>Task 2: PriorConfig + priors_synthetic.yaml + MD-040 test</name>
  <files>src/ambo/model/priors.py, config/priors_synthetic.yaml, tests/unit/test_priors.py, docs/MODULE_CONTRACTS.md, docs/BUILD_LOG.md</files>
  <action>
Implement 03_MODULES §4.2: `BetaParams`, `GammaParams` (shape/rate),
`TruncGammaParams`, `HalfNormalParams`, `ChannelPrior`, `GlobalPriors`,
`PriorConfig` (`channels: dict[str, ChannelPrior]`, `globals: GlobalPriors`),
`load_priors(path) -> PriorConfig`, extra=forbid, frozen.

YAML lists all seven SPEC-02 channels with identical MD-040 values:
lam Beta(2,4); K Gamma(2, 1.3); s Gamma(3,2) trunc [0.3, 3.0]; beta HalfNormal(0.15).
Globals exactly SPEC-04 §4.

Test: YAML loads; extra key fails; all seven channel entries value-equal (MD-040);
no channel-differentiated numbers. Do not author priors_real.yaml.
  </action>
  <acceptance_criteria>
    - `load_priors` on the synthetic file succeeds
    - MD-040 equality test green
    - `config/priors_real.yaml` does not exist
    - MODULE_CONTRACTS heading `### src/ambo/model/priors.py`
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit/test_priors.py tests/unit/test_repo_layout.py -q &amp;&amp; test ! -f config/priors_real.yaml</automated>
  </verify>
  <done>Channel-agnostic Layer P priors load through a forbid-extra schema.</done>
</task>
</tasks>
