---
phase: 04-mmm-on-s-a
plan: 04
type: execute
wave: 4
depends_on:
  - 04-01
  - 04-02
files_modified:
  - src/ambo/model/mmm.py
  - src/ambo/model/fit.py
  - tests/unit/test_mmm.py
  - tests/unit/test_smoke_fit.py
  - tests/unit/test_config.py
  - docs/MODULE_CONTRACTS.md
  - docs/BUILD_LOG.md
autonomous: true
requirements:
  - REQ-q2-real-incremental-roas
  - REQ-dl8-quality
---

<objective>
T-304: `build_model(df, channels, priors) -> pm.Model` implementing SPEC-04 §2 on
**already scaled** data, plus the only legal `pm.sample` wrapper, plus the CI
smoke fit. Rewrite the sampler-key test (D-05).
</objective>

<context>
@.planning/phases/04-mmm-on-s-a/04-CONTEXT.md
@docs/SPEC-04_mmm_model.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: build_model + sample_model + D-05 test rewrite</name>
  <files>src/ambo/model/mmm.py, src/ambo/model/fit.py, tests/unit/test_mmm.py, tests/unit/test_config.py, docs/MODULE_CONTRACTS.md</files>
  <action>
`mmm.py`: MD-002 exactly. Fourier order 4, period 52.18 named constants (D-12).
Dummies from `promo_flag`, `advent_flag`, `jan_dip_flag`. Media:
`beta * hill(adstock(x_c))` for each name in `channels`. Coords `channel`,
`week`, `fourier`. Free RV names = D-06 table. No `'P-SA'`, `'S-A'`, or `layer`
string literals in `mmm.py` (grep test). Missing spend column → FitError.
`Implements: MD-001, MD-002`.

`fit.py`: `sample_model(...)` is the only `pm.sample` call. kwargs from the
function signature, filled by callers from `Settings.sampler` — do not hardcode
4/1000/0.9/42. Rewrite `test_sampler_keys_appear_nowhere_else_in_src_ambo` per
D-05 (forbid restated MD-050 literals assigned as sampling args, allow
attribute access).

`test_mmm.py`: free-RV name set; grep for scenario literals; prior-predictive
mean in [0.2, 5]× observed mean on a tiny scaled fixture (may use
`pm.sample_prior_predictive` in tests — not `pm.sample`).
  </action>
  <acceptance_criteria>
    - Free-RV names match D-06
    - No S-A/layer literals in mmm.py
    - `pm.sample` AST-confined to fit.py
    - MODULE_CONTRACTS entries for mmm.py and fit.py
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit/test_mmm.py tests/unit/test_import_independence.py tests/unit/test_config.py tests/unit/test_repo_layout.py -q</automated>
  </verify>
  <done>The builder matches SPEC-04 §2; sampling has one legal home.</done>
</task>
<task type="auto">
  <name>Task 2: CI smoke fit (EB-060)</name>
  <files>tests/unit/test_smoke_fit.py, docs/BUILD_LOG.md</files>
  <action>
`@pytest.mark.smoke` test: read P-SA via db.py, first 60 weeks, scale, build with
synthetic priors and `channels_present` for P-SA, `sample_model` chains=1
tune=200 draws=200 seed from settings (or 42 via sampler.random_seed). Assert
completes and max R-hat is finite. Never reported. Warehouse required (CI
already `make transform`).
  </action>
  <acceptance_criteria>
    - `make test` (no SMOKE) skips it
    - `make test SMOKE=1` runs it and it passes locally
  </acceptance_criteria>
  <verify>
    <automated>make test SMOKE=1</automated>
  </verify>
  <done>Smoke fit is the CI sampling path; full fits stay off `make test`.</done>
</task>
</tasks>
