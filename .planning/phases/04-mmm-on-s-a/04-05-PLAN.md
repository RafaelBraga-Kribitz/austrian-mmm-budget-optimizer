---
phase: 04-mmm-on-s-a
plan: 05
type: execute
wave: 5
depends_on:
  - 04-04
files_modified:
  - src/ambo/model/posterior_io.py
  - tests/unit/test_posterior_io.py
  - docs/MODULE_CONTRACTS.md
  - docs/BUILD_LOG.md
autonomous: true
requirements:
  - REQ-dl1-reproducible-pipeline
---

<objective>
T-305: save/load thinned parquet (every 4th draw → 1000 rows when full budget) plus
gitignored netCDF; embed ScaleFactors and provenance; refuse files without scale
factors; atomic writes (EB-050).
</objective>

<tasks>
<task type="auto">
  <name>Task 1: posterior_io</name>
  <files>src/ambo/model/posterior_io.py, tests/unit/test_posterior_io.py, docs/MODULE_CONTRACTS.md, docs/BUILD_LOG.md</files>
  <action>
03_MODULES §4.5: `save_posterior(idata, sf, name) -> Path`,
`load_posterior(name) -> PosteriorBundle`, frozen bundle with draws, metadata, sf.
Columns `&lt;var&gt;` or `&lt;var&gt;__&lt;channel&gt;`. Metadata: scale factors, data
hash, prior file sha256, sampler settings, git commit, seed. Thinning arithmetic
tested (4000→1000 on a constructed idata). Atomicity: temp sibling then replace;
crash-before-replace leaves no final file. Loader raises FitError on missing
scale-factor metadata. Naming BP-D-06 (`P-SA`, …). NetCDF under
`load_settings().paths.posteriors` with `.nc` (already gitignored).
  </action>
  <acceptance_criteria>
    - Round-trip draws + metadata
    - Missing scale factors refused
    - MODULE_CONTRACTS heading `### src/ambo/model/posterior_io.py`
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit/test_posterior_io.py tests/unit/test_repo_layout.py -q</automated>
  </verify>
  <done>Posteriors are loadable only with scale factors attached.</done>
</task>
</tasks>
