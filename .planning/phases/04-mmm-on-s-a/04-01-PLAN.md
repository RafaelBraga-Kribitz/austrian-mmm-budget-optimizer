---
phase: 04-mmm-on-s-a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/04-mmm-on-s-a/04-CONTEXT.md
  - .planning/phases/04-mmm-on-s-a/04-RESEARCH.md
  - .planning/phases/04-mmm-on-s-a/04-PATTERNS.md
  - .planning/phases/04-mmm-on-s-a/04-VALIDATION.md
  - .planning/phases/04-mmm-on-s-a/04-DISCUSSION-LOG.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - pyproject.toml
  - uv.lock
  - src/ambo/common/errors.py
  - src/ambo/model/transforms.py
  - tests/unit/test_transforms.py
  - docs/MODULE_CONTRACTS.md
  - docs/BUILD_LOG.md
  - tests/unit/test_import_independence.py
autonomous: true
requirements:
  - REQ-q1-truth-recovery
  - REQ-dl8-quality

must_haves:
  truths:
    - "`uv run python -c \"import pymc, arviz\"` exits 0 on this lockfile (D-02). SPEC-08 §3's declared `numpy>=1.26,<3` in `[project] dependencies` is unchanged."
    - "`FitError` is an `AmboError` subclass importable from `ambo.common.errors`."
    - "`geometric_adstock_weights`, `adstock_convolve`, and `hill_saturation` are pytensor graphs; an independent numpy reference in `tests/unit/test_transforms.py` agrees to 1e-10."
    - "Impulse test: zeros strictly before the impulse index; `adstock_convolve` does not import or call `numpy.convolve` / `scipy.signal` in `src/`."
    - "`transforms.py` does not import `load_settings` or `ambo.simulate`; `L` is an argument."
    - "`ScaleFactors` rejects an all-zero channel with `FitError`; `from_model_scale(to_model_scale(df, sf), sf)` matches the original spend/revenue columns to 1e-12."
    - "`docs/MODULE_CONTRACTS.md` has an entry whose heading is exactly `### src/ambo/model/transforms.py`, and the `errors.py` entry lists `FitError`."
  artifacts:
    - src/ambo/model/transforms.py
    - tests/unit/test_transforms.py
    - src/ambo/common/errors.py
    - pyproject.toml
    - uv.lock
    - docs/MODULE_CONTRACTS.md
  key_links:
    - "D-02 numpy constraint → every later plan that imports PyMC."
    - "ScaleFactors → posterior_io metadata (04-05) and T-1 back-transform (only site)."
    - "FitError → ScaleFactors construction and later build_model missing-column."
  prohibitions:
    - "Do not import `ambo.simulate` from `src/ambo/model/`."
    - "Do not create `config/priors_real.yaml` or `docs/PRIOR_ELICITATION.md`."
    - "Do not copy `origin/m0-bootstrap`'s `uv.lock`."
    - "Do not put `np.convolve` in `src/`."
    - "Do not rewrite git history (EB-082)."
---

<objective>
Stand up Phase 4 planning documents, restore a lockfile on which `import pymc` works,
add `FitError`, and ship `ambo.model.transforms` (normalized geometric adstock, Hill,
scaling pair) with tests that would have caught T-1 and T-2.

Purpose: T-301 is the head of the model critical path and the single most bug-prone
module (Guide §2.1). D-02 must land in the same plan because PyMC is unimportable on
the current numpy 2.5.2 / numba 0.63.0b1 pair.

Output: planning corpus, an importable PyMC environment, `transforms.py` + tests +
contracts, BUILD_LOG opening for Phase 4.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-mmm-on-s-a/04-CONTEXT.md
@.planning/phases/04-mmm-on-s-a/04-RESEARCH.md
@.planning/phases/04-mmm-on-s-a/04-PATTERNS.md
@AGENTS.md
@docs/SPEC-04_mmm_model.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Phase 4 planning corpus and ROADMAP/STATE pointers</name>
  <files>.planning/phases/04-mmm-on-s-a/04-CONTEXT.md, .planning/phases/04-mmm-on-s-a/04-RESEARCH.md, .planning/phases/04-mmm-on-s-a/04-PATTERNS.md, .planning/phases/04-mmm-on-s-a/04-VALIDATION.md, .planning/phases/04-mmm-on-s-a/04-DISCUSSION-LOG.md, .planning/phases/04-mmm-on-s-a/04-01-PLAN.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - `.planning/ROADMAP.md` Phase 4 section (plans still TBD; W6 open-item paragraph is stale)
    - `.planning/STATE.md` (current_phase 4, Phase 4 not yet planned)
  </read_first>
  <action>
Planning documents are already authored in this directory (this task commits them).
Update ROADMAP Phase 4: replace `**Plans**: TBD` with the eight 04-01…04-08 plan
lines (unchecked except 04-01 in progress is still unchecked until 04-01 SUMMARY).
Do **not** yet tick Phase 4's `[ ]` or rewrite the stale W6 paragraph beyond a
one-line pointer that CONTEXT D-01 / plan 04-02 closes it — the W6 *correction of
the open-item blurb* is 04-08's ROADMAP cleanup, but listing the eight plans is
this task so later PRs have a map.

Update STATE.md: last_activity to Phase 4 planning; pending todos T-301…T-308;
next = execute 04-01.
  </action>
  <acceptance_criteria>
    - `.planning/phases/04-mmm-on-s-a/04-CONTEXT.md` exists and contains D-01 through D-20.
    - ROADMAP Phase 4 lists 04-01-PLAN.md through 04-08-PLAN.md.
    - STATE.md `current_phase` remains 4.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/04-mmm-on-s-a/04-CONTEXT.md && grep -q "04-01-PLAN.md" .planning/ROADMAP.md</automated>
  </verify>
  <done>Phase 4 planning corpus is tracked and ROADMAP names the eight plans.</done>
</task>

<task type="auto">
  <name>Task 2: Restore an importable PyMC stack (D-02)</name>
  <files>pyproject.toml, uv.lock</files>
  <read_first>
    - `pyproject.toml` `[project] dependencies` numpy line
    - `04-RESEARCH.md` Pitfall 1
  </read_first>
  <action>
Add a `[tool.uv]` table with
`constraint-dependencies = ["numpy>=1.26,<2.5"]` (and, if the resolver still
picks `numba==0.63.0b1` after that and pymc still fails, add `"numba>=0.60,<0.63"`
to the same list — Numba stays transitive, never a `[project]` dependency).

Do **not** change the SPEC-08 declared `numpy>=1.26,<3` line in `[project]
dependencies`.

Run `uv lock` then `uv sync`. Prove `uv run python -c "import pymc, arviz, pytensor"`.
Record resolved numpy/numba/pymc versions in the BUILD_LOG entry of Task 5.
  </action>
  <acceptance_criteria>
    - `pyproject.toml` still contains `numpy>=1.26,<3` in `[project] dependencies`.
    - `pyproject.toml` contains `constraint-dependencies` with `numpy` capped below 2.5.
    - `uv run python -c "import pymc, arviz"` exits 0.
    - `uv.lock` numpy version is &lt; 2.5.
  </acceptance_criteria>
  <verify>
    <automated>uv run python -c "import pymc, arviz, numpy; assert tuple(int(p) for p in numpy.__version__.split('.')[:2]) &lt; (2, 5)"</automated>
  </verify>
  <done>PyMC and ArviZ import; lockfile numpy is below 2.5.</done>
</task>

<task type="auto">
  <name>Task 3: FitError and transforms.py with tests and contracts</name>
  <files>src/ambo/common/errors.py, src/ambo/model/transforms.py, tests/unit/test_transforms.py, docs/MODULE_CONTRACTS.md, tests/unit/test_import_independence.py</files>
  <read_first>
    - `docs/EXECUTION_BLUEPRINT/03_MODULES.md` §4.1
    - `src/ambo/simulate/dgp.py` `adstock_recursive` / `hill` (read for independence, do not import)
    - `tests/unit/test_dgp.py` impulse-first block
    - `docs/MODULE_CONTRACTS.md` `errors.py` entry and Dependency directions
    - `04-PATTERNS.md` transforms assignment
  </read_first>
  <action>
Add `class FitError(AmboError)` to `errors.py` with a docstring that it is the
`ambo.model` package error (all-zero channel, missing spend column, sampler
failures later). Update the errors.py MODULE_CONTRACTS Public API.

Create `src/ambo/model/transforms.py` implementing:

- `geometric_adstock_weights(lam, L)` — pytensor; `w_i = lam**i / sum`; L Python int.
- `adstock_convolve(x, lam, L)` — causal unrolled sum (D-18). Pre: x ≥ 0 enforced
  on the numpy eval path used by tests; graph path assumes the model will feed
  scaled non-negative spend.
- `hill_saturation(a, K, s)` — `a**s / (a**s + K**s)`, independent of `dgp.hill`.
- `class ScaleFactors` frozen: `revenue_mean: float`, `spend_means: dict[str, float]`.
- `compute_scale_factors(df, channels) -> ScaleFactors` — revenue mean; per-channel
  mean of strictly positive spend; all-zero → FitError naming the channel.
- `to_model_scale(df, sf)` / `from_model_scale(df, sf)` — DataFrame pair, inverse
  to 1e-12 on revenue and the scaled spend columns.

Do not import `load_settings` or `ambo.simulate`. Do not use `np.convolve` in this
file. Functions &lt; ~60 lines. Docstring `Implements: MD-020, MD-021, MD-030`.

Tests in `tests/unit/test_transforms.py`: impulse first; weights sum 1; pytensor
function vs numpy reference (in the test file) to 1e-10; Hill(0)=0, Hill(K)=0.5;
round-trip; all-zero FitError; empty/negative domain as appropriate. Hypothesis
round-trip allowed.

MODULE_CONTRACTS entry heading exactly `### src/ambo/model/transforms.py` with the
five labeled sections.

Update `test_import_independence.py` print strings that still say the model package
is "otherwise empty" / "vacuously true today -- model/fit.py does not exist yet"
so they describe the actual scan (fit.py still absent; transforms present). Do not
weaken the assertions.
  </action>
  <acceptance_criteria>
    - `from ambo.common.errors import FitError` works and `issubclass(FitError, AmboError)`.
    - `uv run pytest tests/unit/test_transforms.py -q` exits 0.
    - `rg -n "convolve|lfilter" src/ambo/model/transforms.py` matches nothing.
    - `rg -n "ambo.simulate|load_settings" src/ambo/model/transforms.py` matches nothing.
    - `docs/MODULE_CONTRACTS.md` contains `### src/ambo/model/transforms.py`.
    - `uv run pytest tests/unit/test_repo_layout.py tests/unit/test_import_independence.py -q` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit/test_transforms.py tests/unit/test_repo_layout.py tests/unit/test_import_independence.py -q</automated>
  </verify>
  <done>Transforms, FitError, and contracts are tested; layout and firewall guards still green.</done>
</task>

<task type="auto">
  <name>Task 4: BUILD_LOG Phase 4 open + lint/test</name>
  <files>docs/BUILD_LOG.md</files>
  <read_first>
    - `docs/BUILD_LOG.md` M2 warehouse close (append after it; do not rewrite)
  </read_first>
  <action>
Append an M2 / Phase 4 opening entry: D-02 resolved versions, T-301 landed,
planning corpus committed, 1 d Phase 4 budget reminder (D-17), no history rewrite.
Run `make lint && make test` and record the pass counts.
  </action>
  <acceptance_criteria>
    - BUILD_LOG contains a 2026-09-03 (or today) Phase 4 / T-301 entry naming the
      resolved numpy version and that pymc imports.
    - `make lint && make test` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>make lint &amp;&amp; make test</automated>
  </verify>
  <done>T-301 is logged; lint and test are green.</done>
</task>

</tasks>
