---
phase: 04-mmm-on-s-a
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
files_modified:
  - tests/unit/test_transform_sanity.py
  - docs/BUILD_LOG.md
autonomous: true
requirements:
  - REQ-q1-truth-recovery
---

<objective>
T-302 / MD-070: on S-A spend, model `adstock_convolve` vs simulator
`adstock_recursive` correlate &gt; 0.95 per channel at that channel's truth λ.
The test file imports both packages; neither package imports the other.
</objective>

<context>
@.planning/phases/04-mmm-on-s-a/04-RESEARCH.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: MD-070 shared-shape test</name>
  <files>tests/unit/test_transform_sanity.py, docs/BUILD_LOG.md</files>
  <action>
Read S-A spend through `ambo.common.db.read_mmm_input("P-SA")` (warehouse already
built in CI job 2). Truth λ from `data/synthetic/s_a/truth.json` (tests may read
Layer P committed JSON). For each of the six present channels, correlate
simulator `adstock_recursive(x, lam)` with model adstock evaluated via
`pytensor.function` at `L=load_settings().adstock_length`. Assert r &gt; 0.95.
Docstring states equality is NOT expected (MD-020). Import-independence guard
must stay green. Impulse-style assertion that a reversed kernel would fail is
optional; do not share transform code.
  </action>
  <acceptance_criteria>
    - All six S-A channels r &gt; 0.95
    - `test_import_independence` still green
    - Test docstring names MD-070 and the deliberate mismatch
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit/test_transform_sanity.py tests/unit/test_import_independence.py -q</automated>
  </verify>
  <done>MD-070 is green before any fit is attempted.</done>
</task>
</tasks>
