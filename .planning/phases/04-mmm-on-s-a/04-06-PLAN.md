---
phase: 04-mmm-on-s-a
plan: 06
type: execute
wave: 6
depends_on:
  - 04-05
files_modified:
  - src/ambo/model/diagnostics.py
  - tests/unit/test_diagnostics.py
  - reports/model/.gitkeep
  - docs/MODULE_CONTRACTS.md
  - docs/BUILD_LOG.md
autonomous: true
requirements:
  - REQ-q1-truth-recovery
---

<objective>
T-306: MD-071/072/074 machinery. `DiagGates.standard()` vs `layer_r()` explicit.
Synthetic idata unit tests (injected divergences, low ESS). Report writer path
ready for 04-07's real S-A run. Add `reports/model/.gitkeep` if the directory
has no committed file yet (D-17).
</objective>

<tasks>
<task type="auto">
  <name>Task 1: diagnostics module</name>
  <files>src/ambo/model/diagnostics.py, tests/unit/test_diagnostics.py, reports/model/.gitkeep, docs/MODULE_CONTRACTS.md, docs/BUILD_LOG.md</files>
  <action>
`DiagGates.standard()`: R-hat &lt; 1.01, ESS bulk/tail &gt; 400, divergences = 0,
BFMI &gt; 0.3. `layer_r()`: ESS 300, divergences ≤ 5. Never auto-select.
`run_diagnostics(idata, gates) -> DiagResult` pure. `write_diag_report` writes
`reports/model/diag_&lt;layer&gt;.md` plus PPC PNG; energy plot when divergences &gt; 0.
PPC: 90% central interval; coverage = share of weeks inside; MD-072 threshold
85% is evaluated, not silently dropped.

Unit tests construct pathological InferenceData (or a tiny real smoke idata
fixture) to prove fail vs pass. Relaxed profile only via `layer_r()`.
  </action>
  <acceptance_criteria>
    - Injected divergence count fails `standard()`, documented
    - `layer_r` is a constructor, not a heuristic on n_obs
    - MODULE_CONTRACTS heading `### src/ambo/model/diagnostics.py`
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit/test_diagnostics.py tests/unit/test_repo_layout.py -q</automated>
  </verify>
  <done>Gate evaluation is unit-tested before the expensive S-A fit.</done>
</task>
</tasks>
