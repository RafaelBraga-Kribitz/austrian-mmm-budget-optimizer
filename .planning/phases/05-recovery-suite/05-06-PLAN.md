---
phase: 05-recovery-suite
plan: 06
type: execute
wave: 6
depends_on:
  - 05-01
  - 05-03
  - 05-04
  - 05-05
files_modified:
  - src/ambo/validate/report.py
  - tests/unit/test_recovery_report.py
  - Makefile
  - reports/recovery/RECOVERY_REPORT.md
  - reports/recovery/*.png
  - docs/MODULE_CONTRACTS.md
  - docs/BUILD_LOG.md
autonomous: true
requirements:
  - REQ-dl2-recovery-report
---

<objective>
T-406: generate `reports/recovery/RECOVERY_REPORT.md` in SPEC-05 §8 order from
committed posteriors and JSON side-files only (VR-703). Verdict and closing are
templates with slots. Wire `make recover`. Closing ≥ 500 chars, names MD-020 and
lift tests.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: report generator + make recover</name>
  <files>src/ambo/validate/report.py, tests/unit/test_recovery_report.py, Makefile, reports/recovery/RECOVERY_REPORT.md, docs/MODULE_CONTRACTS.md, docs/BUILD_LOG.md</files>
  <action>
`generate_recovery_report() -> Path`. Section order machine-checked (headings
1…8). Templates, no free-form LLM prose. Plots: true vs posterior ROAS (dot+HDI,
truth as ×) per scenario; curve overlays on the MD-082 grid. Zero-effect headline
from VR-304 JSON. Holdout table from CSVs. OLS from JSON. Cross-check summary
from JSON. Local matplotlib rc (D-14). `make recover` must not import
`sample_model` / call `pm.sample`. Regeneration test: delete local `*.nc`, run
generator, report content stable (ignore PNG timestamps if needed; markdown
slots must match).
  </action>
  <acceptance_criteria>
    - doc-structure test enforces §8 order
    - closing section ≥ 500 chars and mentions MD-020 and lift tests
    - `make recover` does not sample
    - MODULE_CONTRACTS heading `### src/ambo/validate/report.py`
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit/test_recovery_report.py tests/unit/test_repo_layout.py -q</automated>
  </verify>
  <done>RECOVERY_REPORT.md is generated, not hand-edited.</done>
</task>
</tasks>
