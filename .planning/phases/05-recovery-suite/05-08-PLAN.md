---
phase: 05-recovery-suite
plan: 08
type: execute
wave: 8
depends_on:
  - 05-01
  - 05-06
files_modified:
  - scripts/generate_ssot.py
  - reports/NUMERIC_SSOT.md
  - config/ssot_whitelist.yaml
  - tests/unit/test_generate_ssot.py
  - Makefile
  - docs/BUILD_LOG.md
autonomous: true
requirements:
  - REQ-e4-numeric-ssot
---

<objective>
T-408: `scripts/generate_ssot.py` collects JSON side-files into
`reports/NUMERIC_SSOT.md` (GB-301 columns). Emit keys that exist (recovery
booleans, coverage stats, zero-channel verdict). Omit Layer R / optimizer keys
(BP-D-08). Wire `make ssot`. Checker stays; it now has a real table. `updated_at`
from artifact mtimes.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: SSOT generator</name>
  <files>scripts/generate_ssot.py, reports/NUMERIC_SSOT.md, config/ssot_whitelist.yaml, tests/unit/test_generate_ssot.py, Makefile, docs/BUILD_LOG.md</files>
  <action>
Registry of producers; never compute analytics. Deterministic key order.
Planted-mismatch fixture still caught by `check_ssot_consistency.py`. Whitelist
entries require justification (already parse-enforced). `make ssot` runs the
generator. Include `recovery_pass_sa/sb/sc`, `roas_coverage_*`,
`zero_channel_verdict` at minimum (GB-302 subset that exists).
  </action>
  <acceptance_criteria>
    - SSOT regenerates identically given the same artifacts
    - planted mismatch test still red-then-green
    - `make ssot` is no longer a stub
  </acceptance_criteria>
  <verify>
    <automated>uv run python scripts/generate_ssot.py &amp;&amp; uv run python scripts/check_ssot_consistency.py &amp;&amp; uv run pytest tests/unit/test_generate_ssot.py tests/unit/test_governance_checks.py -q</automated>
  </verify>
  <done>SSOT is generated from side-files; the checker has something to reconcile.</done>
</task>
</tasks>
