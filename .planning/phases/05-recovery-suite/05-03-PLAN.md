---
phase: 05-recovery-suite
plan: 03
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - src/ambo/model/fit.py
  - src/ambo/model/posterior_io.py
  - src/ambo/validate/holdout.py
  - tests/unit/test_holdout.py
  - reports/model/holdout_P-SA.csv
  - reports/model/holdout_P-SB.csv
  - reports/model/holdout_P-SC.csv
  - docs/MODULE_CONTRACTS.md
  - docs/BUILD_LOG.md
autonomous: true
requirements:
  - REQ-q1-truth-recovery
---

<objective>
T-403: wire `--variant holdout` (first T−13 weeks, scale factors on that slice),
conditional forecast of the last 13 with actual spend, MAPE + 90% coverage vs
seasonal-naive `revenue_{t-52}`. Commit CSVs only (D-19). Gate: beat naive MAPE
on P-SA and P-SB.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: holdout variant + holdout.py + CSVs</name>
  <files>src/ambo/model/fit.py, src/ambo/model/posterior_io.py, src/ambo/validate/holdout.py, tests/unit/test_holdout.py, reports/model/holdout_P-SA.csv, reports/model/holdout_P-SB.csv, reports/model/holdout_P-SC.csv, docs/MODULE_CONTRACTS.md, docs/BUILD_LOG.md</files>
  <action>
Allow `{layer}__holdout` in posterior_io names. `run_fit(..., variant="holdout")`
slices first T−13, computes ScaleFactors on the slice, samples, predicts last 13
with observed spend (extend trend/Fourier/promo flags from the mart). Assert
T ≥ 65. `run_holdout(layer)` writes `reports/model/holdout_<layer>.csv` with
columns including model MAPE, naive MAPE, 90% coverage (A-7). Do not commit
holdout parquets. Gate evaluated in tests/report: model MAPE &lt; naive on P-SA
and P-SB; P-SC reported either way. BUILD_LOG wall times.
  </action>
  <acceptance_criteria>
    - CSV schema test green
    - coverage number present (never MAPE alone)
    - P-SA and P-SB beat naive
    - MODULE_CONTRACTS heading `### src/ambo/validate/holdout.py`
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit/test_holdout.py tests/unit/test_repo_layout.py -q</automated>
  </verify>
  <done>VR-401 holdout CSVs exist with MAPE and coverage.</done>
</task>
</tasks>
