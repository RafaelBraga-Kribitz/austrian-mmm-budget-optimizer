---
phase: 05-recovery-suite
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - Makefile
  - data/posteriors/P-SB.parquet
  - data/posteriors/P-SC.parquet
  - reports/model/diag_P-SB.md
  - reports/model/diag_P-SC.md
  - reports/model/ppc_P-SB.png
  - reports/model/ppc_P-SC.png
  - docs/BUILD_LOG.md
autonomous: true
requirements:
  - REQ-q1-truth-recovery
---

<objective>
T-402: full MD-050 fits for P-SB and P-SC; commit parquets + standard-profile diag
reports (and PPC PNGs). Extend `make fit-synthetic` to three layers (D-18). Expect
MD-073 on S-C. Do not loosen MD-071. Do not change priors_synthetic.yaml.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: extend make fit-synthetic</name>
  <files>Makefile</files>
  <action>
FIT_SYNTHETIC_RUNTIME: 3 fits (P-SA, P-SB, P-SC), ≤35 min/fit.
Recipe: echo runtime; then three `uv run python -m ambo.model.fit --layer`
lines in that order. No skip-if-exists. Comment names T-402 / D-18.
  </action>
  <acceptance_criteria>
    - Makefile runs all three layers
    - stub message gone from fit-synthetic (already real)
  </acceptance_criteria>
  <verify>
    <automated>grep -q "P-SC" Makefile</automated>
  </verify>
  <done>fit-synthetic matches SPEC-08 §5's three-scenario listing.</done>
</task>
<task type="auto">
  <name>Task 2: run and commit P-SB and P-SC</name>
  <files>data/posteriors/P-SB.parquet, data/posteriors/P-SC.parquet, reports/model/diag_P-SB.md, reports/model/diag_P-SC.md, reports/model/ppc_P-SB.png, reports/model/ppc_P-SC.png, docs/BUILD_LOG.md</files>
  <action>
`export PYTENSOR_FLAGS="cxx=/usr/bin/g++"`. Fit P-SB then P-SC via the CLI (the
existing MD-073 ladder applies). Commit parquet + diag + PPC (energy PNG only if
divergences &gt; 0). Do not commit `*.nc`. BUILD_LOG: wall times, R-hat, ESS,
divergences, BFMI, PPC %, which ladder attempt won. Combined wall time feeds
BP-D-18. If MD-071 still red after 0.99 and only divergences failed, stop and
ask the human — do not auto-apply rung 3/4, do not widen the gate.
  </action>
  <acceptance_criteria>
    - Both parquets load via `load_posterior`
    - Both diag reports all-green under the standard profile
    - BUILD_LOG records both wall times
  </acceptance_criteria>
  <verify>
    <automated>uv run python -c "from ambo.model.posterior_io import load_posterior; load_posterior('P-SB'); load_posterior('P-SC')"</automated>
  </verify>
  <done>S-B and S-C posteriors are committed with healthy sampler diagnostics.</done>
</task>
</tasks>
