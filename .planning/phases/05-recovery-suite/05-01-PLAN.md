---
phase: 05-recovery-suite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/05-recovery-suite/05-CONTEXT.md
  - .planning/phases/05-recovery-suite/05-RESEARCH.md
  - .planning/phases/05-recovery-suite/05-PATTERNS.md
  - .planning/phases/05-recovery-suite/05-VALIDATION.md
  - .planning/phases/05-recovery-suite/05-DISCUSSION-LOG.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - src/ambo/common/errors.py
  - src/ambo/validate/recovery.py
  - config/recovery_gates.yaml
  - tests/unit/test_recovery.py
  - docs/MODULE_CONTRACTS.md
  - docs/BUILD_LOG.md
autonomous: true
requirements:
  - REQ-q1-truth-recovery
must_haves:
  truths:
    - "config/recovery_gates.yaml matches SPEC-05 §3 cell-for-cell (D-04)."
    - "compute_recovery on a constructed posterior with known β,K,s,λ and spend reproduces ROAS to 1e-8 (D-06)."
    - "evaluate_gates uses YAML thresholds only; a test asserts every SPEC-05 §3 cell."
    - "validate/ does not import ambo.simulate.dgp (D-03)."
    - "90% intervals come from arviz.hdi (D-07)."
    - "docs/MODULE_CONTRACTS.md heading exactly ### src/ambo/validate/recovery.py"
  artifacts:
    - src/ambo/validate/recovery.py
    - config/recovery_gates.yaml
    - tests/unit/test_recovery.py
  prohibitions:
    - "Do not import ambo.simulate.dgp from validate."
    - "Do not import ambo.simulate from ambo.model."
    - "Do not create priors_real.yaml or PRIOR_ELICITATION.md."
    - "Do not commit P-SB/P-SC posteriors in this plan (05-02)."
    - "Do not call pm.sample."
    - "Do not invent Layer R."
---

<objective>
Stand up Phase 5 planning documents and ship `ambo.validate.recovery` (T-401):
VR-301…306 metrics + gate evaluation against a YAML table that mirrors SPEC-05 §3.
Unit-tested on constructed posteriors. Full P-SB/P-SC evaluation is 05-02.
</objective>

<context>
@.planning/phases/05-recovery-suite/05-CONTEXT.md
@.planning/phases/05-recovery-suite/05-RESEARCH.md
@docs/SPEC-05_validation_recovery.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Phase 5 planning corpus and ROADMAP/STATE pointers</name>
  <files>.planning/phases/05-recovery-suite/05-CONTEXT.md, .planning/phases/05-recovery-suite/05-RESEARCH.md, .planning/phases/05-recovery-suite/05-PATTERNS.md, .planning/phases/05-recovery-suite/05-VALIDATION.md, .planning/phases/05-recovery-suite/05-DISCUSSION-LOG.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <action>
Planning documents are already authored in this directory (this task commits them).
Update ROADMAP Phase 5: replace `**Plans**: TBD` with 05-01…05-10 plan lines
(unchecked). Do not tick Phase 5. Do not yet rewrite the stale WARNING-4 paragraph
(05-10). STATE: last_activity Phase 5 planning; pending todos T-401…T-410; next =
execute 05-01 Task 2.
  </action>
  <acceptance_criteria>
    - 05-CONTEXT.md contains D-01 through D-26.
    - ROADMAP Phase 5 lists 05-01-PLAN.md through 05-10-PLAN.md.
    - STATE current_phase remains 5.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/05-recovery-suite/05-CONTEXT.md &amp;&amp; grep -q "05-01-PLAN.md" .planning/ROADMAP.md</automated>
  </verify>
  <done>Phase 5 planning corpus is tracked and ROADMAP names the ten plans.</done>
</task>

<task type="auto">
  <name>Task 2: ValidationError, gate YAML, recovery.py, tests, contracts</name>
  <files>src/ambo/common/errors.py, config/recovery_gates.yaml, src/ambo/validate/recovery.py, tests/unit/test_recovery.py, docs/MODULE_CONTRACTS.md, docs/BUILD_LOG.md</files>
  <action>
Add `ValidationError(AmboError)`.

Write `config/recovery_gates.yaml` whose cells equal SPEC-05 §3 (D-04). Include
ungated flags for VR-302/VR-303 on S-C and VR-304 only on S-C.

Implement `src/ambo/validate/recovery.py` per 03_MODULES §5.1 and CONTEXT D-03…D-12,
D-25, D-26:

- `LAYER_TO_SCENARIO = {"P-SA": "s_a", "P-SB": "s_b", "P-SC": "s_c"}`
- frozen pydantic `RecoveryMetrics`, `ChannelRecovery`, `GateResults`
- `compute_recovery(layer, *, bundle=None, frame=None, truth=None) -> RecoveryMetrics`
- `evaluate_gates(m) -> GateResults`
- compile model `adstock_convolve` + `hill_saturation` once; ROAS = Σm_eur/Σx_eur
  with `m_eur = m_scaled * revenue_mean`
- HDI via `arviz.hdi(..., hdi_prob=0.9)`
- VR-303 MD-082 grid; truth via `response_curve_at`; model SS curve as D-08
- VR-305 D-09; VR-306 D-10; VR-304 D-26
- write JSON side-files under `reports/recovery/` when a repo-root reports dir
  exists; tests may pass a tmp path or skip write via a keyword
- no `pm.sample`; no `simulate.dgp` import

Tests in `tests/unit/test_recovery.py`:
1. Constructed posterior + spend: known ROAS to 1e-8.
2. True ROAS inside a tight HDI ⇒ VR-301 pass; outside ⇒ fail (injected).
3. YAML cells match a hard-coded SPEC-05 §3 table in the test (the test is the
   independent copy).
4. Zero-max truth curve ⇒ MAE% is None and excluded from the median.
5. validate AST: no import of `ambo.simulate.dgp`.
6. Half-life ranking pass/fail on constructed λ.
7. Serialization round-trip JSON.

MODULE_CONTRACTS: `### src/ambo/validate/recovery.py` plus `ValidationError` on
errors.py; update dependency-directions to mention `simulate.truth` for validate.

BUILD_LOG append T-401 entry. Do not commit P-SB/P-SC artifacts.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_recovery.py tests/unit/test_repo_layout.py tests/unit/test_import_independence.py -q` exits 0
    - `config/priors_real.yaml` still absent
    - heading `### src/ambo/validate/recovery.py` exists
    - recovery.py does not contain `pm.sample` or `simulate.dgp`
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit/test_recovery.py tests/unit/test_repo_layout.py tests/unit/test_import_independence.py -q</automated>
  </verify>
  <done>VR-301…306 engine is fixture-tested; thresholds live in YAML.</done>
</task>
</tasks>
