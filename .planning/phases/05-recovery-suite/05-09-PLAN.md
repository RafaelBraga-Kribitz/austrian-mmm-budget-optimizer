---
phase: 05-recovery-suite
plan: 09
type: execute
wave: 9
depends_on:
  - 05-08
files_modified:
  - tests/unit/test_governance_checks.py
  - scripts/check_layer_order.py
  - docs/BUILD_LOG.md
autonomous: true
requirements:
  - REQ-e2-layer-order
---

<objective>
T-409: extend (do not replace) `check_layer_order.py` and its tests so both
GB-501 failure modes fire in throwaway repos. Green on the current repo (no
Layer R). CI job 5 already wired with fetch-depth 0.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: ancestry failure-mode tests</name>
  <files>tests/unit/test_governance_checks.py, scripts/check_layer_order.py, docs/BUILD_LOG.md</files>
  <action>
Read existing tests first. Add any missing GB-501 cases: (1) Layer R artifact
without RECOVERY_REPORT.md; (2) Layer R added on a commit that is not a
descendant of the report's first-add. Keep vacuous success when no Layer R and
no freeze tag. Do not invent a freeze tag. Do not add Layer R files to this repo.
  </action>
  <acceptance_criteria>
    - `uv run python scripts/check_layer_order.py` exits 0 on this repo
    - tmp-repo tests prove both GB-501 failures
    - script is not rewritten from scratch
  </acceptance_criteria>
  <verify>
    <automated>uv run python scripts/check_layer_order.py &amp;&amp; uv run pytest tests/unit/test_governance_checks.py -q</automated>
  </verify>
  <done>Layer-order is green today and will fail if Layer R lands first.</done>
</task>
</tasks>
