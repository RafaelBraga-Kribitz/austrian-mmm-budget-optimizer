---
phase: 05-recovery-suite
plan: 04
type: execute
wave: 4
depends_on:
  - 05-02
files_modified:
  - src/ambo/validate/baseline_ols.py
  - tests/unit/test_baseline_ols.py
  - reports/recovery/ols_P-SB.json
  - docs/MODULE_CONTRACTS.md
  - docs/BUILD_LOG.md
autonomous: true
requirements:
  - REQ-q1-truth-recovery
---

<objective>
T-404: OLS + HC1 (numpy/scipy only) with adstock fixed at prior-mode λ and no
saturation. Textbook fixture to 1e-8. Side-by-side S-B table for the report.
Whatever signs emerge, they ship (VR-601).
</objective>

<tasks>
<task type="auto">
  <name>Task 1: OLS+HC1</name>
  <files>src/ambo/validate/baseline_ols.py, tests/unit/test_baseline_ols.py, reports/recovery/ols_P-SB.json, docs/MODULE_CONTRACTS.md, docs/BUILD_LOG.md</files>
  <action>
`fit_ols_baseline(df, channels, prior_mode_lambdas) -> OLSResult`. Design matrix:
intercept, t/T, Fourier (same 4 harmonics as the model), promo/advent/jan flags,
adstocked spend at prior-mode λ (model `adstock_convolve`, no Hill). HC1 sandwich
per Guide §4.3 / standard formula. No statsmodels. Columns: coef, HC1 se,
sign-stability vs Bayesian median ROAS (loaded from T-401 JSON if present, else
computed). Test: textbook 2-regressor example to 1e-8; `import statsmodels`
absent from src. Write `reports/recovery/ols_P-SB.json`.
  </action>
  <acceptance_criteria>
    - textbook test 1e-8
    - no statsmodels import in src
    - MODULE_CONTRACTS heading `### src/ambo/validate/baseline_ols.py`
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit/test_baseline_ols.py tests/unit/test_repo_layout.py tests/unit/test_import_independence.py -q</automated>
  </verify>
  <done>OLS+HC1 exists without a new dependency.</done>
</task>
</tasks>
