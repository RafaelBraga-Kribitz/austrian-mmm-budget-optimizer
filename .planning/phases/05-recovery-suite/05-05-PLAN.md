---
phase: 05-recovery-suite
plan: 05
type: execute
wave: 5
depends_on:
  - 05-02
files_modified:
  - src/ambo/validate/crosscheck.py
  - tests/unit/test_crosscheck.py
  - reports/recovery/crosscheck_mapping.md
  - reports/recovery/crosscheck_P-SB.json
  - docs/MODULE_CONTRACTS.md
  - docs/BUILD_LOG.md
autonomous: true
requirements:
  - REQ-q1-truth-recovery
---

<objective>
T-405: pymc-marketing MMM on S-B, matched as closely as 0.19.4's API allows.
Channel-ROAS median correlation ≥ 0.8 vs raw-PyMC (gate). Mapping doc lists every
matched and unmatchable element. Imports confined to this module (MD-003).
</objective>

<tasks>
<task type="auto">
  <name>Task 1: crosscheck.py + mapping doc</name>
  <files>src/ambo/validate/crosscheck.py, tests/unit/test_crosscheck.py, reports/recovery/crosscheck_mapping.md, reports/recovery/crosscheck_P-SB.json, docs/MODULE_CONTRACTS.md, docs/BUILD_LOG.md</files>
  <action>
`run_crosscheck("P-SB")`. Record installed `pymc_marketing.__version__` (expect
0.19.4) in the mapping doc. Table: adstock form, saturation form, prior-by-prior
mapping, unmatched + why (VR-602). Correlation of per-channel posterior-median
ROAS vs `metrics_P-SB.json`. If the API cannot match a piece, document it — do
not pretend. Guard test already exists; creating this file makes it load-bearing.
Do not import pymc-marketing anywhere else. Sampling goes through marketing's
own API inside this module — that is not `pm.sample` in `ambo.model.fit`; if
their API calls `pm.sample` internally that is fine. Do not add a second
`pm.sample` call site in ambo src. BUILD_LOG wall time.
  </action>
  <acceptance_criteria>
    - mapping doc committed with version pin
    - correlation evaluated and stored in JSON
    - `test_pymc_marketing_imported_only_in_validate_crosscheck` still green
    - MODULE_CONTRACTS heading `### src/ambo/validate/crosscheck.py`
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit/test_crosscheck.py tests/unit/test_import_independence.py tests/unit/test_repo_layout.py -q</automated>
  </verify>
  <done>VR-602 mapping and correlation exist; MD-003 remains confined.</done>
</task>
</tasks>
