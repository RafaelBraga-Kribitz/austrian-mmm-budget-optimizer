---
phase: 05-recovery-suite
plan: 07
type: execute
wave: 7
depends_on:
  - 05-02
files_modified:
  - scripts/generate_golden_metrics.py
  - tests/golden/recovery_bands.json
  - tests/golden/test_recovery_bands.py
  - docs/BUILD_LOG.md
autonomous: true
requirements:
  - REQ-dl1-reproducible-pipeline
---

<objective>
T-407: golden tolerance bands ±0.15·posterior SD on per-channel median ROAS
(VR-701/702). Never checksum draws (T-6). First generation is this plan's
deliverable; later regen needs human approval (D-21).
</objective>

<tasks>
<task type="auto">
  <name>Task 1: golden bands + tests</name>
  <files>scripts/generate_golden_metrics.py, tests/golden/recovery_bands.json, tests/golden/test_recovery_bands.py, docs/BUILD_LOG.md</files>
  <action>
Script header: how to regen + "PR justification required" (EB-073). Read
committed parquets via T-401 metrics JSON (preferred: bands on the same ROAS
draws recovery uses). Sorted keys. Test: schema; committed posteriors' medians
lie inside bands; file stable across two generator runs. No hash of parquet
bytes or draw vectors.
  </action>
  <acceptance_criteria>
    - bands file committed
    - golden test green against committed parquets
    - generator is deterministic (sorted keys)
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/golden/test_recovery_bands.py -q</automated>
  </verify>
  <done>Cross-platform golden bands exist without draw checksums.</done>
</task>
</tasks>
