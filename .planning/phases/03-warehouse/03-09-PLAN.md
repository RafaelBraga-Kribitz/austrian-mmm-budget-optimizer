---
phase: 03-warehouse
plan: 09
type: execute
wave: 8
depends_on:
  - 03-08
files_modified:
  - .github/workflows/ci.yml
  - docs/RISK_REGISTER.md
  - docs/BUILD_LOG.md
  - .planning/ROADMAP.md
  - .planning/phases/03-warehouse/03-VALIDATION.md
  - docs/EXECUTION_BLUEPRINT/02_WBS.md
autonomous: false
requirements:
  - REQ-dl1-reproducible-pipeline

must_haves:
  truths:
    - "CI job 3 runs on a two-leg matrix, `ubuntu-latest` and `windows-latest`, with the same conditional make-install step job 2 already uses. The job name stays `dbt`, so EB-060's six jobs remain six — a matrix leg is not a seventh job (D-22, Phase 1 D-21 precedent)."
    - "CI job 3 regenerates the export and fails on drift: `make transform`, then `make export`, then a diff check on `exports/` that exits non-zero if the committed file differs. A mart change that alters the export fails the build unless the committed CSV is regenerated in the same pull request (D-02)."
    - "No new CI job is added — job 3 gains steps and a matrix, mirroring how Phase 1's D-20 seed diff-check was folded into job 1 rather than becoming a seventh job."
    - "The three stale upstream statements are corrected: WBS T-205 AC-3 (exports gitignored, superseded by SPEC-08 §2 post-W5), the ROADMAP Phase 3 BP-D-05 note (stale since ADR-000 D-2 ratified BP-D-01 through BP-D-20 wholesale), and the ROADMAP Phase 3 effort line (no per-phase budget, now 1 d per D-17). Each is a correction of a stale statement of already-ratified fact — an interpretation, not a spec edit — so a `docs/BUILD_LOG.md` entry suffices and no ADR is owed (Phase 1 D-07)."
    - "`docs/RISK_REGISTER.md` carries an entry requiring D-26's leak-scan scope for committed `exports/*.csv` to be revisited in Phase 6, before the `layer_r_present` flip, so the revisit does not depend on anyone's memory (D-05)."
    - "`docs/BUILD_LOG.md`'s M2 entry is closed with the actual Phase 3 effort against the 1 d budget and the 2 d tripwire, an index of every red-then-green proof produced across plans 03-01 through 03-08, and any shed that was taken with its D-19 rank."
    - "The Phase 3 per-task verification map in `03-VALIDATION.md` has its Plan and Wave columns filled from the plans that actually shipped each check, and its sign-off checklist completed."
    - statement: "Regenerating the export on the `windows-latest` CI leg produces a file byte-identical to the `ubuntu-latest` leg — the diff check on `exports/` is empty on both legs of job 3."
      verification: backstop
  artifacts:
    - .github/workflows/ci.yml
    - docs/RISK_REGISTER.md
    - docs/BUILD_LOG.md
  key_links:
    - "Job 3's Windows leg is what encodes path-separator, line-ending and shell-quoting portability. Today job 3 is Ubuntu-only while the dev machine is Windows, so any divergence would surface only locally, get fixed ad hoc, and never reach CI (RESEARCH Pitfall 5)."
    - "The job 3 export diff-check plus plan 03-08's byte-identical regeneration test are the two halves of D-02. Without the byte-stable writer, this gate would be flaky; without this gate, the writer's stability would be unverified in CI."
    - "`docs/RISK_REGISTER.md`'s D-05 entry is the tracking mechanism that survives context loss between Phase 3 and Phase 6."
  prohibitions:
    - "A red gate in this phase is never resolved by rewriting, amending, or force-pushing git history (EB-082, 09_ANTI_PATTERNS §A-17). Layer order and the append-only history are the argument this project makes; if a gate goes red, investigate or tell the human — never edit the past."
    - "No dbt test, model contract, CI gate, or acceptance threshold from this phase may be weakened, disabled, or excluded to make CI green. If the Windows leg fails, fix the portability problem it found — that is the entire reason the leg was added."
---

<objective>
Harden CI job 3 with the Windows matrix leg and the export drift-check, correct the three stale
upstream statements this phase surfaced, record the D-05 risk entry, and close the M2 build-log
entry.

Purpose: everything built in plans 03-01 through 03-08 is verified locally on a Windows dev machine
and, for the Ubuntu half, in CI. The two gaps are that job 3 has never run on Windows — so any
path-separator or line-ending divergence in the dbt invocation would surface only locally and never
get encoded — and that the committed export has no CI gate proving it stays in sync with the mart.
This plan closes both, then leaves the phase's paper trail correct.

Output: a two-leg job 3 with a drift gate, three corrected documents, one risk-register entry, a
closed M2 build-log entry, a completed validation map, and a human confirmation that both CI legs are
green.

Standing obligations (D-18, D-19): if any shed was taken during this phase, it is recorded in
`docs/BUILD_LOG.md` with its D-19 rank as part of Task 2 — a shed that reached this plan unrecorded is
itself a finding.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-warehouse/03-CONTEXT.md
@.planning/phases/03-warehouse/03-RESEARCH.md
@.planning/phases/03-warehouse/03-PATTERNS.md
@.planning/phases/03-warehouse/03-VALIDATION.md
@.planning/phases/03-warehouse/03-08-SUMMARY.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CI job 3 — the Windows matrix leg and the export drift gate</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - `.github/workflows/ci.yml` (all 168 lines — the header comment stating the six-job contract and the no-`if:`-on-a-job rule, the `test` job's matrix and conditional make-install step at lines 57-98, the `lint` job's regenerate-then-diff steps at lines 44-50, and the current `dbt` job body at lines 104-114)
    - `.planning/phases/03-warehouse/03-PATTERNS.md` § `.github/workflows/ci.yml` — the two verbatim patterns to copy and where each step goes
    - `.planning/phases/03-warehouse/03-RESEARCH.md` § Common Pitfalls Pitfall 5 (why the Windows leg matters) and § Assumptions Log item A2 (the untested-on-Linux path-resolution assumption these two legs together resolve)
    - `Makefile` (the `transform` and `export` targets as rewritten in plans 03-01 and 03-08)
    - `.gitattributes` (the LF pin the Windows leg's checkout must honour for the diff check to be meaningful)
  </read_first>
  <action>
Modify the `dbt` job in `.github/workflows/ci.yml`. Keep `name: dbt` unchanged — the job stays one of
EB-060's six, exactly as the `test` job stays one job despite two matrix legs (Phase 1 D-21).

Add a `strategy` block with `fail-fast: false` and `matrix.os: [ubuntu-latest, windows-latest]`, and
change `runs-on` to read from the matrix. Copy the `test` job's Windows make-install step verbatim,
positioned identically — immediately after the checkout step and before the uv setup step. That step
carries a step-level OS conditional selecting which setup runs; it is not a job-level skip guard, so
the D-16 rule that no job may carry an `if:` key is untouched and the job still runs to completion and
reports a real result on both legs.

Add a timeout to the job in line with the `test` job's, since job 3 now runs twice.

After the existing `make transform` step, add two steps implementing D-02's drift gate, following the
exact two-step shape job 1 already uses for the season-windows seed: a step that runs `make export`,
then a step named to say what its failure means, running a diff check with a non-zero exit on any
difference under `exports/`. Job 3 already builds dbt, so this adds no job and EB-060's six stay six.

Update the job's header comment. The current text describes the D-17 vacuously-correct check, which
plan 03-01 retired when it made `make transform` unconditional. Replace it with what the job now does:
build the dbt project on committed synthetic data with no private inputs, on both an Ubuntu and a
Windows runner, then regenerate the export and fail on drift. Name D-21, D-22 and D-02 so a reader can
trace each part.

Two portability points the executor must check rather than assume. First, the diff check must be
meaningful on Windows: `.gitattributes` pins LF on checkin and checkout for CSV files, and plan
03-08's writer emits LF explicitly, so a Windows checkout should not introduce carriage returns. If
the Windows leg reports a whole-file diff, that is a real finding about the LF pin, not a reason to
relax the gate — investigate it and record the outcome. Second, `make` on the Windows runner comes
from the same Chocolatey package job 2 already uses, and the Makefile's shell pin at lines 17-18 is
what makes the recipes portable; do not add Windows-specific recipe branches.
  </action>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` still declares exactly six jobs: `lint`, `test`, `dbt`, `ssot`, `layer-order`, `leak`.
    - The `dbt` job declares `name: dbt`, a `strategy.matrix.os` of `[ubuntu-latest, windows-latest]`, `fail-fast: false`, and `runs-on` reading from the matrix.
    - The `dbt` job contains a make-install step carrying a step-level Windows conditional, positioned between the checkout step and the uv setup step, identical in form to the `test` job's.
    - No job in the file carries a job-level `if:` key.
    - The `dbt` job's steps, in order, include: checkout, the conditional make install, uv setup, dependency sync, `make transform`, `make export`, and a diff check on `exports/` that exits non-zero on any difference.
    - The `dbt` job's header comment names D-21, D-22 and D-02, and no longer describes a vacuously-correct existence check.
    - `uv run python -c "import yaml,pathlib; d=yaml.safe_load(pathlib.Path('.github/workflows/ci.yml').read_text()); assert sorted(d['jobs'])==['dbt','layer-order','leak','lint','ssot','test']; j=d['jobs']['dbt']; assert j['strategy']['matrix']['os']==['ubuntu-latest','windows-latest']; assert 'if' not in j"` exits 0.
    - Locally, `make transform && make export && git diff --exit-code exports/` exits 0 — the gate is green before it is pushed.
  </acceptance_criteria>
  <verify>
    <automated>uv run python -c "import yaml, pathlib; d = yaml.safe_load(pathlib.Path('.github/workflows/ci.yml').read_text()); assert sorted(d['jobs']) == ['dbt','layer-order','leak','lint','ssot','test'], sorted(d['jobs']); j = d['jobs']['dbt']; assert j['name'] == 'dbt'; assert j['strategy']['matrix']['os'] == ['ubuntu-latest','windows-latest'], j['strategy']; assert all('if' not in job for job in d['jobs'].values()); print('ci job 3 OK')" && make transform && make export && git diff --exit-code exports/</automated>
  </verify>
  <done>Job 3 runs on both operating systems, regenerates the export and fails on drift, and EB-060's six jobs are still six.</done>
</task>

<task type="auto">
  <name>Task 2: The three stale-document corrections, the D-05 risk entry, and the M2 build-log close</name>
  <files>docs/RISK_REGISTER.md, docs/BUILD_LOG.md, .planning/ROADMAP.md, docs/EXECUTION_BLUEPRINT/02_WBS.md, .planning/phases/03-warehouse/03-VALIDATION.md</files>
  <read_first>
    - `docs/RISK_REGISTER.md` (the existing entry format and risk-id scheme to extend)
    - `docs/BUILD_LOG.md` (the M2 entry opened by plan 03-01 — the budget split, shed order and departures already recorded there)
    - `.planning/ROADMAP.md` lines 209-234 (the Phase 3 section: the effort-budget line and the trailing BP-D-05 note, both to be corrected)
    - `docs/EXECUTION_BLUEPRINT/02_WBS.md` lines 500-512 (T-205's acceptance criteria, AC-3 specifically)
    - `docs/SPEC-08_engineering.md` §2 (the `exports/ (*.csv COMMITTED — DL-1 compares against them; EB-081)` line that supersedes the WBS card)
    - `docs/ADR/ADR-000_document-precedence-and-blueprint-defaults.md` D-2 (the wholesale BP-D-01 through BP-D-20 ratification that makes the ROADMAP note stale)
    - `.planning/phases/03-warehouse/03-CONTEXT.md` § Corrections to upstream documents and § Specific Ideas (the three corrections and the Phase 1 D-07 rule that a stale-fact correction is an interpretation, not a spec edit)
    - `.planning/phases/03-warehouse/03-VALIDATION.md` (the per-task verification map with TBD Plan and Wave columns, and the sign-off checklist)
    - The `03-0N-SUMMARY.md` files from plans 03-01 through 03-08 (the red-then-green evidence to index)
  </read_first>
  <action>
Correct WBS T-205 AC-3 in `docs/EXECUTION_BLUEPRINT/02_WBS.md`: replace the "Exports land gitignored;
`.gitkeep` intact" text with the post-W5 position — `exports/*.csv` is committed by design per
SPEC-08 §2 and EB-081, and Phase 9's DL-1 probe compares regenerated exports against the committed
versions. Note in the same edit that this card was written before the W5 resolution and was never
updated. Be aware that `docs/EXECUTION_BLUEPRINT/` is gitignored as internal build scaffolding, so
this edit will not appear in the commit — the tracked record of the correction is the
`docs/BUILD_LOG.md` entry below. Make the edit anyway: the file remains on disk and is what future
executors read.

Correct the two stale statements in `.planning/ROADMAP.md`'s Phase 3 section. Replace the
effort-budget line "Shares M2's 2 d with Phase 4" with the D-17 split — Phase 3 is 1 d of M2's 2 d
and Phase 4 takes the other 1 d, each with its own 2× trip point and its own standing-rule-5 ADR
obligation. Replace the trailing BP-D-05 note: `layer_r_present` starting false and AD-044 staying
dormant until Phase 6 is binding, ratified wholesale by ADR-000 D-2, which states the
Definition-of-Ready accept/override item is satisfied for all twenty blueprint defaults by that ADR —
it is not an open decision to accept or override at task time. Use scoped edits; do not rewrite the
file or touch any other phase's section.

Add an entry to `docs/RISK_REGISTER.md` recording D-05. It must state: `exports/mmm_input_weekly.csv`
is now committed; once Phase 6 flips `layer_r_present`, that committed file will carry Layer R aEUR
rows into a repository that goes public at M3; Phase 1's D-26 scoped aEUR figures in `exports/` out of
the leak scan, correctly, at a time when nothing was committed there; and D-26's scope must therefore
be revisited in Phase 6, **before** the flip. Name Phase 6 as the owning phase and
`scripts/export_marts.py`'s header comment as the in-code pointer. State plainly why the scan was not
widened now: the patterns cannot be tuned against data that does not exist, and a gate that cries wolf
now is one that gets ignored in Phase 6. Wording is Claude's Discretion.

Close the M2 entry in `docs/BUILD_LOG.md` that plan 03-01 opened. Record: the actual Phase 3 effort
against the 1 d budget and the 2 d tripwire, with an explicit statement of whether the tripwire was
reached; any shed taken, with its D-19 rank and the reason (and, if no shed was taken, say so —
silence is ambiguous); an index of every red-then-green proof produced across plans 03-01 through
03-08, naming the plan and what each proved, following the Phase 1 T-010 AC-1 precedent of recording
guard-test evidence in this file; and the three stale-document corrections with the Phase 1 D-07
rationale that correcting a stale statement of already-ratified fact is an interpretation, not a spec
edit, so no ADR is owed. Confirm the three deliberate departures plan 03-01 recorded are still
accurate as built — D-15's seed-derived spine, D-16's strict AD-043, and the profiles.yml path form —
and note that all three need a line in the M2 pull-request description so a reviewer does not read
them as oversights.

Fill in `.planning/phases/03-warehouse/03-VALIDATION.md`. Replace every TBD in the per-task
verification map's Plan and Wave columns with the plan and wave that actually shipped each check,
mapping the WBS task IDs to the plan numbers: T-201 to plan 03-01 wave 1, T-202 to plan 03-03 wave 2,
T-203 to plans 03-04 wave 3 / 03-05 wave 4 / 03-06 wave 5, T-204 to plans 03-02 wave 1 and 03-07
wave 6, T-205 to plans 03-08 wave 7 and 03-09 wave 8. Update each row's File Exists and Status
columns to reflect reality. Tick the Validation Sign-Off checklist and set `nyquist_compliant: true`
and `status: validated` in the frontmatter only if every box is genuinely satisfied — if one is not,
leave it unticked and say why in the plan SUMMARY.
  </action>
  <acceptance_criteria>
    - `docs/EXECUTION_BLUEPRINT/02_WBS.md` T-205 AC-3 no longer states that exports land gitignored, and names SPEC-08 §2 and EB-081 as the superseding source.
    - `.planning/ROADMAP.md`'s Phase 3 effort line reads as a 1 d budget with the 2 d tripwire, and no other phase's section is modified — verified by a scoped diff.
    - `.planning/ROADMAP.md`'s Phase 3 BP-D-05 note states the default is binding per ADR-000 D-2 and no longer instructs an accept-or-override at task time.
    - `docs/RISK_REGISTER.md` has a new entry naming Phase 6 as owner, the committed export as the artifact, the aEUR-after-M4 interaction, and the reason the leak scan was not widened now.
    - `docs/BUILD_LOG.md`'s M2 entry records the actual effort against 1 d and 2 d, an explicit shed statement (taken with rank, or none taken), an index of every red-then-green proof by plan, and the three document corrections with their no-ADR-owed rationale.
    - `.planning/phases/03-warehouse/03-VALIDATION.md` contains no remaining `TBD` in the Plan or Wave columns.
    - `uv run pytest tests/unit/test_governance_checks.py tests/unit/test_repo_layout.py -q` exits 0 — the documentation edits do not break any governance guard.
    - `uv run python scripts/check_ssot_consistency.py` exits 0.
    - `make lint` exits 0; `make test` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit/test_governance_checks.py tests/unit/test_repo_layout.py -q && uv run python scripts/check_ssot_consistency.py && uv run python -c "import pathlib; t=pathlib.Path('.planning/phases/03-warehouse/03-VALIDATION.md').read_text(encoding='utf-8'); rows=[l for l in t.splitlines() if l.startswith('| T-2')]; assert rows, 'verification map rows not found — the check itself is broken'; assert not any('TBD' in r for r in rows), [r for r in rows if 'TBD' in r]; print('validation map filled')"</automated>
  </verify>
  <done>The three stale statements are corrected with the tracked rationale, the D-05 revisit is registered where it cannot be forgotten, and the M2 build log carries the phase's full evidence index.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm CI job 3 green on both matrix legs</name>
  <files>(none — this task changes no file; it confirms an external CI execution)</files>
  <read_first>
    - `.github/workflows/ci.yml` (as modified in Task 1 — the `dbt` job's matrix and new steps)
    - `.planning/phases/03-warehouse/03-VALIDATION.md` § Manual-Only Verifications (the row this checkpoint discharges)
    - `.planning/phases/03-warehouse/03-RESEARCH.md` § Assumptions Log item A2 (the Linux path-resolution claim the Ubuntu leg resolves)
    - `.planning/phases/01-repository-foundation/01-CONTEXT.md` D-11 and D-19 (the milestone branch `m2-warehouse-model` and the draft-pull-request-at-milestone-start convention)
  </read_first>
  <action>
Push the milestone branch, open the resulting GitHub Actions run, and confirm with the developer that
CI job 3 is green on both the `ubuntu-latest` and `windows-latest` matrix legs and that the other five
jobs are green with none skipped. Halt until the developer responds. Do not modify the workflow, the
Makefile, the committed export, or any gate in response to a red leg without first reporting the
failure and its log excerpt.
  </action>
  <what-built>
CI job 3 (`dbt`) now runs on a two-leg matrix — `ubuntu-latest` and `windows-latest` — building the
full dbt project on committed synthetic data with no private inputs, then regenerating
`exports/mmm_input_weekly.csv` and failing on any drift against the committed file. Everything else
in Phase 3 has been verified by automated tests on the local Windows machine.

This is the one thing that cannot be asserted from a local run. Two claims depend on an actual
GitHub Actions execution and nothing else can substitute for it:

  1. RESEARCH Assumptions Log A2 — the CWD-relative path resolution that puts the warehouse at
     `data/warehouse/ambo.duckdb` was empirically verified on Windows only. The Ubuntu leg is what
     confirms it holds on Linux.
  2. The export's byte-identity across operating systems — the `.gitattributes` LF pin plus the
     writer's explicit line-feed terminator should make the regenerated file identical on both legs,
     but a Windows-runner checkout introducing carriage returns would show up only here.
  </what-built>
  <how-to-verify>
1. Push the branch. Per Phase 1's D-11 and D-19, Phases 3 and 4 share the milestone branch
   `m2-warehouse-model`, with the milestone pull request opened as a draft at milestone start so
   `pull_request` CI fires on every push.
2. Open the Actions run for that push.
3. Confirm the `dbt` job reports **two** check runs, one per matrix leg, and that **both** are green.
4. Open the Windows leg's log and confirm three specific things: the make-install step ran and
   printed a GNU Make version; `make transform` completed with the dbt build green; and the export
   drift-check step produced no diff output.
5. Confirm the other five jobs (`lint`, `test`, `ssot`, `layer-order`, `leak`) are green and that
   none was skipped — a skipped job cannot satisfy a required status check (D-16).
6. Confirm job 1's season-windows seed diff-check is still green — this phase touched the seed's
   consumers but not the seed.

If the Windows leg fails on an export diff, that is a real portability finding about the LF pin and
must be fixed at its source. Do not relax the gate, do not add a Windows-specific exclusion, and do
not regenerate the committed file from the Windows runner to make the diff go away.

If any gate is red, EB-082 still applies: no force-push, no rebase, no amend. Fix forward.
  </how-to-verify>
  <resume-signal>Type "approved" once both `dbt` matrix legs and the other five jobs are green, or describe which leg failed and what its log showed.</resume-signal>
  <verify>
    <human-check>The developer confirms, from the GitHub Actions run for the pushed milestone branch, that job `dbt` reports two green check runs (one per matrix leg) and that `lint`, `test`, `ssot`, `layer-order` and `leak` are green with none skipped.</human-check>
  </verify>
  <done>Both `dbt` matrix legs are green on a real GitHub Actions execution, the Windows leg's log shows GNU Make installed, `make transform` green and an empty export drift-check, and the developer has typed "approved".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local dev machine to CI runners | The only place cross-platform divergence in the dbt invocation, path separators, or line endings becomes observable |
| committed export to CI drift gate | The gate is what makes the committed artifact a checked fact rather than a snapshot someone remembered to update |
| git history to the layer-order argument | The project's central claim is verifiable from history alone; history is append-only (EB-082) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-36 | Repudiation | A committed export silently diverging from the mart that produced it | high | mitigate | Job 3 regenerates via `make transform` then `make export` and fails on any diff under `exports/`, following job 1's proven seed-diff shape. A mart change that alters the export cannot merge unless the committed CSV is regenerated in the same pull request |
| T-03-37 | Tampering | Cross-platform divergence in path resolution, path separators, or line endings never reaching CI | high | mitigate | Job 3 gains a `windows-latest` matrix leg with the same Chocolatey make install job 2 uses, keeping the job name `dbt` so EB-060's six jobs stay six. The Ubuntu leg simultaneously resolves RESEARCH Assumptions Log A2, which was verified on Windows only |
| T-03-38 | Repudiation | A CI job passing by being skipped rather than by running | medium | mitigate | No job-level `if:` key is introduced; the Windows conditional is a step-level selector inside a job that runs to completion on both legs. Asserted by the automated YAML check and by the human checkpoint's explicit no-skipped-jobs confirmation |
| T-03-39 | Repudiation | The D-05 leak-scan revisit being forgotten between Phase 3 and Phase 6 | medium | mitigate | A `docs/RISK_REGISTER.md` entry naming Phase 6 as owner and the required action, plus an in-code pointer in `scripts/export_marts.py`'s header. Two independent homes, neither dependent on memory |
| T-03-40 | Tampering | A red gate resolved by rewriting history | high | mitigate | EB-082 and 09_ANTI_PATTERNS §A-17 forbid force-push, rebase and amend, including as a fix for a red gate; `scripts/check_layer_order.py` runs as CI job 5 on full history. The checkpoint's instructions restate this at the moment it would be tempting |
| T-03-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages installed. The Windows leg's `make` comes from the same Chocolatey package job 2 already uses, established and proven in Phase 1 (RESEARCH § Environment Availability) |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this plan produces (new symbols — not pre-existing, exclude from drift verification)

**CI:** the `dbt` job's `strategy.matrix.os` two-leg configuration, its Windows-conditional
make-install step, its `make export` step, and its `exports/` drift-check step (the job name `dbt`
is pre-existing and unchanged)

**Documentation:** a new `docs/RISK_REGISTER.md` entry for the D-05 leak-scan-scope revisit; the
closed M2 entry in `docs/BUILD_LOG.md`; the corrected Phase 3 effort-budget line and BP-D-05 note in
`.planning/ROADMAP.md`; the corrected T-205 AC-3 in `docs/EXECUTION_BLUEPRINT/02_WBS.md` (a
gitignored file, so the tracked record is the build-log entry); the completed per-task verification
map and sign-off in `.planning/phases/03-warehouse/03-VALIDATION.md`
</artifacts_this_phase_produces>

<verification>
- The YAML structure check passes: six jobs, `dbt` with a two-leg matrix, no job-level `if:` key.
- `make transform && make export && git diff --exit-code exports/` exits 0 locally.
- `uv run pytest tests/unit/test_governance_checks.py tests/unit/test_repo_layout.py -q` exits 0.
- `uv run python scripts/check_ssot_consistency.py` exits 0.
- `.planning/phases/03-warehouse/03-VALIDATION.md` has no remaining TBD in its Plan or Wave columns.
- Human confirmation that both `dbt` matrix legs and the other five CI jobs are green, with none skipped.
- `make lint` exits 0; `make test` exits 0.
</verification>

<success_criteria>
- `make transform` builds the dbt project green locally and in CI job 3, on committed synthetic data with no private inputs, on both Ubuntu and Windows (ROADMAP success criterion 1).
- A mart change that alters the export cannot merge without regenerating the committed CSV in the same pull request.
- EB-060's six CI jobs remain six.
- Every stale upstream statement this phase surfaced is corrected with a tracked rationale and no ADR owed.
- The M2 build log carries the phase's effort, sheds, evidence index, and departure notes.
</success_criteria>

<output>
Create `.planning/phases/03-warehouse/03-09-SUMMARY.md` when done.
</output>
