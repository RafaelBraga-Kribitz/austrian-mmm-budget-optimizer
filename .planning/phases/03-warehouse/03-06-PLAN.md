---
phase: 03-warehouse
plan: 06
type: execute
wave: 5
depends_on:
  - 03-05
files_modified:
  - dbt/models/marts/fct_platform_reported.sql
  - dbt/models/marts/_fct_platform_reported__schema.yml
  - dbt/tests/ad044_layer_r_channel_presence.sql
  - dbt/tests/ad043_no_unit_suffix_columns.sql
  - tests/fixtures/real_anon_fake/media_weekly.csv
  - tests/fixtures/real_anon_fake/outcome_weekly.csv
  - tests/fixtures/real_anon_fake/promo_calendar.csv
  - tests/fixtures/real_anon_fake/README.md
  - tests/unit/test_warehouse_build.py
autonomous: true
requirements:
  - REQ-q1-truth-recovery

must_haves:
  truths:
    - "`fct_platform_reported` exists as a contract-enforced table at week x layer x channel grain with columns week_start DATE, layer VARCHAR, channel VARCHAR, platform_conversions BIGINT, platform_conv_value DOUBLE, impressions BIGINT (D-07, D-24, SPEC-03 §3)."
    - "The `fct_platform_reported` contract text pre-declares that Layer R adds a `clicks` column at M4 as an anticipated extension recorded in the intake ADR, so it lands as a planned amendment rather than an ADR-worthy surprise break (D-24)."
    - "Offline-channel rows survive into `fct_platform_reported` with SQL NULL platform metrics, never coerced to 0 — a platform-absent channel and a platform-reported-zero channel are distinguishable."
    - "AD-044 exists as a singular test in the project and is dormant: it is gated on the `intake_manifest_present` var, which defaults to false, so it compiles and passes inert until Phase 6 activates it (T-203 AC-1, BP-D-05)."
    - "The `layer_r_present` jinja branch has actually executed end-to-end against a fixture: one dbt invocation with `layer_r_present: true` and `data_real_anon_path` pointed at `tests/fixtures/real_anon_fake/` builds every raw, staging and mart model green, including all contracts and the AD-040/041/043 tests (D-20)."
    - "In that fixture build, `dim_layer` gains a fourth row with layer R, weeks 12, monetary_unit aEUR, source_tag REAL-ANON, and channels_present listing `other` — proving the Layer-R-only channel (BP-D-04) flows through the pivot and the presence derivation correctly."
    - "The fixture is synthetic and obviously fake: it encodes shape, not values, uses round constant magnitudes, and could not be mistaken for or shaped by real client data (D-20, AGENTS A-4)."
    - "Ordering holds on the Layer R branch too: the fixture's twelve week_start values are consecutive ISO Mondays present in the committed seed, so the gapless-spine test passes for layer R without any special-casing."
  artifacts:
    - dbt/models/marts/fct_platform_reported.sql
    - dbt/models/marts/_fct_platform_reported__schema.yml
    - dbt/tests/ad044_layer_r_channel_presence.sql
    - tests/fixtures/real_anon_fake/media_weekly.csv
    - tests/fixtures/real_anon_fake/outcome_weekly.csv
    - tests/fixtures/real_anon_fake/promo_calendar.csv
    - tests/fixtures/real_anon_fake/README.md
  key_links:
    - "`fct_platform_reported` is Phase 8's DC-702 attribution-gap input. Freezing the Layer P surface now gives Phase 8 something stable to build against during the drop-blocked window."
    - "The `layer_r_present` branch written in plan 03-01 plus the fixture built here is what Phase 6 inherits: a flag flip onto a path that has already executed, rather than dead SQL running for the first time in a phase that also carries a permission gate, a prior freeze, and real client data."
    - "`intake_manifest_present` gates AD-044 independently of `layer_r_present`, so the fixture run exercises the Layer R data path without requiring the Phase-6-owned intake-channel seed to exist."
  prohibitions:
    - "The Layer R fixture must be synthetic and obviously fake — never derived from, shaped by, or containing real client data, real spend or revenue magnitudes, campaign names, client identity, or anonymization factors. The Layer R branch is never executed against a real private drop in this phase (AGENTS A-4, A-2, D-20)."
---

<objective>
Build `fct_platform_reported`, ship AD-044 in its dormant form, and — the point of this plan —
actually execute the `layer_r_present` branch against a fake fixture so Phase 6 flips a flag onto a
path that has already run.

Purpose: BP-D-05's guarded union is ratified and ships whether or not it is exercised. Shipping it
unexercised means the first execution of that SQL happens in Phase 6, the phase that also carries a
permission gate, a prior freeze, and real client data in play. D-20 moves that first execution here,
where a failure costs an afternoon instead of a milestone.

Output: the third contract-enforced mart, the dormant AD-044 test, a small obviously-fake Layer R
fixture, and one pytest that runs a full `dbt build` with the flag on.

Standing obligations (D-18, D-19): tests ship with what they verify. The `fct_platform_reported`
enforced contract is shed item 2 alongside `dim_layer`'s and, if shed, must be written into
`docs/BUILD_LOG.md` first.

**Anonymization is absolute (AGENTS A-4).** The fixture in this plan is authored from nothing but
the SPEC-02 §5.2 taxonomy and the BP-D-03 column names. If at any point real client data, a real
magnitude, or a real name would make the fixture "more realistic", that is the signal to stop, not
to proceed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-warehouse/03-CONTEXT.md
@.planning/phases/03-warehouse/03-RESEARCH.md
@.planning/phases/03-warehouse/03-PATTERNS.md
@.planning/phases/03-warehouse/03-05-SUMMARY.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: fct_platform_reported with a frozen Layer P surface and a pre-declared Layer R extension</name>
  <files>dbt/models/marts/fct_platform_reported.sql, dbt/models/marts/_fct_platform_reported__schema.yml, dbt/tests/ad043_no_unit_suffix_columns.sql</files>
  <read_first>
    - `dbt/models/staging/stg_media_weekly.sql` (written in plan 03-03 — the eight staging columns, including the NULL `clicks` on Layer P)
    - `dbt/models/marts/_dim_layer__schema.yml` and `_fct_mmm_input__schema.yml` (written in plans 03-04 and 03-05 — the contract form and description discipline to match)
    - `docs/SPEC-03_data_model.md` §3 (the `fct_platform_reported` row)
    - `docs/SPEC-06_decision_layer.md` §5 (what DC-702 consumes from this mart — the reason to freeze it now)
    - `.planning/phases/03-warehouse/03-CONTEXT.md` D-24
    - `data/synthetic/s_a/media_weekly.csv` (a `print_regional` row — confirm the platform metrics are empty, not zero)
    - `dbt/tests/ad043_no_unit_suffix_columns.sql` (written in plan 03-03, extended in plan 03-05 — the hint list to extend once more)
  </read_first>
  <action>
Create `dbt/models/marts/fct_platform_reported.sql`, grain week x layer x channel, materialized as a
table. It is a projection of `{{ ref('stg_media_weekly') }}` selecting exactly six columns with
explicit casts: `week_start` to date, `layer` to varchar, `channel` to varchar,
`platform_conversions` to bigint, `platform_conv_value` to double, `impressions` to bigint. No
aggregation, no filtering, no coalescing — the staging grain is already this mart's grain, and NULL
platform metrics on offline channels must survive as NULL. Coercing them to 0 would make a channel
that reports nothing indistinguishable from a channel that reports zero, which is precisely the
confusion AGENTS T-8 warns about in the attribution-gap module that consumes this mart.

`clicks` is deliberately not projected. It is NULL on every Layer P row today and arrives with real
content only at M4.

Create `dbt/models/marts/_fct_platform_reported__schema.yml` with `contract: {enforced: true}`,
`materialized: table`, and the six columns declared with `name` and `data_type`. No inline
`constraints:` block, for the same DuckDB support reason as plans 03-04 and 03-05. Add a model-level
`unique` test on the expression concatenating `week_start` cast to varchar, `layer` and `channel`
joined by a pipe delimiter, and column-level `not_null` on the three grain columns only —
`platform_conversions`, `platform_conv_value` and `impressions` are legitimately NULL for offline
channels and must not carry a not_null test.

Give the model and all six columns a `description:`. `platform_conv_value`'s description must state
that it is the platform-reported conversion value per week, layer and channel in the layer's own
monetary unit, that it is the BP-D-03 rename of the producing side's `platform_revenue_eur`, and
that it is the OBJECT of study in the attribution gap, never a calibration target (AGENTS T-8).
`platform_conversions` and `impressions` descriptions must state that NULL means the channel reports
no platform data at all, distinct from a reported zero.

The model-level description carries D-24's pre-declaration and is the load-bearing text of this
task. State that the Layer P surface is frozen at these six columns as of Phase 3; that Layer R adds
a `clicks` column at M4 as an anticipated extension recorded in the intake ADR; and that adding
`clicks` at that point is therefore a planned contract amendment rather than a contract break
requiring its own ADR. Phase 8 builds DC-702 against the frozen six during the drop-blocked window
and must not be surprised by the seventh.

Extend `dbt/tests/ad043_no_unit_suffix_columns.sql` by adding a `-- depends_on:` hint line for
`fct_platform_reported`, completing the AD-043 scan's coverage of every staging and mart relation.
Do not rewrite the file; add the one hint line.
  </action>
  <acceptance_criteria>
    - `uv run dbt build --project-dir dbt --profiles-dir dbt` exits 0 and reports `fct_platform_reported` built as a contract-enforced table.
    - `fct_platform_reported` has exactly 2028 rows and exactly 6 columns, in ordinal order `week_start`, `layer`, `channel`, `platform_conversions`, `platform_conv_value`, `impressions`.
    - A count of `fct_platform_reported` rows where `channel` is `print_regional` and `platform_conversions` is NULL is greater than zero — offline NULLs survived.
    - A count of rows where `platform_conversions` equals 0 and `channel` is `print_regional` returns 0 — no NULL was coerced to zero.
    - `_fct_platform_reported__schema.yml` declares `contract: {enforced: true}`, six `columns:` entries each with `name` and `data_type`, and contains no `constraints:` key.
    - The model-level `description` states all three parts of the D-24 pre-declaration: the frozen Layer P surface, the `clicks` addition at M4, and that it is a planned amendment recorded in the intake ADR.
    - `dbt/tests/ad043_no_unit_suffix_columns.sql` now carries dependency hints for all four staging models and all three marts.
    - `dbt build` reports the `fct_platform_reported` grain `unique` test as PASS.
  </acceptance_criteria>
  <verify>
    <automated>uv run dbt build --project-dir dbt --profiles-dir dbt && uv run python -c "import duckdb; c=duckdb.connect('data/warehouse/ambo.duckdb', read_only=True); assert c.execute('select count(*) from fct_platform_reported').fetchone()[0]==2028; cols=[r[0] for r in c.execute(\"select column_name from information_schema.columns where table_name='fct_platform_reported' order by ordinal_position\").fetchall()]; assert cols==['week_start','layer','channel','platform_conversions','platform_conv_value','impressions'], cols; assert c.execute(\"select count(*) from fct_platform_reported where channel='print_regional' and platform_conversions is null\").fetchone()[0]>0; print('fct_platform_reported OK')"</automated>
  </verify>
  <done>The third mart is contract-enforced at week x layer x channel grain, offline NULLs are preserved, and the Layer R `clicks` extension is pre-declared as a planned amendment.</done>
</task>

<task type="auto">
  <name>Task 2: AD-044 in its dormant form, gated independently of layer_r_present</name>
  <files>dbt/tests/ad044_layer_r_channel_presence.sql</files>
  <read_first>
    - `dbt/models/marts/dim_layer.sql` (written in plan 03-05 — the `channels_present` format AD-044 compares against)
    - `docs/SPEC-03_data_model.md` §4 AD-044
    - `docs/EXECUTION_BLUEPRINT/00_MASTER_PLAN.md` §5 BP-D-05 (line 109 — the `layer_r_present` var and the `intake_channels.csv` seed for AD-044) and BP-D-19
    - `docs/EXECUTION_BLUEPRINT/10_VALIDATION_GATES.md` §4 G-DATA-W (lines 60-66 — where AD-044 sits in the warehouse gate, marked M4)
    - `dbt/dbt_project.yml` (the `intake_manifest_present` var written in plan 03-01)
  </read_first>
  <action>
Create `dbt/tests/ad044_layer_r_channel_presence.sql` as a singular test wrapped in a jinja
conditional on `var('intake_manifest_present')`. The false branch — the only branch that renders in
Phase 3 — is a zero-row select, so the test compiles, appears in `dbt build` output, and passes
inert. The true branch, which Phase 6 activates, compares the comma-split channel list from
`dim_layer` for layer R against the intake-channel seed and returns the symmetric difference.

Two design points to record in a header comment, because both are planner decisions a reviewer would
otherwise question.

First, why the gate is `intake_manifest_present` and not `layer_r_present`. BP-D-05 names both the
`layer_r_present` var and an `intake_channels.csv` seed for AD-044, and Phase 6 owns that seed. If
AD-044 were gated on `layer_r_present` alone, this plan's D-20 fixture run — which sets
`layer_r_present` true — would render a `ref()` to a seed that does not exist yet, and the run would
fail at parse time for a reason unrelated to what it is testing. A second, independent var lets the
Layer R *data path* be exercised now while the *manifest comparison* stays dormant until its seed
lands. Phase 6 flips both. This does not weaken AD-044: the test is absent from the M4 gate today
either way, and G-DATA-W already marks it M4.

Second, that the true branch is written but has never executed, and that Phase 6 must treat its first
run as new code — unlike the Layer R data path, which this plan does exercise. Say so plainly rather
than letting a reader assume the whole test was proven.

Add a `-- depends_on:` hint for `dim_layer` so the test is scheduled after that mart materializes in
both branches.

Do not create `dbt/seeds/intake_channels.csv` in this phase. It is Phase 6's artifact, and a
header-only placeholder committed now would be a second, empty home for a fact Phase 6 owns.
  </action>
  <acceptance_criteria>
    - `uv run dbt build --project-dir dbt --profiles-dir dbt` exits 0 and its output names `ad044_layer_r_channel_presence` as PASS.
    - `dbt/seeds/intake_channels.csv` does not exist.
    - Setting `intake_manifest_present` to true without the seed present makes `dbt build` fail at parse time with a message naming the missing `intake_channels` node — confirming the gate is real and the true branch is genuinely wired to the seed. Record the captured message in the plan SUMMARY, then revert the var.
    - The test file's header comment states both design points: why the gate is a separate var, and that the true branch has never executed.
    - The test carries a dependency hint for `dim_layer`.
  </acceptance_criteria>
  <verify>
    <automated>uv run dbt build --project-dir dbt --profiles-dir dbt && test ! -f dbt/seeds/intake_channels.csv</automated>
  </verify>
  <done>AD-044 is present in the project and dormant, gated by a var that lets the Layer R data path be exercised now without borrowing Phase 6's seed.</done>
</task>

<task type="auto">
  <name>Task 3: The obviously-fake Layer R fixture and one dbt build with the flag on</name>
  <files>tests/fixtures/real_anon_fake/media_weekly.csv, tests/fixtures/real_anon_fake/outcome_weekly.csv, tests/fixtures/real_anon_fake/promo_calendar.csv, tests/fixtures/real_anon_fake/README.md, tests/unit/test_warehouse_build.py</files>
  <read_first>
    - `dbt/models/raw/raw_media_r.sql`, `raw_outcome_r.sql`, `raw_promo_r.sql` (written in plan 03-01 — the exact column names each guarded branch reads, which the fixture must supply)
    - `dbt/models/staging/stg_media_weekly.sql`, `stg_outcome_weekly.sql`, `stg_promo.sql` (written in plan 03-03 — the Layer R union branches)
    - `dbt/seeds/season_windows.csv` (confirm 2024 ISO Mondays are covered — the fixture's weeks must exist in the seed or the gapless-spine test will fail for layer R)
    - `tests/unit/test_warehouse_build.py` (as written in plans 03-01 through 03-05 — the `_dbt_env` helper and `test` target invocation pattern to reuse)
    - `docs/SPEC-02_agency_data_pipeline.md` §5.2 (the fixed seven-channel taxonomy the fixture's channels must come from)
    - `AGENTS.md` §1 A-4 (anonymization is absolute — the constraint governing every value in this fixture)
    - `scripts/leak_scan.py` (the CI scanner that walks the whole tree including `tests/fixtures/`)
  </read_first>
  <action>
Author a minimal, obviously-fake Layer R fixture under `tests/fixtures/real_anon_fake/`. It encodes
shape, not values. Every number is a round constant chosen to be self-evidently synthetic; nothing in
it is derived from, shaped by, or proportional to any real client figure. If a value would be more
"realistic" by resembling something real, that is the signal to stop.

Twelve consecutive ISO Mondays beginning 2024-01-01 (which is the ISO Monday of 2024 week 1 and is
present in the committed seed, so the D-15 gapless-spine test passes for layer R with no
special-casing). Three channels from the SPEC-02 §5.2 taxonomy: `search_brand`, `meta`, and `other`.
Including `other` is deliberate — it is the Layer-R-only channel under BP-D-04, so the fixture is what
proves `spend_other` becomes non-zero and `other` becomes listed in `channels_present` once a Layer R
exists. Three channels rather than seven is deliberate too: it exercises the partial-taxonomy case
Layer R will actually present.

`media_weekly.csv` carries the seven Layer R media columns in the order `raw_media_r.sql` projects —
`week_start, channel, spend_aeur, clicks, impressions, platform_conversions, platform_conv_value` —
for 36 rows (12 weeks x 3 channels), with round constant magnitudes per channel.
`outcome_weekly.csv` carries `week_start, revenue_aeur, orders` for 12 rows, with revenue strictly
greater than zero on every row so AD-041 holds. `promo_calendar.csv` carries
`week_start, promo_flag` for 12 rows with values in 0 and 1 only. Write all three LF-only with a
header row, matching the repository's `.gitattributes` pin.

`README.md` in the same directory states in one paragraph: this data is fabricated for the sole
purpose of exercising the `layer_r_present` jinja branch before real anonymized data exists; it
contains no client identity, no real magnitude, and no anonymization factor; it must never be treated
as a sample of real agency data; and it is scanned by the leak-scan CI job along with the rest of the
tree.

Extend `tests/unit/test_warehouse_build.py` with `test_layer_r_branch_builds_green_against_fixture`.
It runs one `dbt build` against the `test` target using `_dbt_env(tmp_path)` for an isolated tmp
warehouse and target path, passing `--vars` with `layer_r_present` true and `data_real_anon_path` set
to the fixture directory rendered with `Path.as_posix()`. It asserts `returncode == 0` — every model,
every contract, and every test green with the flag on — and then connects read-only to the tmp
warehouse and asserts the Layer R shape: `dim_layer` has 4 rows; the R row has weeks 12,
monetary_unit aEUR, source_tag REAL-ANON, and `channels_present` exactly
`search_brand,meta,other` in taxonomy order; `fct_mmm_input` has 350 rows, of which 12 are layer R;
every layer R row has `spend_other` greater than 0 while `spend_radio`, `spend_search_generic`,
`spend_display_video` and `spend_print_regional` are all exactly 0.0; and `fct_platform_reported` has
2064 rows. Assert finally that the real `data/warehouse/ambo.duckdb` was not modified — the fixture
run must be fully isolated.

Include the last 40 lines of captured output in every assertion message. A red result here is the
finding this plan exists to produce, and it must be diagnosable without re-running.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_warehouse_build.py -q` exits 0 with all tests passing, including `test_layer_r_branch_builds_green_against_fixture`.
    - The fixture build exits 0 with `layer_r_present` true — all raw, staging and mart models plus all contracts and the AD-040, AD-041, AD-043 and both-directions tests green on a four-layer warehouse.
    - In the fixture warehouse, `dim_layer` has 4 rows and the R row reads weeks 12, monetary_unit `aEUR`, source_tag `REAL-ANON`, channels_present exactly `search_brand,meta,other`.
    - In the fixture warehouse, `fct_mmm_input` has 350 rows, 12 of them layer R, every layer R row has `spend_other` greater than 0, and `spend_radio`, `spend_search_generic`, `spend_display_video`, `spend_print_regional` are all exactly 0.0 on those rows.
    - In the fixture warehouse, `fct_platform_reported` has 2064 rows.
    - The real `data/warehouse/ambo.duckdb` modification time is unchanged by the fixture run.
    - `tests/fixtures/real_anon_fake/media_weekly.csv` has 37 lines (header plus 36 rows); `outcome_weekly.csv` and `promo_calendar.csv` have 13 lines each; all three are LF-only.
    - `uv run python scripts/leak_scan.py` exits 0 with the fixture committed.
    - `tests/fixtures/real_anon_fake/README.md` states that the data is fabricated, contains no client identity or real magnitude, and must never be treated as a sample of real agency data.
    - `uv run pytest tests/unit/test_repo_layout.py tests/unit/test_line_endings.py -q` exits 0 — the new fixture directory does not violate the SPEC-08 §2 layout guard or the LF pin.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit/test_warehouse_build.py tests/unit/test_repo_layout.py tests/unit/test_line_endings.py -q && uv run python scripts/leak_scan.py</automated>
  </verify>
  <done>The `layer_r_present` branch has executed end-to-end and green against an obviously-fake fixture, producing a four-layer warehouse with `other` correctly present only on Layer R. Phase 6 inherits a path that has run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture CSV to Layer R raw view | The first non-synthetic-scenario file path the warehouse reads; in Phase 6 this same boundary receives real anonymized data |
| `tests/fixtures/real_anon_fake/` to the public repository | The repository goes public at M3; anything committed under `tests/` is published |
| `fct_platform_reported` to Phase 8's DC-702 | The attribution-gap module treats platform numbers as the object of study, never a calibration target |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-21 | Information Disclosure | The fake Layer R fixture leaking client identity, real magnitudes, or anonymization factors | high | mitigate | Fixture authored solely from the SPEC-02 §5.2 taxonomy and BP-D-03 column names with round constant values; a `README.md` in the directory states its nature; `scripts/leak_scan.py` must exit 0 with the fixture committed; AGENTS A-4 governs and the plan's prohibition restates it |
| T-03-22 | Elevation of Privilege | The Layer R branch executing against a real private drop during Phase 3 | high | mitigate | The only true-valued `layer_r_present` invocation in this phase also sets `data_real_anon_path` to the fixture directory; `data/real_anon/` remains empty this phase; AGENTS A-2 forbids any Layer R fit before the M3 gates and `scripts/check_layer_order.py` runs in CI |
| T-03-23 | Tampering | The fixture run overwriting the real warehouse or dbt target directory | medium | mitigate | The run uses the `test` profile target with `AMBO_TEST_WAREHOUSE` and `DBT_TARGET_PATH` in `tmp_path`; the test asserts the real warehouse's modification time is unchanged |
| T-03-24 | Spoofing | An offline channel's absent platform data coerced to a reported zero | high | mitigate | `fct_platform_reported` projects staging NULLs unchanged with no coalesce; the three metric columns carry no `not_null` test; acceptance criteria assert both the presence of NULLs and the absence of coerced zeros on `print_regional` |
| T-03-25 | Repudiation | AD-044 appearing to be proven when only its dormant branch has ever run | medium | mitigate | The test's header comment states plainly that the true branch has never executed and that Phase 6 must treat its first run as new code; the parse-time failure proof confirms the true branch is genuinely wired to the seed rather than dead text |
| T-03-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages installed; the fixture is three hand-authored CSV files and the test reuses the existing subprocess harness (RESEARCH § Package Legitimacy Audit: not applicable) |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this plan produces (new symbols — not pre-existing, exclude from drift verification)

**Files:** `dbt/models/marts/fct_platform_reported.sql`,
`dbt/models/marts/_fct_platform_reported__schema.yml`,
`dbt/tests/ad044_layer_r_channel_presence.sql`,
`tests/fixtures/real_anon_fake/media_weekly.csv`,
`tests/fixtures/real_anon_fake/outcome_weekly.csv`,
`tests/fixtures/real_anon_fake/promo_calendar.csv`,
`tests/fixtures/real_anon_fake/README.md`

**dbt models:** `fct_platform_reported`

**dbt tests:** `ad044_layer_r_channel_presence` (singular, dormant), the model-level `unique`
grain test on `fct_platform_reported`, and its three grain-column `not_null` tests

**Python symbols:** `tests/unit/test_warehouse_build.py::test_layer_r_branch_builds_green_against_fixture`
</artifacts_this_phase_produces>

<verification>
- `uv run dbt build --project-dir dbt --profiles-dir dbt` exits 0 with three contract-enforced marts and every AD-040 through AD-044 test reported.
- `uv run pytest tests/unit/test_warehouse_build.py -q` exits 0, including the Layer R fixture build.
- `uv run python scripts/leak_scan.py` exits 0 with the fixture committed.
- `fct_platform_reported` is 2028 rows x 6 columns with offline NULLs preserved.
- The AD-044 parse-time gate proof is recorded in the plan SUMMARY.
- `make lint` exits 0; `make test` exits 0.
</verification>

<success_criteria>
- All three marts are contract-enforced, completing D-07.
- The Layer P platform surface is frozen and the Layer R `clicks` extension is pre-declared.
- AD-044 is present and dormant without borrowing a Phase 6 artifact.
- The `layer_r_present` branch has executed green end-to-end against a fixture that is unmistakably synthetic.
</success_criteria>

<output>
Create `.planning/phases/03-warehouse/03-06-SUMMARY.md` when done.
</output>
